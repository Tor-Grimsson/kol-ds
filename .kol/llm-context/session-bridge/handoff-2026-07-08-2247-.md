# Handoff — 2026-07-08 22:47

## Goal of the current arc
Close out the button-vanish bug for real and land the "chrome law" it uncovered: every control is Button chrome — two variants (primary; outline always secondary), one size scale (sm 26 / md 32 / lg 40). Both are landed; the arc is in the review/polish tail.

## Last actions taken (causal trail, newest first)
- Textarea polish loop with the user live on `/demo`: grip = new v1 icon `resize-grip` (user's Figma cut, 3rd iteration — full-bleed diagonal), 12px render, `vector-effect: non-scaling-stroke` (else 1.5 keyline scales to 0.625px mush), `overflow: visible` (round caps sit on viewBox edge), seated 4px/4px; grip is the REAL drag handle (JS corner drag, both axes, min 120×40) with native `resize` OFF; fixed the pre-existing double-padding cascade bug (`.kol-control.kol-control--textarea` now out-specifies the size classes); Spec class-list gap-1.
- Built `/demo` (showcase route, `showcase/src/pages/Demo.jsx`): all reviewed components live + per-section Spec blocks (every class emitted, every state→token). User reviews here now, NOT the artifact.
- Landed the chrome law (changeset `chrome-law-controls`): Dropdown trigger emits `kol-btn kol-btn-{primary|outline} kol-btn-{size}` (inline styles deleted, open state fused, aliases default/subtle→primary + minimal→outline), Input/Textarea ghost→outline alias, ToggleSwitch rewritten (bare default, shells, sizes, no auto-uppercase), control--outline border → oq-16.
- Landed the button-states spec (changeset `btn-hover-touch-guard`, rewritten): oq fills per the user's stop choices, focus ring, active steps, pressed = solid inverted ink; sibling fixes tag (oq-12 — user dialed down from 24), switch track oq-16, control--ghost opaque.
- Docs synced along the way: 01-tokens (fg-vs-oq law), 02-icons INDEX 107→108, color.js gained fg-inverse + oq sections, component-docs.js variant rows updated.

## Current state / open decision points
- **3 changesets held:** `btn-hover-touch-guard`, `chrome-law-controls`, `icon-resize-grip`. One batched publish when the user unparks (merge the Version Packages PR, then `git pull` immediately — standing gotcha).
- **Queue agreed with user:** (1) unpark publish, (2) F6 dead `.kol-btn-selected` — define or delete, (3) F7 ghost label contrast (oq-48 ≈ 3.4:1, under AA; nudging to 64 shifts every app — deliberate call), (4) Slider still ships default/minimal/subtle (off-law), (5) legacy-alias sweep (ghost/minimal/subtle/plain/control) before the next major.
- Artifact (https://claude.ai/code/artifact/1d9ad975-e15c-4821-945e-39502cb2a53a) was the review surface for the spec-design phase; superseded by `/demo` for component review. Its §3 "reference button" section is obsolete; harmless.
- No session log written for this arc (user chose handoff only); the ledger `backlog/2026-07-08-button-chrome-audit.md` carries the audit + spec detail.

## Next intended action
- User reviews `/demo` fresh after context clear; then unpark the 3-changeset publish, then F6/F7 decisions.

## Working memory not yet in AGENT-CONTEXT
- **User's browser is Firefox.** Never reach for webkit-only tricks (`::-webkit-resizer` cost a full rage-cycle); Firefox's native textarea grip cannot be hidden except via `resize: none` — that's WHY the grip is a JS drag handle.
- **Vite gotcha confirmed live:** NEW files under the icons `import.meta.glob` need a dev-server restart; HMR covers edits only. The user's active dev server was on **5174**; something else (node PID 12168, not ours) listens on 5199 — possibly an orphan he may want to kill himself.
- **Review protocol this user demands** (learned the hard way): specimens must be the REAL component (never re-authored lookalikes), matrices are frozen snapshots (interactivity lives in one live strip), every cell labeled with the actual token name (never property descriptions like "opaque fill"), one component per section, and ship the URL fast — no playwright loops while he waits, verify only when the change is structural.
- Answer questions before acting; he interrupts hard when a question goes unanswered.
- The em-dash mojibake seen when serving the artifact via python http.server is a charset artifact of local serving only — the published artifact and vite are clean.
