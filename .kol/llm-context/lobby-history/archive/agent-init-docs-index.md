# agent-init-docs-index

**Filed:** 2026-07-31 → **dotfiles**
**Entry:** `~/.dotfiles/lobby/done/agent-init-docs-index.md`
**Ledger:** `~/.dotfiles/lobby/INDEX.md` — **the truth about this ticket**
**Last known:** 🟢 `closed` · synced 2026-08-01

## Why it went there

The fix is two skill files in dotfiles (`ag-init`, `agent-init`), not anything in
this repo. Three sessions running, every wrong proposal here came from grepping
source for a rule already written down in `docs/documentation/` — so the boot
sequence had to load the vault index, not just `.kol/` history.

## What stays here

Keeping `docs/documentation/INDEX.md` worth loading — the READ-FIRST block and its
precedence line are this repo's to maintain.

---

## ✅ RETURNED — 2026-08-01

🟢 `closed` in **dotfiles** — step 3 + step 8 applied to both `/ag-init` and
`/agent-init`: they now read `docs/documentation/INDEX.md` at boot and name the
sections governing the work at hand.

**Remainder here:** `none`. The index already carries the READ-FIRST block and the
precedence reversal (docs define · code is state · gates are proof), so the landing
skill has something to land on.

*(Receipt written 2026-08-01, backdated from the entry — it predates the receipt
convention.)*
