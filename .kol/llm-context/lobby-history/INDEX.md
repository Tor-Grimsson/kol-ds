# Lobby history

The lobby's processed records, graduated out of `lobby/` on **2026-08-01** so the
queue holds nothing but live work. **119 files.**

| folder | holds | count |
|---|---|---|
| `done/` | 🟢 closed — specs that were built, each carrying its `## ✅ RESOLUTION` | 108 |
| `archive/` | ⚪ parked · ⚫ retired · ownership and deferral notes · the three returned receipts | 11 |

**Nothing in here is pending.** This is history — the same class of material as
`session-log/`, which is why it lives in `.kol/` and not in `docs/`. The live
queue and its ledger stay at `lobby/INDEX.md`; that ledger keeps the state
vocabulary, this repo's bar for 🟢, and the dated History of what was processed.

Read a record here to answer *"was this component built, and what shipped?"* —
the resolution names the version. Protocol:
`~/.dotfiles/docs/operations/systems/lobby/`.
