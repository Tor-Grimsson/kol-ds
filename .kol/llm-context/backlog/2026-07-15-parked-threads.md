# Parked threads — 2026-07-15 milestone sweep

Open items closed out of AGENT-CONTEXT at the showcase-truth-rebuild milestone.
Each is parked deliberately, not forgotten — pick up on an explicit ask.

- **Lobby INDEX.md queue-table rewrite** — table still lists ~54 rows that live in `done/`;
  real queue = 4 (ParaType · InteractiveImage · TailwindContentSource · TypeScaleSection).
  Rewrite proposed 2026-07-15, user never green-lit.
- **ComboLab CSS `text-transform: uppercase` ×3** — JSX-coordinated conformance pass
  (author labels in-case, drop the CSS rules). From the overnight raid.
- **green/purple ramps + chart palette** — provisional hexes need a real categorical-palette
  design pass. From the raid (bucket D defaults).
- ~~**SplitToolButton ⇄ ShapeDropdown reconciliation**~~ — **RULED 2026-07-15: reasoned keep.**
  Deliberately distinct contracts (two-button split vs single-trigger arm-and-open); the trio
  ruling (incl. Dropdown) is law in `docs/documentation/03-components/05-control-chrome.md`.
- **Ghost resting-label AA contrast (`oq-48`)** — live again since ghost was un-retired
  (2026-07-15 ruling); needs a contrast pass.
- **JSDoc headers for chess/dashboards packages** — components without `Name — sentence.`
  headers ride authored fallbacks ('' for chess/dashboards) on their doc pages; adding headers
  = package edits + bumps.
- **Home hero copy understates the surface** (audit P2-4) — describes ~kol-component only;
  user-facing text, owner's wording call.
- **Doc-sync manifest dead entries** — hook points at `01-foundations/04-layout-breakpoints.md`
  and `04-compositions/09-dashboards-system.md`, neither exists; prune or write them.
- **CollectionLanding pre-existing `uppercase` utility classes** — type-conform sweep material.
- **VISUAL AUDIT of the whole showcase** (user: "riddled with bugs") — Todoist reminder set
  2026-07-15, due 07-16. Priority surfaces: the 72 newly-rostered component pages (first
  render ever), ⌘K search, provenance badges, Foundations live tables, Home bento off the
  real kol-dashboards package.
