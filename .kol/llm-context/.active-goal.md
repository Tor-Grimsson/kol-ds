status: done
iter: 0
max: 30
goal: COMPLETE 2026-08-01 14:52. CLOSING PASS — seal today's three arcs so the vault and the next agent carry the laws, not just the code. Work top to bottom, DO NOT STOP until done. 14 gates clean + production build green at the end.

(C1) SYNC docs/ for the five things that exist only in code comments:
  - Table WIDTH CONTRACT (`width="panel"|"column"`, the table caps itself, the gate bans hand-wrapping) → docs/documentation/01-foundations/05-layout-systems.md + the Table row in 03-components/01-inventory.md
  - DocTabs (one tab strip, chip|plain, three copies collapsed) → 03-components/01-inventory.md + 05-control-chrome.md
  - VARIANT PREVIEW (a demo exports `variants`, picker rides PreviewCard's toolbar) → docs/operations/03-showcase/ (the showcase's own machinery)
  - TagPath (namespace dims, leaf carries; shared by the overlay and the frontmatter panel) → 01-inventory.md
  - set-membership (in_sets vs used_in, derived from sets+blocks+demos+pages) → docs/operations/04-content-pipeline/01-sources.md, beside the readers table already there
  Bump every touched doc's `updated:`.

(C2) BUMP kol-workshop. It took real edits this session (DocKit props chips + sortable, DocsFrontmatter icons/labels/TagPath, tags/TagPath.jsx new, tags/index.js export) and was never bumped: 0.10.0 -> 0.11.0.

(C3) SQUARE THE LOBBY. lobby/INDEX.md still says "2 live entries" — re-count against inbox/ and fix the header and any stale row. Report the true count; do NOT reclassify anything, statuses are the user's call.

(C4) UPDATE AGENT-CONTEXT.md. Its newest entry is the rail-ladder arc; nothing about MediaLibrary, the lobby closures, the references/search surface, or the twelve callouts. Add ONE 2026-08-01 entry covering all three arcs, respect the 5-entry cap by trimming the oldest with a pointer.

(C5) Then the user runs /log-work-milestone — leave the repo in a state where that log has everything it needs: playbook current, versions correct, lobby square, context updated.

VERIFICATION: gates 14/14 and production build green at the close. No new ports needed.
git is the user's — never run a git command.
