status: done
iter: 0
max: 30
goal: ThemeToggle rework per the APPROVED 2-variant spec: variants button/flush, props fill(subtle|none)/iconLeft/iconRight/label/fullWidth/size, legacy variant aliases (icon/button/hop/hop-bare) render their old chrome verbatim, system slot = the split circle (no third glyph ever), publish framework 0.9.0, docs synced (ThemeToggle.mdx + SHIPPED-PACKAGES + api extraction), verified live in the showcase
