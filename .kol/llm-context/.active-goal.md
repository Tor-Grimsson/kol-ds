status: done
iter: 0
max: 30
session: 42d20819-c5c7-4659-bf6d-bfd5fe71b666
goal: TAGS THAT DISCRIMINATE, then the folder moves the audit found. 72 of 78 docs carry `domain/design-system` and 20 of 29 tags appear exactly once — the set fails at both ends, so the rail's Tags section and the graph are noise. Rewrite every tags block on three axes, gate the shape, then move the four misfiled things. Work top to bottom, do not stop to ask.
- [x] T1 `domain/design-system` is DELETED, not redistributed — it is the vault's own name and every page in it qualifies, which is why it says nothing.
- [x] T2 THREE AXES per doc: `domain/<subject>` (what it is about) · `audience/agency-internal` on repo machinery only · `pattern/<shape>` only where one genuinely applies. Never a tag that covers more than half the vault.
- [x] T3 Rewrite the `tags:` block in all 78 vault docs against the audit's chapter verdicts. Every tag from the closed namespace set in `.kol/docs-framework/03-tag-taxonomy.md`; if a subject needs a namespace that does not exist, say so out loud rather than inventing one.
- [x] T4 THE GATE — `validate:tags`: every doc has >= 2 tags, every tag is in the closed namespace set, and NO tag covers more than half the vault. Negative-test all three.
- [x] T5 MOVE `06-research/workflows/` (6 pages) to `operations/06-workflows/`. Repoint every wikilink.
- [x] T6 `06-research` then holds benchmark alone, under the 3-page minimum — split benchmark's 143-line index into three pages and make 06-research the benchmark chapter.
- [x] T7 MOVE `08-breakpoints/03-methods.md` to operations (a Chrome/Xcode/Playwright test rig is machinery) and `03-showcase/02-doc-card-sets.md` to `01-foundations` (it scopes kol-doc-*/kol-card-* type roles). Keep every chapter at 3+ pages after the moves.
- [x] T8 `05-reference-graph` is the undecidable one — repo machinery, DS subject. Leave it in operations and WRITE DOWN why, so it is a decision rather than an oversight.
- [x] T9 Gates clean, docs synced, playbook appended.
