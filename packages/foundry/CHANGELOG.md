# @kolkrabbi/kol-foundry

> Started 2026-08-14 at 0.5.5 — earlier versions shipped without entries (that history
> lives in the repo's session logs). From here every publish adds an entry, and breaking
> or global-surface changes are flagged **BREAKING**.

## 0.5.6 — 2026-08-15

- `TypefaceVariablePreview`'s heading wore the dead `kol-heading-sm` t-shirt
  stop (no rule in kol-theme) — mapped to `kol-sans-heading-04`.

## 0.5.5 — 2026-08-14

- Doc-comment only: the example font path in `GlyphMetricsGrid` lowercased to
  `/fonts/right-grotesk-ttf/` (the component takes font URLs as props — no behavior
  change; rides the theme 0.41.0 font-folder wave).
