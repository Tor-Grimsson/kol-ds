/* Authored-case labels shared by the archive table + controls rail (casing
 * law: no charAt().toUpperCase(), no `capitalize` — unknown values render
 * verbatim as authored upstream). */
export const TIME_CLASS_LABELS = { bullet: 'Bullet', blitz: 'Blitz', rapid: 'Rapid', daily: 'Daily' }
export const RESULT_LABELS = { win: 'Win', draw: 'Draw', loss: 'Loss', stalemate: 'Stalemate', resigned: 'Resigned', timeout: 'Timeout', checkmated: 'Checkmated', abandoned: 'Abandoned' }
