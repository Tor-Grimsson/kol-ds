---
title: Shipped packages
type: reference
status: active
updated: 2026-08-01
description: THE package list — every @kolkrabbi package this repo ships, with current version and one-line job. The single canonical table; update versions with every publish.
aliases:
  - shipped-packages
  - packages
  - package-list
tags:
  - domain/workflow
  - domain/design-system
related:
  - "[[01-release/INDEX|release pipeline]]"
  - "[[../documentation/00-overview/01-package-topology|package topology]]"
  - "[[../INDEX|docs home]]"
---

# Shipped packages

Every package this repo maintains and publishes to npm, in one table. **Versions are updated with every publish** (part of the release ritual — the batch that ships bumps this file). Deps/ownership detail lives in [[../documentation/00-overview/01-package-topology|the package topology]].

> Versions below = **verified against the registry 2026-08-01** after the batch publish of theme 0.18.0 · component 0.19.0 · framework 0.11.1 · workshop 0.11.0. Push == publish.

## UI tier

| Package | Version | Job |
|---|---|---|
| `@kolkrabbi/kol-theme` | **0.18.0** | Foundation CSS — tokens, type classes, all component chrome; 0.11.9/0.11.10 the `kol-doc-*` (11) + `kol-card-*` (6) type-role sets (footer mono 10/14; 0.11.11 radius law 4px; 0.11.14-19 CodeBlock chrome verbatim from the live reference + doc-body tracking 0.04em + table scroll edge = surface fade only (no shadow); 0.11.20-22 `--kol-content-*` = ONE frame + two caps (shell 1800 · column 768 · measure 65ch; page/wide tiers killed same day — width is content, not page identity)) (`kol-type-roles.css`, prose-twinned furniture, opt-in); 0.11.7 nav opacity ladder; 0.11.25 kol-color.css dark-mode header cites the corrected law (explicit > system > light); **0.12.0 links ship colorless** — `--kol-link` defaults currentColor, per-repo hook (user law 2026-07-29), placeholder blues + dark overrides killed; **0.12.1 doc-table cell roles split** on the mono fault line (`-token` nowrap line-height-1 · `-copy` wrapping 12/18; `-value`/`--wrap` deprecated aliases), `th` gains vertical-align:top, elder `.kol-spectrum-grid` duplicate resolved in theme's favour; **0.12.2 shell grid gutter theme-owned** — `.shell-content-grid` gap 32px base (48px ≥1600 step comes alive; the JSX `gap-8` utility that outranked it is removed in workshop 0.3.1); **0.13.0 width family unified** (lobby/WidthSystemContradictions resolution) — `--kol-content-panel: 960px` fills the 768→1800 gap (tables/code/framed panels), law comment now names three inner caps + the no-improvised-caps rule; table td column divider fg-08→oq-08 (the fourth seam of chess-audit finding 4); **0.13.1 seam law extended** — `.kol-doc-figure` border fg-12→oq-08 (opaque, table-wrapper weight) + `.kol-codeblock--bare` (chrome-less variant for hosts that own the frame); **0.13.2** bare keeps the code SURFACE (only border/radius go) + styleguide figcaption italic-sans → mono upright (unsanctioned; RG italic is a placeholder cut); **0.13.3 tag color family authored** (wave-4 vault parity) — `.tag`/`.tag-naked`/`.tag--{8 colors}` over the palette tokens (light = `-light` fill + dark ink · dark = saturated fill + light ink, yellow keeps dark ink · naked = saturated text; system-follow mirror per the theme law) — the classes shipped in Tag.jsx since the port but the RULES never left the monorepo, same dangling trap as the palette tokens themselves ; **0.13.4 `.kol-icon-frame*`** — the IconFrame rung set (8 variants + pinned squares 28/32/36), mirroring the `kol-btn` colour declarations exactly and carrying NO state rules, so the atom's "no states" property lives in the CSS ; **0.14.0–0.17.0 the rail ladder + the chip ramp** — `.shell-nav-item` is THE rail-row look at every rung, and Chapter/Page are separated by a step on one colour ramp (500 vs 400) instead of `.text-body`, a colour utility that made them indistinguishable; `kol-tag--*` rebuilt on Pill's model (3 variants, one scheme, every variant hoverable — `color` had been a second axis that silently swapped the base class off the only class carrying a `:hover` rule); Tag + Pill adopt the `kol-helper-*` ramp (`line-height: 1`); overlay chrome drops its border AND its shadow (the scrim is the separation); `--kol-shell-toc-w` 14rem → **16rem**, equal to the left rail ; **0.18.0 the media + docs surface** — the media-library chrome (`kol-components-organisms.css`), and `.kol-table-pill` / `.kol-table-token` put to work by the cells they were written for (they had sat in the theme unused while chess.kolkrabbi.io used them properly) |
| `@kolkrabbi/kol-icons` | **0.8.11** | `<Icon>` + kol-icon-set-v1 (165 icons / 26 groups; 0.8.11 `library` re-authored — 3 bars → 3 books + one leaning (tmpl-proposal approved 2026-07-30); 0.8.10 `scribble-02` culled — dupe of `scribble`; 0.8.9 stroke-conformance five — rows (re-authored, mirror-exact, 1.5) + pause/more/frequency/foundation fill→stroke, user-approved via the tmpl-proposal review; diagonals in their own arrow-diagonal group; 0.8.4 v1 `copy` = the elder glyph, old one retired to the shelf) + `registerIcons` (BYO); 0.8.0 v1-ONLY — legacy stroke/solid/svg/svg-web trees removed (local shelf `_tmp/legacy-icons/`), `variant` prop dropped, 17 promotions in; 0.8.1 keyline conformance — chevrons+carets fit 18×18, expanded redrawn stroke-1.5, arrow family complete: 4 diagonals baked from the downright master; chevron-expanded killed (twin of chevron-up) |
| `@kolkrabbi/kol-component` | **0.19.0** | The components — atoms → molecules → organisms + `<Graphic>`; 0.12.5 CodeBlock REPLICATED from the elder reference (react-syntax-highlighter + oneDark, filename-or-language chip, 32×32 icon copy w/ `copy`→check); 0.12.6 Figure rides the doc-eyebrow/caption roles; **0.13.0 chip-law defaults** — Pill `subtle`/`sm` (was outline/md, against the never-outline + chip-sm laws), Tag `sm`; Pill/Tag/Badge taxonomy documented in both component headers; 0.13.1 CodeBlock taxonomy-ok exemption (nests Icon from a sibling package); **0.14.0 Pill joins Button's variant vocabulary** — `primary` (grey fill, default) / `secondary` (outline) / `inverse`, old `subtle`/`outline` kept as deprecated aliases; **0.14.1 CodeBlock `bare` prop** — highlight/chip/copy without the framed chrome (kills PreviewCard's frame-in-frame Code tab); 0.12.7 Dropdown auto-size = sm at every viewport unless explicit `size` (user law) + ContentFilters view-mode toggle → ViewToggle (active filled chip / inactive bare) ; **0.14.2 DocsToc on THE rail row** — `kol-mono-12`/`py-1`/zero-indent → `shell-nav-item kol-mono-14` + emphasis/body colors, so on-this-page rows share the size AND the 20px left edge of the Related rows beneath them (the one-rail law was prose only; now gated by `pnpm validate:rails`) ; **0.14.3 DocsToc keeps the rail row** (no API change; ships with the workshop 0.5.0 rail sweep) ; **0.15.0 new atom `IconFrame`** — a STATIC square frame holding one icon: the button rung's visual weight with none of its interactivity (heading ornament, label ornament, legend swatch). Promoted from kol-website's `SectionTitle`, where it was an anonymous `<span>` wearing `kol-btn` classes. It owns `.kol-icon-frame*` rather than borrowing `kol-btn-*`, and that IS the component: on a span the button's hover/active/focus rules are suppressed by ELEMENT, so "no states" was an accident of the tag rather than a guarantee of the API. 8 variants × sm/md/lg; `onClick`/`href`/`disabled` deliberately absent — wanting those means wanting `Button iconOnly`. Migration measured at zero differing computed properties ; **0.15.1** ExitPreview demo scoping (no package change) — ships with the framework/workshop Phase-0 rule set ; **0.15.2 IconFrame `radius`** — `sm` (the system's radius token, default) or `full` (a full round), nothing between: a round frame is its own chrome idiom (edge-straddling controls, avatars, status dots) rather than a radius tweak, and it is the only sanctioned exception to the hard radius invariant. Pairs with theme's `.kol-icon-frame-radius-full` ; **0.15.3 IconFrame `iconSize`** — the glyph unbinds from the size ladder, same prop name and same `iconSize ?? LADDER[size]` shape `Button` and `Input` already carry (call-site precedent: ShapeDropdown, AlignmentGrid). The pinned square never moves — only the centred glyph does, which is what the 2026-07-28 "box never moves with glyph size" law always implied. Motive: a `radius="full"` frame reads heavier than the square its ladder was tuned against (a circle inscribes ~78.5% of its bounding box), so an edge-straddling round control wants a smaller glyph in the same box. Default `null` keeps the ladder, so every existing call site renders byte-identical ; **0.16.0–0.18.0 the rail arc** — `Tag` rebuilt on Pill's model (3 variants, one `kol-tag--*` scheme; `color` dropped as a second axis); `Table` gains **`sortable`** columns and a **`width` contract** (`panel` default / `column`) — the table caps ITSELF, and the gate asserts *a Table may not be hand-wrapped in a cap div* rather than grepping for a token name (the `width-ok:` comment exemption was deleted — an exemption that survives becomes precedent; the new rule caught three pages on its first run) ; **0.19.0 the media library** — **`MediaLibrary`** (`variant="page"|"modal"`) plus `MediaLibraryProvider` / `useMediaLibrary` / `MediaPicker` / `MediaBrowser` / `MediaViewer`: ONE component replacing **four consumer forks (~1542 lines across 9 files)**, with the media client **injected via `client`, never imported** (ARCHITECTURE §3). Folders disclose **in place** (Finder's list model) — click-to-enter and the breadcrumb-above-a-divider both disappeared. Fixed on promotion: video tiles paint (`#t=0.1`; 222 of 433 objects had rendered as empty boxes), the page view drills into folders, Escape steps back one level. New atom **`InteractiveImage`** (built as an ATOM on the taxonomy gate's ruling; gsap dropped for the canon `useTilt`, `useId()` for the module-global SVG ids that clobbered each other), and `useCoarsePointer` promoted out of TiltCard on gaining its second consumer |
| `@kolkrabbi/kol-framework` | **0.11.1** | App shell — `AppShell`, `SideNav`, `ShellHeader`, `ThemeToggle` + `useTheme`, heroes, footer; 0.5.5 icon names → v1 (`hamburger`/`x`); 0.5.4 theme state = explicit > system > light; 0.5.8 codeblock chrome moved OUT (home = kol-theme); **0.6.0 tri-state theme** — ThemeToggle cycles light → dark → system (`system` clears stamp + storage, follows the OS live; `desktop` glyph), `useTheme` adds `mode`/`cycle`, `applyTheme('system')`; **0.6.3 shell tab type stated in the rule** (14/18, no utility class racing it, 1600px type bump dropped); **0.6.4 duplicate class homes deleted** — prose + spectrum-grid chrome now lives only in kol-theme; **0.7.0 width-family unification** — `--kol-container-max` = the shell's responsive ladder (100% → 1400 → 1600 → `var(--kol-content-shell)` ≥1920, killing the 1800-vs-1600 contradiction) + `.kol-full-bleed` promoted from kol-website's local utility (negative-margin, container-relative; 50vw trap documented at the rule); **0.8.0/0.8.1 published by the kol-website agent (2026-07-30, see `lobby/NOTE-2026-07-30-framework-0.8.x…`)** — `.kol-full-bleed` inset parameterised (`--kol-full-bleed-inset`, DS-ladder fallback, non-breaking) so consumers off `.kol-page` declare their own gutters; **rode along: the ThemeToggle roll rework** (user-approved verbally, visual review still pending via the toggle proposal — the `desktop` system glyph it ships is already rejected, fix lands with the proposal verdict); **⚠ 0.8.0 is BROKEN** (npm-publish leak: raw `workspace:*` deps, the 0.5.11/0.5.12 trap again) — **deprecated on the registry 2026-07-30**, use 0.8.1; **0.9.0 ThemeToggle on the APPROVED 2-variant spec** (2026-07-30) — variants are container GEOMETRY only (`button` = padded rung + radius · `flush` = no pad/radius/fill ever), everything else props (`fill` subtle|none — the one bg element · `label` — off pins the square per rung · `iconRight` · `fullWidth` — kills hop as a variant · `size`); **one glyph, ever** — all three roll-strip positions are `mode-toggle-01`, system is told by label/tooltip, the `desktop` glyph is gone; deprecated aliases `icon`/`hop`/`hop-bare` render their 0.6.x chrome verbatim; ShellHeader migrated to the new API (fill none + label off) ; **0.9.1 PageSection heads read the width scale** — `max-w-[960px]`/`[720px]` were hand-typed transcriptions; now `--kol-content-panel`/`--kol-content-column`. NB the non-fullbleed head widens 720→768px ; **0.10.0 the `system`-is-not-a-state ruling** (lobby/ThemeToggleSystemState) — ThemeToggle cycles **light ↔ dark only**; `system` was a labelled rung a click walked into, which made the control claim a mode the user had actually *unset* and left it unable to say what the next click would do. Label + slot now follow the RESOLVED theme; the reset is `useTheme().clear()` fired by **alt/shift-click**. ⚠ **`fill` default flipped `subtle` → `none`** — a bare `<ThemeToggle />` renders `kol-btn-nav` (quiet container), not `kol-btn-primary` (grey filled rung); pass `fill="subtle"` to keep the fill. SideNav's collapse chevron, `data-sidenav` stamp and `is-collapsed` class REMOVED — dead since the CSS was deleted 2026-07-29, so the package shipped a control that did nothing; `collapsed`/`onToggle` remain accepted-but-inert ; **0.10.1 `.kol-exit-preview` drops `text-transform: uppercase`** — casing is a content-layer concern and components never auto-case (standing law). The label is authored as `Exit`; a consumer wanting EXIT types EXIT ; **0.11.0/0.11.1 the rail arc** — `--kol-shell-toc-w` 14rem → **16rem** so the right rail equals the left, and `HEADER_ICON` is exported from `ShellHeader` because the header row had been mixing two glyph scales |
| `@kolkrabbi/kol-workshop` | **0.11.0** | Docs/workshop system — markdown engine, search, tag graph, docs shell; 0.1.8/0.1.9 the `Doc*` kit over the kol-doc-* roles — DocTable = PRESET over the kol-component Table, unframed flush minimal (0.1.10); **0.2.0 convergence with the kol-website fork** (tooltips on every icon control, aria-labelled nav/toc landmarks, `id="main"`, brand link split) **+ 5 defect fixes**: ShellDrawer `open` (mobile nav was dead), ShellSearchOverlay current API + engine matching (⌘K was dead), hamburger rail desync, `groupDocsByMajor` now groups plain `NN-slug` ids (vaults listed only INDEX files), `DocumentationReader` usable outside a shell; **0.3.0 consumer seams** — `brand` (any node, not just an img src), `actions` (row-1 slot beside the search trigger), `isActive` (override the prefix-only tab matching) so a non-workshop app can wear the shell; **0.3.1 grid `gap-8` utility dropped** — the gutter is stated in `.shell-content-grid` (theme 0.12.2), the utility had made the 48px wide step dead code; **0.3.2 vault-grade inventory** — `buildInventory` ids collision-safe (duplicate basenames gain the parent folder; uppercase INDEX treated as index), `DocumentationReader` resolves modules by the doc's own file path (the endsWith heuristic grabbed the first basename match), shell rails `overflow-x-hidden` (long tree rows panned the grid sideways); **0.3.3 rail labels = kol-doc-eyebrow** (one label voice across shell sidebar, frontmatter, reader sidebar, quick actions); **0.3.4 vault-reader retype to the doc voice** (wave 4 spine) — DocsArticle drops `.kol-prose` (prose = blog/CMS only, never docs surfaces) for the MdxDoc flex-gap rhythm, every rendered element typed through the mdx-components dialect (kol-doc-heading h1 · DocSection sections · kol-doc-body copy/lists · kol-doc-code-inline + colorless emphasis links in render-tokens · panel-capped CodeBlock), markdown tables through THE kol-component Table as a dynamic-column DocTable sibling, DocsHeader → kol-doc-heading/-lede, the duplicated intro/section block switches folded into ONE renderBlock; **0.3.5 vault parity** — buildInventory extracts section `headings` (fence-aware) so search matches doc CONTENT, reader grows a Related rail (frontmatter `related:` wikilinks resolved path-first against the inventory, one rail voice), hashtag pills open TAG MODE via new `onTagClick` seam in render-tokens (the `?tag=` Link was dead in any consumer without a tag index); **0.3.6 hub-page rescue** — table cells split on UNESCAPED pipes only + `\|` unescaped (Obsidian wikilinks in tables exploded across cells, which is why INDEX hubs rendered as junk), `resolveDocLink` falls back to `${parentDir}-${basename}` for index/collision-prefixed inventory ids (INDEX wikilinks rendered as dead plain text) ; **0.4.0 THE FRAME + one rail, enforced** — ShellLayout's content wrapper had NO cap (`w-full px-4 md:px-5 lg:px-6`), so every consumer page inherited the raw viewport (2152px of frame at a 2200px window vs an 1800px law); now `mx-auto max-w-[var(--kol-content-shell)]` on the `--kol-pad-section-x` ramp — the parent of every full-width complaint in the 2026-07-30 review. TocColumn `lg`→`xl` matches the grid's own third-column breakpoint (at `lg` three children entered a two-column grid between 1024–1279px, wrapping the rail to row 2 and halving main to ~373px of a 900px window — why `/icons` and `/components` read as empty while rendering 165 icons / 188 components). Childless sidebar groups are **Links, not toggles** (chevron + count render only when there IS something to expand). DocsFrontmatter's FIELD_ORDER screened for the workshop-SAMPLE dialect (`category`/`date`/`modified`) that no kol-docs doc carries, admitting title+tags and dropping seven parsed fields — now the real contract, with dates formatted and arrays joined. `getTagColor` resolves the tag's NAMESPACE against the closed taxonomy instead of hashing its spelling (hash kept as fallback); TagGraph reads `var(--kol-palette-*)` instead of eight hand-typed copies of them. Dead widths retired: reader not-found `max-w-[1400px]` (tier killed at theme 0.11.22) and TagModeOverlay `max-w-[864px]` → panel ; **0.5.0 ONE metadata contract + rail TLC** — DocsFrontmatter stopped FILTERING and started ORDERING: contract fields first, legacy sample-dialect keys next, any unknown key after, so a field can never be dropped for being unfamiliar (`related` is the one omission — the rail renders it as live links). Dates format, arrays join. The showcase's MdxDoc had a SECOND panel with the opposite strategy (a denylist) — deleted; both surfaces now render this component. Rail sections gain the LEFT tree's header anatomy: rotating chevron, label, count — they were bare text that collapsed on click with no indicator, discoverable only by clicking something that looked inert. ; **0.5.1 tag inventory spans content types** — the overlay/graph row prefers an entry's own `href` over `docHref(id)`, so ONE tag system can cover a vault doc at `/documentation/:id` AND a component page at `/components/:slug`. Without it the inventory could only hold one kind of thing, which is why the node graph drew the 46 markdown docs and nothing else no matter how the component pages were tagged ; **0.6.0 the Phase-0 rule set** — (R1) a rail is never a reserved empty gutter: the grid track is `auto` and the width moved onto the rail's content as `w-56 empty:hidden`, so a rail rendering nothing collapses and main reclaims 224px (measured on 4 routes). TocColumn `hidden xl:block` matches gridCols. (R2) every route is a ⌘K row, not just children — `/icons`, `/references`, `/documentation` were unreachable by name; rows may carry an `action` closure so tags live in THE palette instead of a second search box, and the row projection no longer drops it; `TagModeGate` moved to wrap every shell route and the graph toggle is no longer gated on `hasFilters`, so the node graph has a real entry point (verified: 39 nodes, 135 edges from /foundations); a `?` shortcut sheet renders FROM the array that binds the keys, and the undocumented Alt+B duplicate is dropped ; **0.7.0–0.10.0 THE RAIL LADDER** — new **`RailSection`** (L1 Category / L2 Chapter) and **`RailRow`** (L3 Page/Section) own the markup that nine hand-written row stacks across five files had each re-typed; the rail's vocabulary is now written down — **Category · Chapter · Page · Section** — and verified live as one `shell-nav-item kol-mono-14` string at one height in BOTH rails ; **0.11.0 the docs surface** — `DocKit` props chips + sortable columns, `DocsFrontmatter` icons/labels, and new **`TagPath`** (namespace dims, leaf carries) extracted so a tag can't be spelled two ways across the overlay and the frontmatter panel |
| `@kolkrabbi/kol-dashboards` | **0.2.2** | Analytics — hand-rolled SVG charts (no d3), card family, `MetricsDashboard`; kol deps = peerDeps + workspace:* devDeps (0.2.1); 0.2.2 icon names → v1 (`book-open`/`bookmark`/`roadmap`/`trending-up`) |
| `@kolkrabbi/kol-chess` | **0.5.3** | Chess apparatus — interactive board, 3 piece sets, playback/notation/sidelines, archive, rail blocks (0.5.2), `./data` adapter; 0.5.3 ChessHero emoji-as-icon-name → v1 names. **⚠ Ownership note (2026-07-28 user ruling, recorded 2026-07-30):** the kol-chess consumer repo OWNS the apparatus layout now (elements-not-groups — it composes ChessBoard/GameArchiveTable/rail directly); the packaged `ChessAnalysisLayout`/`ChessBoardWithControls` tier is a **stale snapshot** — a future import of the layout pulls from the **kol-chess repo**, not from here |
| `@kolkrabbi/kol-content` | **0.4.1** | CMS — `/stack` (blog) + `/work` (portfolio) streams; 0.4.1 icon names → v1 (`x`) |
| `@kolkrabbi/kol-foundry` | **0.5.4** | Type-specimen apparatus — typeface hero, variable-axis playground, glyph metrics; 0.5.1 live-truth restore: StyleCard corner-flip hover (name bottom→top / value top→bottom, 120px card), variable-axis card geometry INLINE (30vh/60vh + 80/144px specimen — survives consumers without `@source`), section-header icon foundation→`type`, type-conform sweep (dead t-shirt stops + freestyle chrome → numeric kol-helper-*/kol-mono-*) |
| `@kolkrabbi/kol-store` | **0.1.1** | Commerce — product-detail layout, price display, marquee river |
| `@kolkrabbi/kol-styleguide` | **0.1.1** | Brand-guide specimens — color anatomy, combo lab, logo construction, type blocks; 0.1.1 no-auto-casing conform (labels authored in caps, CSS transforms dropped) |

## Other tiers

| Package | Version | Job |
|---|---|---|
| `@kolkrabbi/kol-media-client` | **0.1.0** | Read-only client for the kol-media CDN |
| `@kolkrabbi/kol-brand-template` | **0.2.0** | Brand-manifest schema + house defaults + CSS generator |
| `@kolkrabbi/kol-brand` | **0.1.2** | Kolkrabbi's own brand manifest (ramps, type, logo SVGs) |
| `@kolkrabbi/kol-scrape` | **0.1.0** | Presence/press scraper CLI |

## Excluded

- `@kolkrabbi/kol-loader` **0.3.0** — **deprecated on the registry 2026-07-30** ("Superseded by @kolkrabbi/kol-icons"); the orphan is closed.
- `@kolkrabbi/kol-specimen` **0.1.0** — subset-twin of `kol-foundry` (the canonical package, ruled 2026-07-15); lives outside this repo, pending `npm deprecate`.
