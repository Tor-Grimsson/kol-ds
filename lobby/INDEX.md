# Component lobby

Staging bay for components flung in from consumer apps via the `/lobby-ds` skill.
Each entry is a spec the DS **recreates from** — not source. To promote: build the
component under `packages/component/src/{atoms,molecules,organisms}/` (or a new
`packages/*` for non-component work) to spec, then move its entry to `done/`
(recreated) or `archive/` (rejected, with a reason) and log the job below.

This is a work queue, not published docs — it intentionally sits outside `docs/`
and does not follow the `.kol/docs-framework` conventions.

**Live entries live in `inbox/`** (moved there 2026-07-31 with the estate-wide
lobby standard). Spec: `~/.dotfiles/docs/operations/systems/lobby/`.

> The 119 records processed before 2026-08-01 graduated to
> `.kol/llm-context/lobby-history/` (`done/` 108 · `archive/` 11);
> this ledger is the queue, not their filing cabinet.

## States

| | state | means | lives in |
|---|---|---|---|
| 🔵 | `filed` | captured, unread | `inbox/` |
| 🟡 | `read` | understood — the row restates it | `inbox/` |
| 🟠 | `addressed` | a change shipped that is *meant* to close it | `inbox/` |
| 🟢 | `closed` | shipped + cited; resolution appended | `done/` |
| ⚪ | `parked` | deliberately not-**now**, reason recorded — revisitable | `archive/` |
| ⚫ | `retired` | closed without a fix, not-**ever** — terminal, and never ages | `archive/` |
| 🔴 | `needs-ruling` | **flag, not a state** — blocked on the user's call | wherever it is |

**Bar for 🟢 closed in this repo:** a shipped version / changeset cited in the
resolution. `read` and `addressed` are never `closed`.

## Queue — 0 entries
The queue holds LIVE TASKS ONLY (user ruling 2026-07-30). Exception below:
`ListGridCards` sits here as an **open collection** by the user's call — it
accrues entries and never closes.

| | Entry | About | Staged | State |
|---|---|---|---|---|
| 🟢 | [CardSetInViewAttention](done/CardSetInViewAttention.md) | **The card set needs an in-view attention state on touch** — the ruling you deferred out of `CardFeatureZoomScale`, answered: build it. Every card says attention with `:hover`, a touch device cannot hold it, and the cards are anchors so a tap navigates. `TiltBentoCoarseRevealInView` solved it for ONE component with a centre-band IntersectionObserver; the second component needed the identical thing and could not have it, so kol-website is carrying the same observer in `hooks/useMobileActiveCard.js` to put `.is-viewing` on `.kol-card-feature`. Two solutions in one evening, one of them in a consumer — the variance is the cost, not the behaviour. Ask: promote it to the set, same mechanism, coarse only, hooked into the rules `:hover` already uses so the two cannot drift, with a `static` escape like TiltBento's | 2026-08-31 | `filed` |
| 🟢 | [SectionFamilyFullBleed](done/SectionFamilyFullBleed.md) | **`fullBleed` belongs to the section family, not two of its members** — the ruling you deferred, answered: build it. `SectionHero` had it; `SectionNewsletter` got it in 0.147.0 by copying SectionHero's breakout literal character for character, which is the point — the second organism to need it had to copy the first. Any filled section inside `.kol-page` has its colour clipped by the page gutter on mobile, so this recurs once per organism. `.kol-full-bleed` is not the answer for organisms: container-relative, and kol-website hit it over-bleeding on Instagram the same evening. Ask: hoist to the family, default false, nothing moves | 2026-08-31 | `filed` |
| 🟢 | [ContentGridMinColumnWidth](done/ContentGridMinColumnWidth.md) | **A content grid can demand a column wider than its container.** `repeat(auto-fill, minmax(<fixed>, 1fr))` guarantees the track, so a narrower container overflows and whatever scroll container sits above starts scrolling sideways. Measured on kol-website `/workshop`: `minmax(22rem,1fr)` = 352px track in a 302px column, `main` scrolled 342 → 372 while the PAGE never overflowed — which is why it presented as a broken gutter and took a container-level measurement to find. kol-chess reports the same class at 768. Ask: guarantee that a content grid never demands a column wider than its container (`minmax(min(<fixed>,100%),1fr)` is the idiom); pairs with `ContentCollectionColsResponsive`, which owns how MANY columns — this owns how narrow one may get | 2026-08-31 | `filed` |
| 🟢 | [ContentCollectionMinColumnWidth](done/ContentCollectionMinColumnWidth.md) | **A wider screen clips MORE text than a phone.** Measured on kol-chess /play with `grid-cols-1 md:2 xl:3`: at 768 two columns are **324** wide against one column at **350** at 390, so the 768 case clips 7 rows of 10 while 1280 clips 3. Nothing overflows and no row moves — the column COUNT is wrong for the width. Split out of `ContentRowRosterVariant` at kol-ds-ui's request, because a minimum column width binds the whole collection and every kind rather than one variant; it is the width half of the question `ContentCollectionColsResponsive` answered for the count. **kol-ds-ui offered to pick a number and ship it — kol-chess says please do**: one page, no view on what the other kinds need, and the only datum we hold is the 324/373 clip counts above | 2026-08-31 | `filed` |
| 🟢 | [ButtonGroupResponsiveGap](done/ButtonGroupResponsiveGap.md) | **`gap-4` at every breakpoint (`ButtonGroup.jsx:35`), no variant and no prop.** The group responds in the axis it stacks on (`flex-col` → `sm:flex-row`) so the same 16px does two jobs: horizontal separation between side-by-side buttons on desktop, vertical separation between full-width stacked ones on a phone. User on kol-website `/`: *"16 is way too big, at least lets see 8 or 12"* — 8 is what reads right on device. Ask: a responsive default (tighter while stacked, 16 once it is a row) or a `gap` prop; note `className` lands on the OUTER container so there is no seam to the flex row that carries the gap | 2026-08-31 | `filed` |
| 🟢 | [CardFeatureZoomScale](done/CardFeatureZoomScale.md) | **The feature card's zoom is a hardcoded `scale(1.03)`** (`kol-animation.css:275`) with no prop or token. Correct on a dense photographic visual, invisible on a sparse one — kol-website's `/` has four line-art specimen cards on white where 3% cannot be seen, beside four dark-UI-screenshot cards in the same set where it reads fine. Ask: a seam for the scale (per-feature alongside `backgroundColor`/`imageAspectRatio`, or a custom property), 1.03 staying the default. **Also raises a set-level question**: the zoom is `:hover` only, and the card is an anchor so a tap navigates — the zoom never fires on touch at all. `TiltBento` just got an in-view attention state; two components in the set now need one, so it may belong to the set rather than per-component | 2026-08-31 | `filed` |
| 🟢 | [SectionNewsletterFullBleed](done/SectionNewsletterFullBleed.md) | **The newsletter card's FILL is clipped by the page gutter.** It is a filled surface inside `.kol-page`, so on mobile the colour stops 20px short of each screen edge and leaves white strips down both sides. A filled card at full width is the normal mobile treatment — fill breaks the gutter, content keeps it — but `className` reaches one box that carries both, so a consumer cannot bleed the fill without dragging the content out too. User: *"the background is not supposed to break with padding"*. Ask: a `fullBleed` prop, matching `SectionHero` which already has one — worth deciding whether the section family shares it rather than each organism inventing it | 2026-08-31 | `filed` |
| 🟢 | [ShellRailNoDrawerOnMobile](done/ShellRailNoDrawerOnMobile.md) | **No `touch` policy folds the rail into a drawer, so on a phone the 48px rail cannot be dismissed at all.** `shell` keeps it, `bare` throws navigation away entirely, `overlay` is a wrong-device notice, and the rail's own hide (`railToggleKey`) is a KEYBOARD key. User on a 390px iPhone: *"on mobile sidnav should fold into a hamburger menu, this takes too much real-estate"* — it is 12.3% of the viewport, and the board gained 302→350px the moment it folded. Two things stop a clean consumer build: `NavRail` writes `--kol-shell-rail-width` INLINE on `:root` (so a consumer needs `!important` twice), and `AppShell` force-SHOWS the rail on every `currentPath` change, which is backwards for a drawer and makes `navHidden` unusable as the seam (child effects run before parent effects). Folded consumer-side meanwhile — trigger + one media block, no DS source touched, the DS rail still the only nav. Ask: `touch="drawer"` with the breakpoint a prop, since coarse-pointer is the wrong test | 2026-08-31 | `filed` |
| 🟢 | [ShellRailNoDrawerOnMobile](done/ShellRailNoDrawerOnMobile.md) | AppShell had no touch policy that folds the rail into a drawer — at 390 the 48px rail is 12.3% of the viewport, and `railToggleKey` is a KEY, so on a phone it could not be dismissed at all. `bare` was the only other way to reclaim the width and it throws navigation away entirely | 2026-08-31 | `addressed` — **kol-shell 0.31.0 + kol-theme 0.112.0**: `touch="drawer"` + `drawerBelow` (768). Both blockers fixed upstream rather than worked around — `NavRail drawer` stops the `:root` token writes entirely (so the fold carries NO `!important`), and the route-change effect is mode-aware, which also unblocks `navHidden` as a consumer seam. ⚠️ **Held open: this repo has no AppShell surface, so the drawer has never been rendered in a browser here** — kol-chess confirms on device before deleting its own fold |
| 🟢 | [ChessBoardInputAndVariantSeam](done/ChessBoardInputAndVariantSeam.md) | `ChessBoard interactive` has one input — `onClick` on a bare div — so there is no drag (the primary gesture on both big sites, and near-only on mobile), no keyboard or screen-reader access at all, and no way to supply legality from outside, which makes Chess960 and every variant impossible in principle. Drag + a11y are **prototyped and working** against a live symlink from kol-chess; the diff rides in `_assets/`. The third part is a ruling request: a `dests` seam, and chess.js → chessops | 2026-08-31 | `addressed` — **kol-chess 0.9.0**: drag (pointer events, mouse+touch+pen) and the a11y half shipped; 29 browser checks green. The prototype carried a defect neither it nor I predicted — **the click after a selecting press never fires** (React replaces the mousedown target, the browser finds no common ancestor and drops it), so a press on your own piece is now authoritative and the skip flag resets on every press. 🔴 **§3 is the user's call and holds this row open**: take `dests` as an optional prop (chess.js untouched) so a consumer can supply legality and 960/variants become reachable |

## Closed

> ⚠ **29 entries sit in `done/` with no rows here** (counted 2026-08-15). This ledger has never
> had a `Closed` section, so every ticket that closed left the queue and left no record — the
> exact failure `05-lookup.md` calls *"No entry without a row"*, one level down. Reconstructing
> 29 rows from the files is a real job and is **not** guessed at here.

| | Entry | About | Staged | Closed | State |
|---|---|---|---|---|---|
| 🟢 | [ContentRowRosterVariant](done/ContentRowRosterVariant.md) | A `ContentRow` variant for a pickable roster row — filled tile, no border or divider, fixed row height, square thumb, two truncated lines that FILL the row rather than setting it. Built by six corrections on kol-chess `/play`, every one of them moving away from what `default` gives | 2026-08-31 | 2026-08-31 | `closed` — **kol-component 0.146.0 + kol-theme 0.113.0**: `variant="roster"`, every ruled number verified in a browser (56 at 390/768/1280 including the long-meta row, 40×40 thumb, 8 pad/gap, both lines ellipsised, hover washing). The fixed height needed a NEW mechanism — the family publishes `--kol-row-min-h`, a floor, which is what let the height drift 34→40→50→58; `.kol-row--fixed` sets height AND min-height, because height alone loses to the min-height already on `.kol-row`. Written as literals in all five name-keyed maps rather than derived from `showcase` — a spread reaches none of them, the trap `showcaseCanvas` fell into twice. Row only, no card. 🔴 **The 768 breakpoint finding is NOT ruled here** — a minimum column width binds the COLLECTION and every kind, so it wants its own ticket and the user's call |
| 🟢 | [InputTypeScaleZoomsIOS](done/InputTypeScaleZoomsIOS.md) | Every DS input auto-zoomed iOS Safari and never zoomed back out — `sm` (12px) and `md` (14px) both trip it and md is the DEFAULT, so every consumer with an input had it on every iPhone. Found because a mobile audit produced 27 screenshots that all looked like a catastrophic gutter failure and none of it was layout | 2026-08-31 | 2026-08-31 | `closed` — **kol-theme 0.112.0**: a 16px floor under `(pointer: coarse)`, keyed on the FIELDS (`.kol-control input` / `textarea`, `.kol-expand input`) rather than the control sizes — the type class sits on the SHELL and is inherited, so a `.kol-control-md` rule would have tied with `kol-mono-14` and been decided by sheet order (§5). The height pin lifts with it. Measured: fine pointer 12px unchanged, coarse 0 of 5 fields under 16 |
| 🟢 | [CardFeatureVisualCollapses](done/CardFeatureVisualCollapses.md) | The feature card's media box was `flex: 1 1 0%` — basis ZERO — so with no ratio its height was donated entirely by the parent and could resolve to 0, dropping the card to title + subtitle with no broken image and no failed request to show for it | 2026-08-31 | 2026-08-31 | `closed` — **kol-component 0.145.0**: `flex-auto` AND a `3/2` default ratio — the basis alone was half of it, a box still needs something of its own to be sized from. Measured at 390×700: 136 · 136 · 136, no collapse; geometry unchanged where it already worked |
| 🟢 | [SectionSplitVisualWidth](done/SectionSplitVisualWidth.md) | `SectionSplit`'s media was `w-auto`, so its width derived from the image aspect against whatever height it was given — on a SHORT viewport it resolved narrower than the column and centred, media visibly inset while the copy stayed at the gutter | 2026-08-31 | 2026-08-31 | `closed` — **kol-component 0.145.0**: `w-full`. Measured at the filer's own three viewports — 390×844, 390×700, 375×667 — inset 0 at every one (390×700 was left 56 · width 278). The filer's "it is viewport HEIGHT, not width" is now a comment on the line |
| 🟢 | [SectionNewsletterControlSize](done/SectionNewsletterControlSize.md) | `SectionNewsletter` hardcoded `size="md"` on its Input and let the Button take its own md default, with no prop and no className on the controls — so a page setting `size="lg"` on every other button could not match the newsletter pair | 2026-08-31 | 2026-08-31 | `closed` — **kol-component 0.145.0**: `controlSize`, forwarded to both, default md. Named `controlSize` not `size` — the organism already has `height` and `headlineSize`, so a bare `size` would not say which of the three it moved. ⚠️ lg is source-verified only; no showcase surface passes it |
| 🟢 | [SectionNewsletterMobileMeasure](done/SectionNewsletterMobileMeasure.md) | The newsletter band's form had ZERO inset at 390 — desktop's 80px is the leftover of the inner measure, so it scales to zero rather than down — and the height rung reserved ~114px of empty grey around the content on a phone | 2026-08-31 | 2026-08-31 | `closed` — **kol-component 0.145.0**: `px-5 sm:px-8` floor (desktop does not move — the measure caps below the padded width), and the organism's `height` default drops 60 → 40. The family LADDER is untouched; a law from one organism's ticket is not a law. Measured: inset 0 → 20, min-height 422 → 295. Two ticket claims corrected — the organism does take `className`, and the rung was 50svh not 60dvh |
| 🟢 | [TiltBentoCoarseRevealInView](done/TiltBentoCoarseRevealInView.md) | On a coarse pointer every TiltBento card rendered fully open — title, subtitle, description and CTA stacked over the artwork at once — on the device where the artwork is largest relative to the text | 2026-08-31 | 2026-08-31 | `closed` — **kol-component 0.145.0**: an IntersectionObserver on the viewport's middle band (`-45%` top and bottom) reveals only the card crossing the centre line — one open at a time, no cross-card coordination, no shared store. `coarseReveal="static"` keeps the old behaviour for a wall of small tiles. Measured: cards 0/1/2 each open alone when centred; fine-pointer rest and hover unchanged |
| 🟢 | [TagModeOverlayIgnoresQuery](done/TagModeOverlayIgnoresQuery.md) | Committing a workshop search showed the COMPLETE unfiltered tag census and zero document rows — `TagModeOverlay` consulted `activeTags` and never read `tagMode.text`, so a query with no chip yielded no rows by construction | 2026-08-31 | 2026-08-31 | `closed` — **kol-workshop 0.25.0**: all four asks. The overlay reads `text`; rows render on query OR chip through the engine's own `matchSearchItems` (no second matcher); the cloud narrows to the result set with counts recomputed; a no-match query says so. Measured on `/workshop-docs`: `a` → a document row plus three narrowed tags, `zzzzqqq` → "No documents match". The filer's diagnosis was right line-for-line |
| 🟢 | [DashCardBadgePropIsDead](done/DashCardBadgePropIsDead.md) | `DashTableCard` destructured a `badge` prop and called `CardHeader` with three arguments, so `<DashTableCard badge={…}/>` rendered nothing and warned about nothing — found building kol-chess `/insights`, where the only way to learn it was to open two components | 2026-08-30 | 2026-08-31 | `closed` — **kol-dashboards 0.3.0**: the card wears `DashListCard`'s shape (header + badge in a `justify-between` row). ONE card was dead, not four — list, chart and featured already render it, and `DashAlertCard` never took it (its chip is `trendValue`). The ticket's `CardHeader` route was rejected: three cards place the badge OUTSIDE the header on purpose, and a slot there would have been a second way to do one thing. ⚠️ `badge` is CONTENT — the card owns the `<Badge>`; a node nests. ⚠️ No card exposes `Badge`'s tones, so a severity chip is still grey — a `badgeVariant` seam, not this ticket |
| 🟢 | [TypefaceCardRestSkinFix](done/TypefaceCardRestSkinFix.md) | 0.140.0 matched the typeface card to the wrong reference — it copied the CARD map's `showcase` (a drawer: outlined, unfilled) when the ask named the ROW (filled, no outline), shipping every /foundry grid card as an outlined empty box | 2026-08-30 | 2026-08-30 | `closed` — **kol-component 0.143.0**: `bg` → surface-secondary, `border` → transparent, verified against ContentRow's box rather than by eye. The two maps share variant names and not treatments — now the comment on the line |
| 🟢 | [OneGrabGestureBothRails](done/OneGrabGestureBothRails.md) | Two rails on one screen, two grab implementations — the shell rail woke on proximity and the params rail did not, and neither was consumer code | 2026-08-30 | 2026-08-30 | `closed` — **component 0.142.0 · shell 0.30.0 · framework 0.36.0**: `useGrabEdge` extracted to kol-component (the only package both reach — shell dropped its framework peer in 0.16.0). NavRail deleted its copy; `useDragResize` returns `ref` + `.kol-rail-grab` in `grabProps`, so consumers get it on a bump. Width ladders deliberately not converged — that is policy, not gesture |
| 🟢 | [TypefaceRowHoverBg](done/TypefaceRowHoverBg.md) | The typeface row washed its background on hover where `/work` steps only its border — third and final pass on one "make it look like /work" ask | 2026-08-30 | 2026-08-30 | `closed` — **kol-component 0.141.0**: `hover` key deleted, `frameHover` kept, rest state verified unchanged. The lesson — diff EVERY state, not the one in the screenshot — is now a comment on the box. I had flagged this key in the previous ticket and shipped without it |
| 🟢 | [TypefaceCardRestSkin](done/TypefaceCardRestSkin.md) | The typeface CARD takes showcase's rest border and bg — two values, explicitly not a derive | 2026-08-30 | 2026-08-30 | `closed` — **kol-component 0.140.0**: `border` fg-08→fg-04, `bg` surface-primary→null. Layout, pad, height, RATIO, HOVER and MEDIA byte-identical. No spread — those are three name-keyed maps a derive could not reach, the same trap the row version fell into |
| 🟢 | [TypefaceRowSkinCorrection](done/TypefaceRowSkinCorrection.md) | 0.138.0 derived `showcaseCanvas` from `showcase` when the ask was two values — it carried `pad` 24→16 and `minH` 160→168 unasked, and could not reach `FILL`, which is keyed by variant name | 2026-08-30 | 2026-08-30 | `closed` — **kol-component 0.139.0**: own box restored, only `bg`/`frame`/`frameHover` taken from showcase. `BOX_SHOWCASE` removed, nothing derives. Took the recommendation in a ticket over the requirement in it |
| 🟢 | [RailSectionPressOpensRail](done/RailSectionPressOpensRail.md) | On the closed rail a section row was a guaranteed dead press — the caret sits outside the 48px clip, and the icon called `onNavigate` with a path no consumer dispatches | 2026-08-30 | 2026-08-30 | `closed` — **kol-shell 0.29.0**: pressing a section on the closed rail snaps it open and expands that section. `useRailDrag` now hands `snapTo` out through a ref, so the press reuses the real snap and the width ladder instead of reimplementing them. In-component, no new prop |
| 🟢 | [TypefaceRowSkin](done/TypefaceRowSkin.md) | `/foundry`'s rows wore the opposite skin to `/work` and `/prints` — transparent fill with an outline vs filled surface with none | 2026-08-30 | 2026-08-30 | `closed` — **kol-component 0.138.0**: `showcaseCanvas` now DERIVES from `showcase` (`{...BOX_SHOWCASE, thumb: 0, column: true}`), so fill/frame/hover/pad/rung are one object and cannot drift. `thumb={0}` alone could not do it — the band is a `column` flag, not the absence of a thumb. kol-foundry untouched; the alias still resolves |
| 🟢 | [SunkenWellEatenByPageWash](done/SunkenWellEatenByPageWash.md) | A washed page and the sunken well landed on the same colour — a five-level well closed by a five-level wash | 2026-08-30 | 2026-08-30 | `closed` — **kol-theme 0.110.0**: the `ab` ladder gained its `100` stop (the ground pole, no mix) and the well points at it. Pure #fff light / #000 dark, gap 10 and 23, unclosable by any wash rung. The 0.108.0 attempt (every component painting the wash) was the wrong layer and was reverted in 0.109.0 |
| 🟢 | [PageGutterOwnership](done/PageGutterOwnership.md) | The page gutter comes from three different layers and no two routes share a left edge; 1752 vs 1800 content at 1920, two foundry pages 48px apart on identical classes | 2026-08-30 | 2026-08-30 | `closed` — **nothing built: `.kol-page` has been the owner since 2026-07-30.** One rule carries cap + gutter + top rung, gutter INSIDE the cap. Asks 1/2/5 already answered by it; ask 4 is two of the user's own rulings, not drift. Every measurement in the audit is a page not using the class — adoption, not design |
| 🟢 | [SettingsToggleGestureConsumerSeam](done/SettingsToggleGestureConsumerSeam.md) | 0.25.0's `settingsKey` navigates unconditionally, but fxr's `,` is drawer-first on three of six routes — and `e.key` silently drops the ⌥ chord on macOS | 2026-08-30 | 2026-08-30 | `closed` — **kol-shell 0.26.0**: `useSettingsToggle()` off a `SettingsToggleContext` (no-op without `settingsPath`, safe outside an AppShell), and `e.code` matching via a `CODE_FOR_KEY` table. I built 0.25.0 assuming settings is always a page; it is a drawer on half of fxr's routes, and only the app knows which surface answers the key |
| 🟢 | [RowRungAndFillThumb](done/RowRungAndFillThumb.md) | The row rung was the variant's literal published inline (unreachable from a stylesheet), and the `fill` thumb derived from `box.minH` rather than the effective rung — kol-website was carrying two `!important`s on one component | 2026-08-30 | 2026-08-30 | `closed` — **kol-theme 0.100.0 + kol-component 0.136.0**. `minHeight` prop; the fill thumb resolves `calc(var(--kol-row-min-h) - 2 * var(--kol-row-pad-y))` in CSS; `ratioAxis="width"\|"height"` picks which axis pays for a non-square ratio. **Corrects my own `RowThumbRatioDead` ruling** — I made the rung the height unconditionally, which narrowed /prints' thumb to 96×136; a choice belonged there. Browser-measured: 136×136 · 96×136 · 136×192 · 192×272 |
| 🟢 | [RowThumbRatioDead](done/RowThumbRatioDead.md) | `ContentRow`'s documented `ratio` prop reached nothing — destructured then dropped, AND kol-theme hard-coded `aspect-ratio: 1 / 1` on the thumb. /prints set A-series and the render did not move | 2026-08-30 | 2026-08-30 | `closed` — **kol-theme 0.99.0 + kol-component 0.135.0**. Honoured, not deleted: the row publishes `--kol-row-thumb-ratio`, the CSS reads it with `1 / 1` as fallback. Ruled the `fill` case — the rung is the thumb's HEIGHT, so a non-square ratio widens it rather than growing the row past its rung. Browser-measured: existing rows pixel-identical (48×48 · 120×120 · fill 136×136), override gives 48×68 |
| 🟢 | [SliderTrackVariableNotForwarded](done/SliderTrackVariableNotForwarded.md) | `Slider`'s docstring promised a `--kol-slider-track` override by two routes and neither was wired — no `style` prop (21 named props, no rest spread) AND a self-declaration on `.slider-black` that beats inheritance | 2026-08-30 | 2026-08-30 | `closed` — **kol-theme 0.98.0 + kol-component 0.134.0**. Both halves: the track pseudo-elements read `var(--kol-slider-track, var(--kol-fg-64))` so the variable inherits (matching `.kol-slider-dual-playhead`, which had it right), and `style` is forwarded to the wrapper. Either fix alone would have left the other route dead. Consumers can drop their cascade rules reaching into `.slider-black` |
| 🟢 | [SettingsScaffoldTabFromTrailing](done/SettingsScaffoldTabFromTrailing.md) | 0.23.0 gave the scaffold ownership of `tab` but left `trailingActions` a NODE — so fxr's icon pair could no longer jump back to Settings, a behaviour the user ruled on 2026-08-28 | 2026-08-30 | 2026-08-30 | `closed` — **kol-shell 0.24.0**: `trailingActions` also takes a function, called with `(tab, setTab)`; a node still works. Chose the render prop over a controlled `tab`/`onTabChange` pair — the controlled shape hands page state back to the consumer, which is what 0.23.0 removed. My regression: a component that takes ownership of a value must hand out a way to set it |
| 🟢 | [SettingsScaffoldTabRows](done/SettingsScaffoldTabRows.md) | fxr's answer to the question `SettingsScaffoldFromFxrPage`'s receipt asked: SETTINGS and ABOUT / REPO are ONE page selector drawn across two rows, so a tab should carry its own row | 2026-08-30 | 2026-08-30 | `closed` — **kol-shell 0.23.0**: `tabs` entries take `row: 'layout'` for the second strip; the scaffold splits them and drives BOTH from one `tab`. Kills the three consequences of the `filtersProps` workaround — dead internal state, a lying `renderContent` argument, and a masthead that only got the right title by prop-spread ordering. ⚠️ Not screen-verified; fxr is on 0.22.0 |
| 🟢 | [ListGridCards](done/ListGridCards.md) | OPEN COLLECTION — every card rendering under a content-filter setup across the estate, gathered as evidence for an eventual card-family consolidation | 2026-08-15 | 2026-08-30 | `closed` — **purpose served.** The consolidation it was collecting for shipped: six of its nine entries folded into four content kinds (`file` · `catalog` · `article` · `showcase`) and their exports dropped. All four of its constraints were met — one component in card+row form, slots not fields, geometry as a prop, domain behaviour on a slot. `PrintGridCardGsap` still ships, the client forks are consumer-local, `Table` was never a card |
| 🟢 | [SettingsScaffoldFromFxrPage](done/SettingsScaffoldFromFxrPage.md) | Re-aim `SettingsScaffold` at fxr's approved `/settings`: its header row should BE `ContentFilters`, not a third header shape — plus the page+drawer pair, plus the duplicate settings kit | 2026-08-30 | 2026-08-30 | `closed` — **kol-shell 0.21.0 + 0.22.0**. The hold was for a conversation; it happened in session and settled all three. Scaffold is now `PageShell → PageHeader → ContentFilters → body`, tabs became the view strip, `searchKeys`/`filterGroups`/`trailingActions`/`tone` pass through. kol-component owns the settings kit (user ruling); shell's `SettingsSection`/`LabelRow` quarantined, `SettingsShortcuts` repointed at `SettingsRow`. ⚠️ The "71 call sites" on record was wrong — it counted uses, not files; the real number was TWO, both ticketed. ⚠️ No repo renders the scaffold yet: source-and-build verified only |
| 🟢 | [SliderDualThumbAndPlayhead](done/SliderDualThumbAndPlayhead.md) | Three seams on `Slider`/`RotaryDial` so kol-mirror can retire a verbatim local copy running at 10 call sites — dual thumbs + playhead, a non-input `readout`, and alt-click reset | 2026-08-30 | 2026-08-30 | `closed` — **kol-component 0.133.0 + kol-theme 0.97.0**. `variant="dual"` (clamp in the component, hollow-in/solid-out thumbs), `playhead`/`onPlayheadChange` on `--kol-slider-playhead` (the hardcoded `#2dd4bf` NOT carried — it is the defect the move ends), `readout` input/value/none, `defaultValue` + alt-click on BOTH controls. Mirror's useMemo-after-early-return hook-order bug not carried. Six specimens verified on /components/slider |
| 🟢 | [ContentSetRetirement](done/ContentSetRetirement.md) | Tracking ticket for the whole Content Set retirement wave — deprecate the eight absorbed cards, swap every consumer, then drop the exports | 2026-08-26 | 2026-08-30 | `closed` — **component 0.132.0 · shell 0.20.0 · store 0.3.0 · content 0.14.0 · foundry 0.9.0**. Step 3 landed: nine exports removed from five barrels, nine ledger rows deleted, sources quarantined to `_tmp/2026-08-30-content-set-exports/`. Dropped early on evidence rather than aged out — the estate sweep showed no consumer outside this repo. Six surfaces migrated in the same pass (kol-store PrintsGrid onto `catalog` + the new `flip`/`fade` props; article-grid · stack-blog · work-grid · work-portfolio · the typeface demo). kol-dashboards `GridCard` untouched (ruled keep 2026-08-27) |
| 🟢 | [RowVariantNamesAndSpecs](done/RowVariantNamesAndSpecs.md) | The content-card variants were named after the pages that first needed them, and the names stopped predicting anything — `/prints` renders `work`, not `print`, and `print`/`catalog` were byte-identical as rows. Plus a `specs` slot, plus an accurate overview page | 2026-08-29 | 2026-08-30 | `closed` — **kol-theme 0.96.0 + kol-component 0.131.0**. The ask ("name by SHAPE") could not work: one variant name drives BOTH card and row and their shapes diverge by design. User re-ruled it — name by what the content IS. Six page names → four kinds: `default`→`file`, `print`→`catalog` (folded; `flip`/`fade` are props now), `work`→`showcase`, `typeface`→`showcase layout="canvas"`. All four old names alias, verified rendering. `specs` shipped; overview shipped as `/sets/content-set-reference`, replacing the stale comparison set. ⚠️ The aliases are PROP VALUES — the retirements gate only watches barrel exports, so nothing chases them at 30 days |
| 🟢 | [QuadrantSyncShipAndChangelogRestore](done/QuadrantSyncShipAndChangelogRestore.md) | Ship `QuadrantSync`, restore the CHANGELOGs a regex destroyed, and rule on the changeset cascade that takes nine packages to 1.0.0 | 2026-08-29 | 2026-08-29 | `closed` — **kol-component 0.130.0** published by hand. The user ran the restore; it reverted to the last commit, so entries newer than 2026-08-26 went with the damage — **accepted as a gap, his call**. `onlyUpdatePeerDependentsWhenOutOfRange` was set and **did NOT stop the cascade** — the house process stays manual bumps |
| 🟢 | [PageHeaderActionsGrowsBlock](done/PageHeaderActionsGrowsBlock.md) | `actions` made the masthead 10px taller (a flex row takes its tallest child — 28px controls against an 18px lede), so no two pages of an app lined up | 2026-08-28 | 2026-08-28 | `closed` — **kol-shell 0.19.1**: the cluster is a zero-height box centred on the row, so the header's height is its text's at any control rung. Measured by the filer in two consumers |
| 🟢 | [DropdownHeightAndHover](done/DropdownHeightAndHover.md) | A `sm` trigger sat 4px short of the pinned icon controls on its own row, and the outline trigger had quietly grown a hover when the rest border it pinned back to moved | 2026-08-28 | 2026-08-28 | `closed` — **kol-theme 0.90.0**: the trigger takes the icon rungs 28/32/36; the outline pin-back re-reads `oq-08`. The filer's diagnosis of part 2 was the wrong mechanism — lines 45-51 are pin-backs, not hover states |
| 🟢 | [AppShellNavKeysHomeFirst](done/AppShellNavKeysHomeFirst.md) | ⌥1 skipped Home: `navKeys` counted `items`, but the rail's first row is the logomark — every key off by one and Home unreachable, against the ask it shipped against ("go from **home** 1 2 3 4") | 2026-08-28 | 2026-08-28 | `closed` — **kol-shell 0.19.0**: with a `logomark` the order is the order on screen, without one `items[n-1]` as before. Reported from the running app |
| 🟢 | [PageShellScrollbarGutter](done/PageShellScrollbarGutter.md) | A page's content width depended on whether it scrolled, and `.kol-filters-first` is measured in container units — so a scrollbar divided by six into the first filter group (user: "Create is the odd one out") | 2026-08-28 | 2026-08-28 | `closed` — **kol-shell 0.18.0**: `scrollbar-gutter: stable` on `PageShell`, which does the work in `fixed` mode and is inert in `scroll`. Diagnosed in source at both ends, **not measured** |
| 🟢 | [MediaClientBucketTable](done/MediaClientBucketTable.md) | The three-bucket table was the consumer's job and was copied byte-for-byte into two of them in a day — and the duplicated fact is which store sends CORS, so a stale `proxy: false` taints the canvas and `getImageData` throws | 2026-08-28 | 2026-08-28 | `closed` — **kol-media-client 0.3.0**: `KOL_BUCKETS` exported, `buckets` takes `null` \| `true` \| an object merged onto it, `proxied()` reads each bucket's `proxy`; `r2.proxy` deliberately left `true` (the cache trap). 8-check self-test in the file |
| 🟢 | [RailTwoLevelSections](done/RailTwoLevelSections.md) | The flat rail needs a second level and a section row to hold it — fxr's ~44 labs rows across four method sections flattened into an icon column built for five destinations | 2026-08-28 | 2026-08-28 | `closed` — **kol-shell 0.17.0**: L2 rows (12px glyph in a 20px box, glyph x 30, oq-64 → oq-96), sections carry the caret, nothing auto-expands, `AppShell railComponent`. The reported `GRAB.marks` TypeError is fxr's fork, not 0.16.1 — but **shell 0.16.0 is deprecated on npm**, its peer range allowed that broken pair |
| 🟢 | [RailFlatGrabOpen](done/RailFlatGrabOpen.md) | The app rail is the flat 48px column again — one div, every child a direct child — and the grab opens it without moving an icon. **Reverses `RailSideNavPixelParity`** (user ruling, built and verified in kol-mirror) | 2026-08-28 | 2026-08-28 | `closed` — **kol-shell 0.16.0** (`NavRail` flat + grab-drag, `AppShell` margin offset, framework peer dropped, `settings`/`themeToggle` gone) + **kol-theme 0.88.0** (the width token back to the rail, `.kol-shell-rail` restored); pill in `kol-animation.css`, numbers in `utilities/motion`; verified in source |
| 🟢 | [DropdownOptionHoverPreview](done/DropdownOptionHoverPreview.md) | `onOptionHover(value \| null)` — the live preview kol-mirror's blend-mode picker lost in the DS swap; a picker over a visual setting is a different control from one over a value | 2026-08-28 | 2026-08-28 | `closed` — **kol-component 0.125.0**: row enter/leave + `null` on close, silent while closed, no listeners bound when absent; verified in source |
| 🟢 | [DropdownGhostWidthAndListHeight](done/DropdownGhostWidthAndListHeight.md) | Four defects from kol-mirror's 29-call-site swap onto the DS `Dropdown`: the ghost stack grew the trigger past its container, no rows ceiling, no row-pitch seam, and the panel row's hover | 2026-08-28 | 2026-08-28 | `closed` — **kol-component 0.124.0** (`maxRows` · `rowHeight` · `.kol-dd-root`; hover shipped 0.123.0) + **kol-theme 0.87.0** (the width cap, the ellipsis, the list ceiling); verified in source. Not built: `onOptionHover` · `placeholder` · `defaultValue`, recorded in the entry as not-asks |
| 🟢 | [PageHeaderTrailingSlot](done/PageHeaderTrailingSlot.md) | A control beside `PageHeader` meant dismantling the component — no trailing slot, an inline bottom margin, no measure for the lede | 2026-08-28 | 2026-08-28 | `closed` — **kol-shell 0.15.0**: `actions` on the subtitle's baseline, `subtitleMaxWidth`, `--kol-page-header-mb`; verified in source |
| 🟢 | [ViewToggleWellGap](done/ViewToggleWellGap.md) | The icon toggle's `p-1` well broke an even row gap (20 one side, 16 the other) | 2026-08-28 | 2026-08-28 | `closed` — **kol-component 0.121.1**: the well is inset (`-mx-1`); verified in source |
| 🟢 | [CatalogCardFrameAndZoom](done/CatalogCardFrameAndZoom.md) | The catalog card's frame reads backwards (fg-04 rest on a 212-tile grid), the plate hairline is inline-only, and `.kol-media-zoom` only reaches `img`/`video` | 2026-08-28 | 2026-08-28 | `closed` — **kol-component 0.121.0** (rest transparent · hover fg-04 · `plateRule` · `.kol-media-ring`) + **kol-theme 0.83.0** (zoom `> :not(.kol-media-ring)`); verified in source |
| 🟢 | [ControlToneSunken](done/ControlToneSunken.md) | The control set gets ONE sunken tone and it is not called "inverse" — rename (`sunken`, `inverse` aliased) + coverage (`tone` on Button · IconFrame · ThemeToggle) + the active chip at fg-08 | 2026-08-28 | 2026-08-28 | `closed` — **kol-component 0.120.0** + **kol-theme 0.82.0** + **kol-framework 0.35.0**; the cluster in the showcase on an fg-02 wash; verified in source |
| 🟢 | [RailSettingsDisclosure](done/RailSettingsDisclosure.md) | Settings in the rail is a disclosure, not a route, with the theme toggle inside it (user: "open close on click and click again" · "theme toggle in the settings and out of the sidebar"); built and measured in fxr first | 2026-08-27 | 2026-08-28 | `closed` — **kol-framework 0.34.0** (`SideNav` panel leaf `{ id, label, icon, panel }`, portalled above its row) + **kol-shell 0.14.0** (`AppShell settings`); verified in source. 🔴 held: the active row's `--kol-accent-primary` icon ("what's going on with the yellow accent?") 🔴 **The shell half was withdrawn the same day**: `AppShell settings` shipped 0.14.0, deleted by 0.16.0's flat-rail reversal, never adopted anywhere. framework 0.34.0's `SideNav` panel leaf survives. Retire or port onto the flat rail — the user's call |
| 🟢 | [ColumnBrowserSeams](done/ColumnBrowserSeams.md) | The last three stopgaps between kol-r2b2 and an override-free stylesheet: `autoFocus`, `stats={false}`, and row state classes with the one-selected-state rule | 2026-08-27 | 2026-08-28 | `closed` — **kol-component 0.119.0** (`autoFocus` on `ColumnBrowser` + `MediaLibraryBrowse`, `stats` on `MediaLibraryLibrary`, `is-selected`/`is-cursor`) + **kol-theme 0.81.0** (the fill in the theme, the trail at fg-02, the deepest selection at fg-04 via `:has(~)`); verified in source |
| 🟢 | [RailLogomarkAtTop](done/RailLogomarkAtTop.md) | The logomark goes back to the TOP of the rail in both states — 0.13.0's footer placement was a move the parity ruling never made (user: "why did you move the logo from top to bottom?") | 2026-08-27 | 2026-08-28 | `closed` — **kol-framework 0.33.0** (`SideNav header` slot, `.kol-sidenav-header` in the hop's geometry) + **kol-shell 0.13.1** (`NavRail` passes the logomark as `header`, footer `false`); verified in source |
| 🟢 | [SideNavWidthLadder](done/SideNavWidthLadder.md) | `--kol-sidenav-w` becomes a ladder — 264px below 1536, 320px from it (user ruling 2026-08-27: 320 desktop · 264 laptop · collapsed tablet · hamburger mobile) | 2026-08-27 | 2026-08-28 | `closed` — **kol-framework 0.32.0**: the two declarations in `kol-framework.css`, `useDragResize` untouched; brand deletes its override on the bump. 🔴 held: `--kol-shell-toc-w` (16rem) vs the 2026-08-01 equal-rails ruling |
| 🟢 | [RailSideNavPixelParity](done/RailSideNavPixelParity.md) | The shell rail and the collapsed `SideNav` measured as two rails (48/56 wide, 20/16 glyphs, 40/38 pitch, y 76/26) on adjacent routes in fxr; user ruling: the rail IS a collapsed SideNav — one component, two states, the drag between | 2026-08-27 | 2026-08-28 | `closed` — **kol-framework 0.31.0** (`bottomItems` · `footer` · `themeToggle` · `iconComponent` · `expandOnSelect` · `defaultCollapsed`) + **kol-theme 0.80.0** (live `--kol-shell-rail-width`, `.kol-shell-rail` retired) + **kol-shell 0.13.0** (`AppShell` on `.kol-brand-layout`, `NavRail` = the SideNav adapter); verified in source |
| 🟢 | CardCornerLeak (by message, no entry) | `.kol-card`'s translucent fill painted under its translucent border and bled through as a hairline on the rounded corner (kol-monitor, verified on a pixel crop) | 2026-08-28 | 2026-08-28 | `closed` — **kol-theme 0.79.1**: `background-clip: padding-box` on the rest rule; verified in source |
| 🟢 | [ColumnBrowserChromeCorrections](done/ColumnBrowserChromeCorrections.md) | Four user rulings on `ColumnBrowser`'s own chrome, overridden in kol-r2b2 all day: every column keeps its right edge; a column's only row keeps its hairline; one ink for folders and files; the resize strip wears the SideNav's pill | 2026-08-27 | 2026-08-28 | `closed` — **kol-component 0.118.3** (edge · `only:` hairline · one ink) + **kol-theme 0.79.0** (the pill, 420ms, hidden at rest); verified in source |
| 🟢 | ContentFiltersFillChain (by message, no entry) | `ContentFilters`' items panel (`container-type` wrapper) sat `flex: 0 1 auto` in the flex body since 0.116.0, so a `PageShell mode="fixed"` fill died there — monitor's Create rack clipped its bottom row | 2026-08-28 | 2026-08-28 | `closed` — **kol-component 0.118.2**: the wrapper is `flex: 1; min-height: 0; display: flex; column` beside the container; verified in source |
| 🟢 | AppShellNavKeys (by message, no entry) | Option+1…9 → `items[n-1].path` via `onNavigate` on `AppShell` (user: "make command or alt 1234 go from home 1 2 3 4 in the sidenav without clicking … in all 3 repos") | 2026-08-28 | 2026-08-28 | `closed` — **kol-shell 0.12.0**: `navKeys` prop, `railToggleKey`'s guard, `e.code` digits, Option not Command; verified in source |
| 🟢 | [WorkspaceSidebarGeometry](done/WorkspaceSidebarGeometry.md) | **One sidebar geometry for the three workspace apps** (user: "I would rather all use the same geometry layout") — fxr's labs rail, monitor's rack sidebar, mirror's surface sidebar are three forks of one idea; `SideNav` takes only routes. Ask: action leaves `{ label, onSelect }`, and the collapse rule — a row that survives collapse has an icon, one without is a label | 2026-08-27 | 2026-08-27 | `closed` — **kol-framework 0.30.0**: action leaves, `currentPath` (router-agnostic), the collapse rule as `.kol-sidenav-section`; verified in source, showcase builds. The inbox file vanished mid-build (not the DS's hand) — `done/` carries the entry as read |
| 🟢 | [MediaKindsExportPath](done/MediaKindsExportPath.md) | `exports` maps `./utilities/*` → `*.jsx`, but `mediaKinds.js` and `ratios.js` (promoted from kol-r2b2 in 0.118.0) are `.js` with no explicit entry, so the subpath resolves to a file that is not there — the same fault fixed for `id3` / `frontmatter` in 0.114.0. The barrel exports both but drags `react-router-dom` | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.118.1**: explicit exports entries for every `.js` utility |
| 🟢 | [MediaLibraryPages](done/MediaLibraryPages.md) | **`MediaLibrary` becomes r2b2's page cut in two** — `variant="browse"` (bucket header + `ColumnBrowser` + count line) and `variant="library"` (r2b2's 868-line `FileList` wall promoted: kind filter · search · sort · SELECT/TREE · grid/list/off · download chip), read-only on a read client; `kol-media-client` lists a named bucket. Brand's `/library` splits into Overview · Browse · Local (user 2026-08-27: bucket is a control, not a page); r2b2 retires its fork on the same organism | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.118.0 · kol-media-client 0.2.0**: `browse` + `library` variants (r2b2's app verbatim, read-only without the write seams), `page` aliased, buckets on the client; showcase builds; verified in source |
| 🟢 | [IconSetCut](done/IconSetCut.md) | **kol-icons carries each glyph's cut (stroke \| solid)** — brand's `/icons` filters by TYPE and today globs the shipped SVGs to classify (`fill="currentColor"` → solid); the package knows this at build and should export it beside `KOL_ICON_SET_V1` | 2026-08-27 | 2026-08-27 | `closed` — **kol-icons 0.24.0**: `KOL_ICON_SET_V1_META` + `getCut`, cuts derived at build, the `icon-cuts` gate |
| 🟢 | [ControlToneInverse](done/ControlToneInverse.md) | **`tone="inverse"` on `ViewToggle` (icon) · `Dropdown` · `Input`** — on a `pageWash` plane the default grey well reads as a second plate; the flip is the two ViewToggle values swapped (well `fg-absolute-24`, active `surface-secondary`), the same dark chip on the Dropdown trigger + fused panel and on the Input field; `ContentFilters` forwards it to its search. Brand carries the ViewToggle half locally (two rules), the other two wait for the bump | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.117.0 · kol-theme 0.78.0**: `tone="inverse"` on the four controls + `ContentFilters`, the theme's `.kol-tone-inverse`; verified in source |
| 🟢 | [FullConsumptionContract](done/FullConsumptionContract.md) | **Two asks from kol-monitor.** (1) `kol-theme.css` imports every domain pack — chess · workshop · foundry · dashboards · styleguide = **81 KB / 23 %** of the theme, all shipping in a rack app's bundle (verified in monitor's `dist`); ask a supported `/core` entry, umbrella unchanged. (2) **Define "fully consuming the DS" and certify a repo against it** — classes over tokens in JSX, `:root` bindings exempt, no local duplicates, no hand-rolled chrome, type on the mono classes | 2026-08-27 | 2026-08-27 | `closed` — **kol-theme 0.77.0 `/core`** + `00-overview/04-full-consumption.md`, the six-grep checklist (a checklist, not a certificate — user) |
| 🟢 | [EditorOverlaysOnFullscreenOverlay](done/EditorOverlaysOnFullscreenOverlay.md) | **Rule the overlay tier.** kol-fxr hand-rolls six overlays — five scrims, `SCRIM = 'rgba(0,0,0,0.6)'` declared byte-for-byte in TWO files, hand-typed `z-[1000]`/`z-[1100]` above the DS's own z-100 layer, and **not one traps focus** (a Tab walks the page underneath — the bug `FullscreenOverlay` fixed for SettingsPanel). Three asks: is `FullscreenOverlay` the one component for this tier (or is the media lightbox a second archetype), should the scrim be a steppable token, and what is the z-contract | 2026-08-27 | 2026-08-27 | `closed` — **ruled + kol-theme 0.76.0 · kol-component 0.116.0**: one tier (`FullscreenOverlay` + `MediaViewer`), two fixed scrims, the `--kol-z-*` ladder as the contract with the DS strays moved; documented in `01-tokens.md` |
| 🟢 | [ColumnBrowserWidthsPersist](done/ColumnBrowserWidthsPersist.md) | `ColumnBrowser`'s width drags report through `onColumnResize` but the only way IN is `columnWidth` (one number) — a consumer cannot restore what it stored. Ask: `columnWidths`, the controlled map | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.115.0**: `columnWidths` map keyed by index + `'preview'`; verified in source |
| 🟢 | [DocPageAndKindShowcase](done/DocPageAndKindShowcase.md) | One plate for every document (markdown · text · code · JSON · YAML on the 1:√2 page in the overlay, zoomed in the column; code block transparent + full width); `DocFrontmatter` above markdown; lightbox arrows `fixed` at the viewport edges with `10rem` gutters; the `kol-framework.css` `.kol-overlay` clash; **a showcase page with all 14 kinds** | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.114.0 · kol-theme 0.75.0 · kol-framework 0.29.0**: `DocPage` + `DocFrontmatter` under `KindPreview`, the framework overlay rules retired, `MediaViewer` arrows fixed, `/sets/preview/kind-preview`; verified in source |
| 🟢 | [PlaybackBarAndAudioSheet](done/PlaybackBarAndAudioSheet.md) | The QuickTime bar as its own molecule, ruled against the reference (frosted `fg-absolute-48` + blur-xl, **radius 12**, white glyphs, `mm:ss` elapsed / **total**, native 2px track + 4×28 pill knob, speed `>>`); `VideoSheet` on it; `AudioSheet` new in two variants; `readCover` unreachable via the exports map; icons `speaker` · `chevrons-right` · `music-note` | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.114.0 · kol-theme 0.75.0 · kol-icons 0.23.0**: `PlaybackBar` + `usePlayback`, `VideoSheet` on it, `AudioSheet` cover | sheet, the three glyphs, `utilities/id3` exported; verified in source |
| 🟢 | [ShelfCardTiltWrapsCard](done/ShelfCardTiltWrapsCard.md) | The shelf's tilt (0.12.0) sits in the media slot under two `overflow-hidden` boxes — the frame stays still while the artwork leans inside it, and the work card's hover zoom died with the `<img>` two levels down. Ask: `tilt` wraps the card with the existing hook, a plain `<img>` back in the slot, no new component | 2026-08-27 | 2026-08-27 | `closed` — **kol-content 0.13.0 · kol-component 0.113.0**: `useTilt({ grounded })` wraps the whole card, `<img>` back in the slot; verified in source |
| 🟢 | [TiltBentoVoiceAndDefaults](done/TiltBentoVoiceAndDefaults.md) | `TiltBento`'s subtitle rides `kol-mono-text` (retired in the theme — zero rules emitted); `titleClassName` (heading-01) and `buttonLabel` (none) defaults silently change a fork's render on swap — pick the rung, confirm the defaults, say so in the docstring | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.113.0**: subtitle on `kol-mono-14`; defaults confirmed and documented; verified in source |
| 🟢 | [ColumnBrowserResize](done/ColumnBrowserResize.md) | `ColumnBrowser`: drag the browser's height (bottom-edge handle, `height` / `onHeightChange`) and each column's width individually (right-edge handles, `columnWidth` / `onColumnResize`), Finder-style — today `h-[528px]` and `w-[260px]` are fixed with no seam; native `resize:` rejected by the user | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.113.0 · kol-theme 0.74.0**: both drags on pointer capture, the handles in the theme; verified in source |
| 🟢 | [LabeledControlSection](done/LabeledControlSection.md) | **Rename `SettingsSection` → `LabeledControlSection`** (user ruling) — it names the place it was first used, not what it is; kol-fxr took it for its params rail. **No alias — the old name goes.** Name only | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.112.0** BREAKING rename, render untouched; verified in source |
| 🟢 | [ContentMediaFocusBinding](done/ContentMediaFocusBinding.md) | The image anchor (`transform-origin` — shared by the fit's `scale` and the hover zoom's `transform`) is hardcoded in `ContentMedia`'s `natural` / `compact` fits and `.kol-media-zoom`; a consumer cannot set it. Ask: one token, `--kol-media-focus`, read in both places, defaults = today's values | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.111.0 · kol-theme 0.73.0**: `--kol-media-focus` read on the fits (default `top left`) and the zoom (default `center`); documented in `01-tokens.md`; verified in source |
| 🟢 | [ContentFiltersFirstGroupFixedWidth](done/ContentFiltersFirstGroupFixedWidth.md) | **Reverses `ContentFiltersFirstGroupHugs`' width** — the first filter group is **ONE CATALOG COLUMN** wide (`1fr` of `repeat(6, 1fr)` gap 24 — `(width − 120px) / 6`, a fraction, never a px), not a hug; the flowing groups start over the second card | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.111.0 · kol-theme 0.73.0**: `.kol-filters-first` = `(100cqw − 120px) / 6` on the header-row container; doc + JSDoc re-ruled; verified in source |
| 🟢 | [ShellPageWash](done/ShellPageWash.md) | The page background steps up from a primary back, per app: `AppShell` paints `surface-primary` behind `children` and takes `pageWash` (→ `--kol-shell-page-wash`); `PageShell` reads it with a primary fallback. Unset = today. One prop for monitor · fxr · mirror | 2026-08-27 | 2026-08-27 | `closed` — **kol-shell 0.11.0**: the back painted always, `pageWash` on the wrapper, `PageShell` on the variable; verified in source |
| 🟢 | [ColumnBrowserMediaFacts](done/ColumnBrowserMediaFacts.md) | **ColumnBrowser preview facts: Dimensions for video, Length for audio + video** off `loadedmetadata` (captured on the frame like `<img>` load, so consumer `renderPreview` nodes count). Plus KindPreview: a real video branch and a play/pause-only audio tile in the column; timeline + volume in the overlay. Filed from kol-r2b2 | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.106.0**: the facts, `AudioPreview` · `AudioTile` · `VideoTile` promoted verbatim, KindPreview's video / audio branches; verified in source |
| 🟢 | [CatalogPageMonitorParity](done/CatalogPageMonitorParity.md) | `natural` was the wrong answer to CatalogPageCardFit — GridCard's fits were the image at 50 % / 30 % top-left, clipped, never contain; and a 2×2 expanded card must hide its three neighbours, which only `CatalogPage` can do (the consumer never sees the filtered order) | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.108.0 · kol-shell 0.10.0**: `fit natural / compact` are GridCard's rules verbatim; `CatalogPage` skips the neighbours (monitor's `computeHiddenSet`); verified in source |
| 🟢 | [CatalogPageCardFit](done/CatalogPageCardFit.md) | `CatalogPage` hardcodes `fit="cover"`; a rack preview wider than the card loses its left rail — monitor carries an `object-fit: contain` override meanwhile | 2026-08-27 | 2026-08-27 | `closed` — **kol-shell 0.9.2**: `toCard` may return `fit` (`cover` default · `natural` · `compact`), per card; verified in source |
| 🟢 | [PlayDiscAndVideoBar](done/PlayDiscAndVideoBar.md) | Three kol-r2b2 rulings compiled: the Finder play/pause disc on the column tiles (hidden at rest, over the ID3 cover / the poster), the QuickTime bar in the overlay (`VideoSheet`), and playback / tools glyphs at the filled-pause weight | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.107.0 · kol-theme 0.72.0 · kol-icons 0.22.0**: `.kol-play-disc` tiles + `readCover`, `VideoSheet` verbatim, `skip-*-15` / `skip-*-bold` / filled `slider-01`; verified in source |
| 🟢 | [ShelfCardMotion](done/ShelfCardMotion.md) | The retired WorkCard's motion came back on the shelf via a site `renderCard` — the staggered perspective entrance and the grounded `TiltCard`. Ask: `ParallaxShelf`'s default card carries both, the site passes items + `titleClass` | 2026-08-27 | 2026-08-27 | `closed` — **kol-content 0.12.0**: `enter` + `tilt` (default true), the entrance verbatim and settled under reduced motion; verified in source |
| 🟢 | [PauseIconFilled](done/PauseIconFilled.md) | `playback/pause` is two round-capped strokes beside a filled `play` — reads a weight lighter; the filled glyph ruled in kol-r2b2 ("send this icon to ds") | 2026-08-27 | 2026-08-27 | `closed` — **kol-icons 0.21.0**: `pause` replaced with the filled bars verbatim; the stroked one quarantined |
| 🟢 | [SettingsShortcutsComboWrap](done/SettingsShortcutsComboWrap.md) | A three-character combo wraps in `SettingsShortcuts`' six-column grid (`⌘ K` as `⌘` over `K`) — the value cell squeezed to min-content; monitor carries a nowrap `comboLabel` meanwhile | 2026-08-27 | 2026-08-27 | `closed` — **kol-shell 0.9.1**: combo cell `whitespace-nowrap`, `LabelRow`'s label yields and truncates; verified in source |
| 🟢 | [ShellHomeSystemMonitorGaps](done/ShellHomeSystemMonitorGaps.md) | Two seams kol-monitor hit adopting kol-shell 0.8.0: `SettingsShortcuts` reads `combo` where `ShortcutsOverlay` reads `keys`; `CatalogPage` passes no `mutuallyExclusiveFilters` and `toCard` cannot express `expanded` / `expandedContent` | 2026-08-27 | 2026-08-27 | `closed` — **kol-shell 0.9.0**: the overlay reads `combo` (`keys` tolerated), `CatalogPage filtersProps` + `expanded` / `expandedContent` through `toCard`; verified in source |
| 🟢 | [SectionCtaConnectVariant](done/SectionCtaConnectVariant.md) | The contact CTA on four pages is a 12-line site wrapper around `SectionCta` (user: "it's stupid to make a prefix family that we opt out of at the first opportunity"). Ask: `variant="connect"` with the copy as defaults | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.105.0**: `connect` = editorial + the contact defaults; verified in source |
| 🟢 | [ContentFiltersFirstGroupHugs](done/ContentFiltersFirstGroupHugs.md) | THE LAW (user, "for the 10th time"): the first filter group hugs its chips, every group after flows — by position, never by chip count; 0.104.1's equal columns reverted; the category label is the eyebrow | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.104.3**: `stack` defaults to `index === 0`; the law cemented in the docstring, the mdx and the doc; verified in source |
| 🟢 | [ContentFiltersEqualColumns](done/ContentFiltersEqualColumns.md) | `ContentFilters` sized a group by its chip count, so the first column came out narrow on every surface ("it's content filters everywhere"). Ruled: equal columns by default, `stack: true` opts in | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.104.1** equal columns → **WITHDRAWN same day** (a misread) → **0.104.2**: the ≤ 6 hug default restored (/prints' shape, `stack` pins), and the category label is the eyebrow role |
| 🟢 | [SettingsPanelApproved](done/SettingsPanelApproved.md) | The user built the drawer locally on the organism and locked it ("LOCK THIS") — the reference composition, superseding 0.102.0's reading — plus five defects: dropdowns behind the drawer, the scrim, unbounded markdown, the browser's file pick, the switch's title | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.104.0 · kol-theme 0.71.0**: the organism is the approved drawer; popover z 210, bounded markdown, file pick collapses the sibling folder, switch keeps title; the scrim was already flat (no DS rule to fix); verified in source, no server run |
| 🟢 | [SectionBackgroundProp](done/SectionBackgroundProp.md) | User: "ALL section family should pass bg as props" — surfaces were hardcoded or absent and pages reached them through `className` | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.103.0**: `background` on all six sections, one `SURFACES` map, defaults = what each painted; verified in source, no server run |
| 🟢 | [WorkCardAndShelf](done/WorkCardAndShelf.md) | Ruled on `/work` GRID + shelf: the work card's title is one line and its meta the uppercase helper at inverse 80; the shelf must render the same card on `WorkCard`'s three-height ladder (user: "the same card with 3 height sizes"); the shelf caption is the eyebrow | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.102.3 · kol-content 0.11.0**: ramp ruled, shelf on `ContentCard work` + the ladder, eyebrow caption; verified in source, no server run |
| 🟢 | [TagTertiary](done/TagTertiary.md) | A fourth `Tag` variant ruled on `/work`'s rows: secondary's fill, no outline, the mono voice at fg-80 (10 / 100% / 0.04em) — a variant, not a two-class override | 2026-08-27 | 2026-08-27 | `closed` — **kol-theme 0.70.0 · kol-component 0.102.2**: `variant="tertiary"`; verified in source, no server run |
| 🟢 | [CollectionItemMinWidth](done/CollectionItemMinWidth.md) | A collection item never sizes to its content: the list track was a bare `1fr` and the `<li>` `min-width: auto`, so a truncated row handed its min-content width to the track — a `/work` row measured 3586px in a 1232px wall | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.102.1**: `min-w-0` on every item, `minmax(0, 1fr)` list track; verified statically, no server run |
| 🟢 | [SettingsPanelEyebrowAndDropdowns](done/SettingsPanelEyebrowAndDropdowns.md) | `SettingsPanel`: section labels must be the eyebrow (the `SectionLabel` pattern — user: "it's literally SECTION LABEL"), one-of-N rows as `Dropdown`s ("super messy like it is"), "all" as a chip, and the segmented strip as rendered is wrong | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.102.0**: `SettingsSection` on `SectionLabel`, `SettingsChoice` = `Dropdown`, `allChip` + `onAll`; measured. The strip's default chrome left as the 08-15 law — his ruling if it changes |
| 🟢 | [KindPreviewMarkdown](done/KindPreviewMarkdown.md) | `KindPreview`: markdown showed a placeholder (user: "why is it not rendering in preview using kol-prose?"); markdown · json · yaml become their own kinds | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.101.0**: markdown → `.kol-prose` via `markdownToHtml`, json / yaml → CodeBlock, the kinds + labels; rendered |
| 🟢 | [SettingsPanelCompliance](done/SettingsPanelCompliance.md) | 0.98.0 turned `SettingsChipRow` into inverse uppercase `Tag` pills (user: "WROOOONG"); the ruled chip is the `.kol-control` control chip every strip wears | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.100.1**: the `.kol-control` chip string back, measured identical on / off; the rest of the panel was already on the register |
| 🟢 | [WorkListingRowsAndFilters](done/WorkListingRowsAndFilters.md) | Ruled on `/work` beside the local `WorkListItem`: the work row's values (mono-14 full ink, 168 min-height, 16 pad, a thumb that fills the content height, fg-08 hover), the dead `@container` step, the filters' fluid group running to the edge and the `stack` prop asked for every time, `Tag` pairing a helper class, a `thumb="fill"` seam, balanced headlines | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.100.0 · kol-theme 0.69.0**: all seven; work row 136×136 in 168 measured, containers declared, short groups stack by default, `Tag` sizes carry type, headlines balance |
| 🟢 | [ShellHomeSystem](done/ShellHomeSystem.md) | The app tier fxr · mirror · monitor write by hand, as one spec — thirteen items: `CatalogPage`, `SettingsShortcuts`, the title voice (done), the eyebrow's ink, catalog hover + truncation, the `nav-*` glyphs, `AppShell` key + touch policy, the theme boot snippet, `SettingsLinks`, neutral selection, the opentype peer, the raven from kol-brand | 2026-08-27 | 2026-08-27 | `closed` — **kol-shell 0.8.0 · kol-component 0.99.0 · kol-theme 0.68.0 · kol-icons 0.20.0 · kol-framework 0.28.0 · kol-brand 0.1.3 · kol-foundry 0.8.1**: all thirteen; catalog page, shortcuts and links rendered |
| 🟢 | [SettingsPanelChromeAndColumnPreview](done/SettingsPanelChromeAndColumnPreview.md) | (1) `SettingsPanel` drawer: wrong left border, wrong type — onto the app register beside it, chips as the DS `Tag`. (2) `ColumnBrowser` preview shows a grey box for anything but images — a `renderPreview` seam, and kol-r2b2's `KindPreview` promoted as the default | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.98.0**: register + `Tag` chips + `oq-08` edge (measured); `renderPreview`, `KindPreview` + the media kinds promoted; measured a CodeBlock in the column |
| 🟢 | [ColumnBrowserCursorSeed](done/ColumnBrowserCursorSeed.md) | `ColumnBrowser` cursor starts at column 0 whatever `prefix` is (the first ↓ acts in a one-row column), and a clamped ↑/↓ re-lands on the open folder, re-firing `onPrefix`. Ruling: seed on the deepest open folder; an arrow that doesn't move does nothing | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.97.3**: seeded on the deepest open folder (re-seeded on outside prefix change), no re-land; driven with a 2-deep prefix, 0 stray `onPrefix` calls |
| 🟢 | [ColumnBrowserOnPick](done/ColumnBrowserOnPick.md) | `onPick(file \| null)` seam on `ColumnBrowser` — the picked file for a Finder-style breadcrumb; the organism kept it internal and only surfaced it through space | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.97.2**: `onPick` on every pick change; driven file / arrow / folder |
| 🟢 | [ColumnBrowserCursorStart](done/ColumnBrowserCursorStart.md) | Shipped `ColumnBrowser` starts its keyboard cursor on row 0 with the selected fill — two lit folders in one column at rest, read as two selections. Ask: the cursor starts on the open folder, or is not drawn until a key is pressed | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.97.1**: not drawn until the keyboard is used (arrows arm, clicks seed); measured one lit row at rest |
| 🟢 | [PageHeaderMonoTitle](done/PageHeaderMonoTitle.md) | `PageHeader` has no mono title voice; kol-monitor's masthead (JetBrains Mono 32 / 500) is the ruled app-tier look and renders mono only by a legacy-sheet accident. Ask: `voice="mono"` (or a `titleClass` seam) on the mono heading rung the theme lacks | 2026-08-27 | 2026-08-27 | `closed` — **kol-shell 0.7.0 · kol-theme 0.67.0**: `voice="mono"` + `titleClass`, three mono heading rungs; measured 32 / 500 and 48 / 500. Addendum (same day) → **kol-shell 0.7.1**: `SettingsScaffold header` forwards it. Addendum 2 → **kol-shell 0.7.2**: `SettingsSection` titles `fg-96` / `helper-14` |
| 🟢 | [FoundrySpecimenSections](done/FoundrySpecimenSections.md) | Ruled on the Málrómur slug page, one pass, eight asks: the last three specimen sections into kol-foundry, their two cards onto the family (text-only `SectionCardItem`, `PairingCard`), one dropdown on a roman-only face, hairline glyph cells, missing glyphs dimmed, a borderless variable-font plate, the filters' title gap | 2026-08-27 | 2026-08-27 | `closed` — **kol-foundry 0.8.0 · kol-component 0.97.0 · kol-theme 0.66.0**: all eight, rendered |
| 🟢 | [FontPreviewClampAndBearings](done/FontPreviewClampAndBearings.md) | FontPreviewNoClamp was a misread: "the sample is cutting" meant the italic glyphs' side bearings sliced at the box edges; the per-rung clamp + ellipsis was correct. Ask: restore the ladder's `lines`, give the overhang room | 2026-08-27 | 2026-08-27 | `closed` — **kol-foundry 0.7.3**: clamp 1 / 3 / 4 / 5 back, `padding-inline: 0.15em; margin-inline: -0.15em` on the clamped element, wrapper unclipped; rendered |
| 🟢 | [ColumnBrowser](done/ColumnBrowser.md) | Built in kol-r2b2 first: Finder-style Miller columns as the listing surface's folder view (preview column, Table row metrics, hover fill = selected, 528 fixed, Finder keyboard, space = Quick Look) + a `columns` glyph, the default card's border off, a flat overlay scrim, the row-thumb ratio defect, wrapper fit | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.96.0 · kol-theme 0.65.0 · kol-icons 0.19.0**: `ColumnBrowser` verbatim with seams, `columns` glyph, default card no border, flat scrim, wrapper fit; rendered |
| 🟢 | [FontPreviewNoClamp](done/FontPreviewNoClamp.md) | `FontPreviewSection`'s size ladder clamps the specimen passage to N lines per rung with an ellipsis (user: "the sample is cutting in typefaces"). Ask: the passage renders whole at every rung, `lineClamp` stays an opt-in | 2026-08-27 | 2026-08-27 | `closed` — **kol-foundry 0.7.2**: no `lines` on the default ladder; rendered whole at 96 / 64 / 48 / 24 (30 / 20 / 14 / 7 lines) |
| 🟢 | [TypefaceCardRevealText](done/TypefaceCardRevealText.md) | The typeface card's hover reveal showed the English pangram; the foundry has its own Icelandic sample (user: "why does it use quick brown fox? we have custom text samples"). Ask: export the passage once, the reveal shows its first sentence in the face | 2026-08-27 | 2026-08-27 | `closed` — **kol-foundry 0.7.1**: `FOUNDRY_SAMPLE_TEXT` exported, the card reveal is its first sentence; rendered |
| 🟢 | [FoundryComponentsReconcile](done/FoundryComponentsReconcile.md) | kol-website's `components/sections/foundry/` forks nine kol-foundry files, all drifted forward (type-class + cap renames, the header pattern, a `GlyphMetricsGrid` rewrite, today's content-set library grid). Ask: the package adopts the local copies as its source so the site can import it and delete the fork | 2026-08-27 | 2026-08-27 | `closed` — **kol-foundry 0.7.0**: the nine synced whole (six diff 0 lines, the rest only the drops), `TypefaceAlphabet` added, `linkComponent` → `onNavigate`, peers component ≥0.95.0 / theme ≥0.64.0; rendered |
| 🟢 | [TypefaceCardAndRow](done/TypefaceCardAndRow.md) | Tuned on `/foundry` beside the local `TypefaceLibraryItem`: the row's name + classification are full ink and the year steps to 64; the card's title is the row's title voice; the card needs the shipped hover reveal (plate + glyph out, pangram in) | 2026-08-27 | 2026-08-27 | `closed` — **kol-theme 0.64.0 · kol-component 0.95.0**: row inks emphasis / 48 / emphasis / 64, card title `kol-mono-14 uppercase` full ink, `reveal` on the typeface card (`.kol-card.has-reveal`, 300ms); all measured |
| 🟢 | [ContentRowsAndPrintCard](done/ContentRowsAndPrintCard.md) | Tuned on `/stack` + `/prints`: the row thumb must be a fixed square the image fits into (not size the row), rows don't zoom, `ContentCard print` must carry `PrintGridCard`'s flip · rect seam · fade · keyboard, and the eyebrow has ONE name (user: "a class for eyebrow that's not called eyebrow but kicker?") | 2026-08-27 | 2026-08-27 | `closed` — **kol-theme 0.63.0 · kol-component 0.94.0**: square row thumbs by rule (120 / 64 measured), no row zoom, print flip + fade + rect + keyboard measured, `kol-eyebrow` + `eyebrow` slots with `kicker` / `label` / `kol-card-kicker` as aliases |
| 🟢 | [ContentFiltersTitleGap](done/ContentFiltersTitleGap.md) | `ContentFilters` header: the divider sits 24px from the title's text edge but 32 from the icon glyphs (IconFrame's 8px a side) and reads pushed toward the title. Tuned on `/stack`: `pr-4` on the title. Ask: that air by rule | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.93.1**: `pr-4` on the title by rule, `titleClassName` untouched; measured 40 / 32 |
| 🟢 | [ContentFiltersCollection](done/ContentFiltersCollection.md) | kol-r2b2 adopted `ContentFilters` + `ContentCard`/`ContentRow` default on a **linked** `packages/component`; the DS edits it needed (ContentFilters slots, `controlStart`) sat unpublished in the tree, plus three missing pieces: `SortHeader`/`SortControls`, `SizeOrDownload`, an on-media `ToggleCheckbox` | 2026-08-27 | 2026-08-27 | `closed` — **kol-theme 0.62.0 · kol-component 0.93.0**: the tree edits had shipped since 0.86.0; `SortHeader` + `SortControls`, `SizeOrDownload` (real `href`), `ToggleCheckbox variant="media"`, `controlStart` on `--top-left`; all four measured |
| 🟢 | [SectionHeroFootGap](done/SectionHeroFootGap.md) | 0.86.0's foot clearance (ramp + overlap) floats end-justified text ~474px above the foot; live sat the lede ~50px above the card. Tuned locally on `/stack`. Ask: inset = `overlap` + one small gap (~48 desktop, ~32 phones), the ramp only without a foot | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.92.0**: with a foot the inset is overlap + 32 / 48 from md; measured 48 / 48 / 32 at 1280 / 900 / 390 |
| 🟢 | [StackCardHover](done/StackCardHover.md) | Tuned on `/stack` first, then filed: the hero-size thumb zooms 1.02 (1.06 on a ~1500px thumb is 90px of travel — user: "that is TOO MUCH ZOOM"), and the article card title dims to 70% / 200ms on hover the way `ListingCard`'s does (user: "shouldn't it also do it to the cards in the content filter?") | 2026-08-27 | 2026-08-27 | `closed` — **kol-theme 0.61.0 · kol-component 0.90.2 · kol-content 0.10.2**: `.kol-media-zoom.is-hero` 1.02 (hero ListingCard, `zoom="hero"`), `kol-content-title-dim` on the article title; measured 0.7 / 0.2s and matrix(1.02) |
| 🟢 | [SectionTextBodyMono](done/SectionTextBodyMono.md) | `.kol-section-text-body` is sans 16; every section that carried its own mono lede lost it as the organism overrides came off. User: "I don't want the sans anywhere, I want the text on the cards to default mono, with props to change it." Ask: the body voice goes mono (14 or 16, the DS picks), sans via `bodyClass` | 2026-08-27 | 2026-08-27 | `closed` — **kol-theme 0.60.0**: body voice = mono 16 / 22 (the section scale), sans opt-in; measured split + newsletter at JetBrains Mono 16px / 22px |
| 🟢 | [SectionNewsletterForm](done/SectionNewsletterForm.md) | `SectionNewsletter` 0.90.0: the email `Input` renders ~190px (NewsletterBand gave ~460) — the form shrink-wraps under SectionText's centring; and a `bodyClass` override on the molecule, the thing SectionHeroNoOverrides removed. User: "the input is too short." | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.90.1**: form `w-full` (shell 520 at desktop, full width at 390), `bodyClass` gone, measure on a 64rem wrapper |
| 🟢 | [SectionNewsletter](done/SectionNewsletter.md) | `NewsletterBand` is the one section-shaped card outside the set — its own `max-w-[1400px]` / `py-24`, no rung, no `theme`, no y-centring. User: the newsletter as a card in the family, same ladder, same cap, content centred. Ask: `SectionNewsletter` = `SectionText` over the email form; `NewsletterBand` a deprecated alias (`title` → `headline`, `description` → `body`) | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.90.0**: on the ladder (default 60), the cap, `theme`, slot seams; alias on the ledger; measured 600 / 500 min-height, centred at 0px |
| 🟢 | [ListingCardHoverZoom](done/ListingCardHoverZoom.md) | `/stack`'s featured `ListingCard size="hero"` hovers on its own `group-hover:scale-105`; the article cards under it zoom on the family rule. User: "too much zoom on hover, it's not relative to the others." Ask: ListingCard on the family's zoom class, no seam of its own | 2026-08-27 | 2026-08-27 | `closed` — **kol-content 0.10.1**: hero + default thumbs on `.kol-media-zoom` (1.06 / 600ms, the article card's rule); measured both at matrix(1.06) over 0.6s |
| 🟢 | [ContentCollectionColsResponsive](done/ContentCollectionColsResponsive.md) | `cols={N}` is one number (one below md, N from md); on the 1600/1800 container three across grew. User: "can we make it 4 in the biggest breakpoint?" Ask: `cols` as a breakpoint map, `{ md: 3, xl: 4 }` | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.89.0**: `cols` map per rung (sm–2xl, 1–6, literal classes), number form unchanged; measured 4 / 3 / 3 / 1 at 1400 / 1279 / 900 / 390 |
| 🟢 | [ListingCardThumbBorder](done/ListingCardThumbBorder.md) | `ListingCard` (kol-content) hardcodes `border border-fg-08` around its thumb, no way off it; `/stack`'s featured hero card shows the line. User: "I hate border — remove border." Ask: `frame` on `ListingCard` (ContentCard's contract), and the article thumb frame defaults OFF on `ContentCard` too | 2026-08-27 | 2026-08-27 | `closed` — **kol-content 0.10.0 · kol-component 0.88.0**: `ListingCard frame` (default false, tint stays), article's `ContentCard` frame default off; measured border 0 on both |
| 🟢 | [ContentCollectionCols](done/ContentCollectionCols.md) | `ContentCollection` sizes by `min` (auto-fill track minimum); kol-website's filtered grids were ruled as a column count (`/stack`: `grid-cols-1 md:grid-cols-3`). Ask: `cols={N}` on the grid form — one below md, N from md, house gaps — `min` stays for the fluid case | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.87.0**: `cols={N}` (2–6, literal classes) wins over `min`, list ignores it; measured 3 tracks at 1280/900, 1 at 390 |
| 🟢 | [SectionHeroFootClearance](done/SectionHeroFootClearance.md) | Stack hero, `justify="end"` + `foot`: at the default `overlap` 250 the Featured card covers the lede — the content's bottom padding is a fixed ramp that ignores the foot rising into it. Ask: bottom inset = padding + `overlap` (`--kol-section-foot-overlap` on the section), default stays 250 | 2026-08-27 | 2026-08-27 | `closed` — **kol-component 0.86.0**: bottom inset = pb ramp + `overlap`, `--kol-section-foot-overlap` published on the section, padding no longer the `p-*` shorthand; measured 474 / 410 / 378 at 1280 / 900 / 390, text clears the foot by the ramp |
| 🟢 | [DisplayTightRamp](done/DisplayTightRamp.md) | kol-theme carries two display voices: `.kol-display-lg` (Right Grotesk **Tight** 500, uppercase baked, 48/64 — the last size-named class) and `.kol-sans-display-01/02` (Narrow 500). User: the Tight face is the correct one. Ask: `display-01` + `-02` → Tight 500 (case stays a role), `display-lg` aliases then deletes, no `-xs/-sm/-md/-lg` type class left — one type system. Retirements row + BREAKING entry | 2026-08-27 | 2026-08-27 | `closed` — **kol-theme 0.59.0**: display 01–04 Tight 500 no tracking, `display-04` shipped, the four elder classes aliased (uppercase kept) as the first CSS rows in the retirements ledger; measured at 1280/768/390 |
| 🟢 | [NotationPanelMoveDecorations](done/NotationPanelMoveDecorations.md) | **`NotationPanel` has no per-move decoration seam**, so kol-chess's review pane re-authors the move list to hang a `Badge` on classified moves. Ask: a `decorate(entry) => node` seam trailing each SAN | 2026-08-27 | 2026-08-27 | `closed` — **kol-chess 0.8.0**: `decorate(entry) => node` rendered trailing each SAN inside the move cell; measured 12 cells, 2 decorated, selection follows the click |
| 🟢 | [SectionSplitMediaBounded](done/SectionSplitMediaBounded.md) | Studio process split at `height="40"` renders like 60 and like nothing: the media item (a square as wide as its half, ~850px) is taller than any rung, so `min-height` never bites — the image sizes the card and the ladder is decorative on the one section that always carries media. Ask: the media half is BOUNDED by the section's rung (max-height = the rung, width follows the ratio inside it, text stays centred), so 40 · 60 · 80 · full give four heights | 2026-08-27 | 2026-08-27 | `closed` — **component 0.85.0**: the media frame is rung − padding tall, width follows the ratio; four rungs measured 1000 / 800 / 600 / 40 |
| 🟢 | [SectionHeightForty](done/SectionHeightForty.md) | Add `40` (35svh / 40vh) to the shared `sectionHeights` map, every section that has `height`, hero included. Default stays `60` everywhere; nothing else moves (user: one ladder, one default — no per-section defaults) | 2026-08-27 | 2026-08-27 | `closed` — **component 0.84.0**: `40` (35svh / 40vh) on every section's ladder, default still 60; measured 400 |
| 🟢 | [SectionHeightLadder](done/SectionHeightLadder.md) | `SectionSplitHeight` was widened same-day to the whole set but returned as the split only (0.82.0). The rest: `height` min-height ladder (`full` · `80` · `60` default, literal classes, content centred, padding inside) on `SectionCards` · `SectionFaq` · `SectionCta` — so hero, split, cards, faq, cta all size the same way | 2026-08-27 | 2026-08-27 | `closed` — **component 0.83.0**: cards · faq · cta on the shared min-height ladder (default 60); all four measured 600, centred |
| 🟢 | [SectionSplitHeight](done/SectionSplitHeight.md) | **WIDENED 2026-08-27 to the whole set** — `height` min-height ladder on `SectionSplit` · `SectionCards` · `SectionFaq` · `SectionCta` (hero already has it): `full` · `80` · `60` default, literal classes, columns/content centred, padding inside. Was: `SectionSplit` has no `height` — content + `py-16/24/32`, the media ratio decides the rest; beside a hero on a tier it reads arbitrary. User: set sizes. Ask: `height` as min-height with the family's three names (`full` 100dvh · `80` 70svh/80vh · `60` 50svh/60vh, default `60`), literal class strings, columns stay centred, padding inside | 2026-08-27 | 2026-08-27 | `closed` — **component 0.82.0**: `height` min-height ladder full/80/60 (default 60), columns centred; measured 600/800/1000 |
| 🟢 | [ThemeToggleLabelTarget](done/ThemeToggleLabelTarget.md) | Labelled `ThemeToggle` reads "Light mode" while light — the state, not the action — while its own `aria-label`/`title` say "Switch to dark mode" (`ThemeToggle.jsx:67` vs `:140`). User: a button names what it does. Ask: visible label = `MODE_LABEL[next]`, matching the a11y strings | 2026-08-27 | 2026-08-27 | `closed` — **framework 0.27.1**: the visible label names the target ("Dark mode" while light); measured both themes |
| 🟢 | [SectionThemeInverse](done/SectionThemeInverse.md) | User: invert a section as a PROP, not inverse classes — `theme="inverse"` = the paired theme of whatever is live (every token to its counterpart, flips with the toggle), on `SectionHero` · `SectionSplit` · `SectionCards`; pinned `light`/`dark` if the theme layer grows a light scope. One scope attribute on the section so children flip for free. Studio split hero is the first user | 2026-08-27 | 2026-08-27 | `closed` — **component 0.81.0**: `theme="inverse" | "light" | "dark"` on hero · split · cards via data-theme + the 0.52.0 subtree scopes; measured flipping with the toggle |
| 🟢 | [SectionHeroSplitHeight](done/SectionHeroSplitHeight.md) | `SectionHero variant="split" height="80"` renders ~175px on kol-website's Studio: the split rewrites the preset `h-…` → `min-h-…` at runtime (`SectionHero.jsx:154`), so Tailwind never generates `min-h-[70svh] md:min-h-[80vh]` — verified in the built CSS (media variant's `h-[70svh]`/`md:h-[80vh]` present ×1, the split's min-h tokens 0). Ask: literal `SPLIT_HEIGHTS` strings, no runtime rewrite; media half self-sizing for an absolute node | 2026-08-26 | 2026-08-26 | `closed` — **component 0.80.1**: literal SPLIT_HEIGHTS map, media half h-full; full/80/60 measured 1000/800/600 |
| 🟢 | [appshell-content-surface](done/appshell-content-surface.md) | `AppShell` paints `.kol-brand-layout` with `bg-surface-tertiary min-h-dvh` — the line brand's `BrandLayout.jsx` L72 has and the package fork dropped — so the page sits tertiary under a primary rail. User ruling on adoption: "sidenav primary, site tertiary, like brand.kolkrabbi.io". kol-studio carries an `html, body` rule until it ships | 2026-08-26 | 2026-08-26 | `closed` — **framework 0.27.0**: the page is surface-tertiary, the rail surface-primary; measured both themes on the workbench story |
| 🟢 | [sidenav-rail-hairline](done/sidenav-rail-hairline.md) | `SideNav`'s `background` prop paints the surface AND a `border-r border-fg-08` hairline under one name, default on — split the hairline into its own prop, default **off** (footer `border-t` stays); `AppShell` forwards SideNav props (today it forwards none, so even `background={false}` is unreachable). User ruling on adoption: "fine to have it as an option, as prop, but default should be without" | 2026-08-26 | 2026-08-26 | `closed` — **framework 0.26.0**: `hairline` prop (default off) split out of `background`; AppShell forwards `sideNav` props; measured 0 → 1px |
| 🟢 | [SectionHeroNoOverrides](done/SectionHeroNoOverrides.md) | Studio hero beside Studio process split, same `headlineSize`: the hero's body is smaller, headline capped, rhythm tighter — `SectionHero` forces `headlineClass` · `bodyClass` (`kol-sans-body-02`) · `gap-6` · `actionsClass` onto `SectionText` on both routes, the old glass-panel parity carried into the set (the label override's siblings). Ask: the hero renders `SectionText` bare like the split; caps/gap become molecule props if ruled | 2026-08-26 | 2026-08-26 | `closed` — **component 0.80.0**: the hero renders SectionText bare; caps headline = `headlineCase="upper"` role; measured hero = split |
| 🟢 | [ChessBoardFluidCoordinates](done/ChessBoardFluidCoordinates.md) | **`ChessBoard size="fluid"` keeps desktop coordinate chrome at every rendered size** — `p-2` + `kol-helper-12` over a 45px square at 390, the rank digit on the rook. Ask: size the labels from the square (`cqw`) when fluid | 2026-08-26 | 2026-08-26 | `closed` — **chess 0.7.1 · theme 0.58.1**: fluid board is a container, coordinates ride cqw; 390 → 7px / 2px measured |
| 🟢 | [SectionRevealSeams](done/SectionRevealSeams.md) | Every DS adoption since 08-12 cut kol-website's `.reveal` entrance (six surfaces + one degraded: Home/Studio/in-development cards, Stack article header, Studio process split, Stack hero, Home foundry) — the organisms expose no per-element class/style seam. Ask: seams, not motion — `itemClassName`/`itemStyle(index)` on `SectionCards`, `className`+`style` on `SectionCardItem`, `headlineClass` + per-slot style on `SectionText` (forwarded by the organisms), per-part seams on kol-content `ArticleHeader`. The reveal stays app-side (user ruling) | 2026-08-26 | 2026-08-26 | `closed` — **component 0.79.0 · content 0.9.3**: slotClass/slotStyle on SectionText + every organism, itemClassName/itemStyle on SectionCards, className/style on SectionCardItem, partClassName/partStyle on ArticleHeader — seams, no motion |
| 🟢 | [SectionCtaEditorial](done/SectionCtaEditorial.md) | User on the `/ CONNECT` band: full width with a hollow middle at 2000px, and the row labels (`kol-helper-16`) are not the set's eyebrow. Ask: inner cap 1600 centred (the centered variant already caps at 900); `promptLabel` / `contactLabel` on the `SectionText` eyebrow default. Consumer passes copy only | 2026-08-26 | 2026-08-26 | `closed` — **component 0.76.4**: editorial band capped at 1600 centred, row labels on the set eyebrow; measured at 2000 |
| 🟢 | [SectionSplitMediaClip](done/SectionSplitMediaClip.md) | The split's media frame (`SectionSplit.jsx:96`, `overflow-hidden`) clips a 3D-transforming media node — kol-website's `TiltCard` on the Home foundry split shows cut corners as it rotates ("jagged edges"). The slot promises an interactive node; the frame doesn't let one move. Ask: `mediaClip={false}` seam, default true | 2026-08-26 | 2026-08-26 | `closed` — **component 0.76.3**: `mediaClip={false}` drops the frame's overflow-hidden for a 3D-transforming node |
| 🟢 | [SectionTextLabelVoice](done/SectionTextLabelVoice.md) | User on the Studio process split + Home foundry split: the eyebrow is the WRONG voice, the hero's is right. `SectionText`'s ruled `kol-section-text-label` (18px · 600 · 0.14em · accent, renders "PROCESS") vs `SectionHero`'s own override `kol-helper-12 text-meta` ("Services"). Ask: the ruled label becomes the hero's voice, no case transform (casing law), the hero drops its override — one eyebrow across the set | 2026-08-26 | 2026-08-26 | `closed` — **component 0.76.2 · theme 0.57.1**: the label voice is the hero's helper-12 / meta across the set; uppercase by role stays (user ruling) |
| 🟢 | [SectionHeroCarouselSeams](done/SectionHeroCarouselSeams.md) | Adopting `SectionHero media=[…]` on kol-website's `/foundry` blocked: `SectionHero.jsx:196-213` forwards items / height / autoplay / navPosition only — `renderTitle` (typeface-font slide titles), `ctaLabel` ("Explore Typeface") and `onNavigate` (SPA) are dropped. Ask: forward the three (+ showTitle/showDescription/showCta if cheap) or spread rest props to the engine. `/foundry` stays on the deprecated `FeaturedCarousel` import meanwhile | 2026-08-26 | 2026-08-26 | `closed` — **component 0.76.1**: renderTitle · ctaLabel · onNavigate · show* · title/descriptionClassName · options forwarded to the carousel engine; measured on the demo |
| 🟢 | [SectionHeroRound2](done/SectionHeroRound2.md) | ONE hero for every page (user's rule). `SectionHero` still can't do what Stack / foundry index / typeface pages / licensing do. Asks: vh height presets `full` (100vh under the navbar) · `80` · `60`, each with a phone pair (user's ruling) · `justify: 'end'` (content at the foot) · gradient veil · an overlap slot for a card across the fold · `media=[…]` carousel mode absorbing `FeaturedCarousel` · text-only hero · `FoundryCTA` folded into the set. Consumer already on `screen` / `h-[60vh]` / `h-[80vh]` strings pending the presets | 2026-08-26 | 2026-08-26 | `closed` — **component 0.76.0 · theme 0.57.0**: height presets full/80/60, justify end, veil, foot+overlap, media array = carousel, text-only hero, FoundryCTA → SectionCta centered; all six measured |
| 🟢 | [SectionSet](done/SectionSet.md) | **SET.** The website sections — `FullBleedHero` · `FeatureSplit` · `FeaturesCardSection` · `CtaGlobal` · FAQ (Accordion only) — each type their own kicker/heading/body/media; none share a primitive. Ask: `SectionText` molecule (label · headline · body · actions, all opt-in, type by role) + `ContentMedia` reused as the slot, the five organisms rebuilt on them under one `Section*` prefix (`SectionHero` · `SectionSplit` + `align` left/right/center · `SectionCards` · `SectionCta` · new `SectionFaq`), old names aliased, inspector `Section` → `InspectorSection`. Ships as one wave. Second filing — `SectionSplit` 08-15 answered the anatomy, not the composition. Studio adopts first | 2026-08-26 | 2026-08-26 | `closed` — **component 0.71.0 · theme 0.54.0**: `SectionText` + SectionHero / SectionSplit (align) / SectionCards / SectionCta / SectionFaq; old names deprecated aliases; parity measured node by node |
| 🟢 | [FrameworkComponentPeer](done/FrameworkComponentPeer.md) | First bump after `NestedDsDependencies` (component 0.68.1 → 0.69.1) → `pnpm why` **Found 2 versions** in both kol-website apps: kol-framework 0.24.0 declares `kol-component ^0.68.1` (+ icons) as `dependencies`, nests a stale copy — the class the closing note left "if ruled". Ask: component + icons to `peerDependencies` with `>=` floors in kol-framework (kol-component → icons, kol-styleguide → its four if room), republish, BREAKING-flagged. Consumer stopgap: `pnpm-workspace.yaml` override | 2026-08-26 | 2026-08-26 | `closed` — **framework 0.25.0 · component 0.70.0 · styleguide 0.2.0**: the tier is a peer in every DS package that consumes it; registry-verified |
| 🟢 | [FeaturedCarouselFullWidth](done/FeaturedCarouselFullWidth.md) | kolkrabbi.io/studio hero shows two videos at once. `kol-framework.css:502` gives every `.is-slides` slide `clamp(320px, 72vw, 900px)`; `fullWidth` only drops the section padding, so a full-bleed carousel shows the neighbours peeking on both sides with their videos autoplaying. Ask: `fullWidth` → slide `width: 100%` (or a `slideWidth` prop, default peek), inactive video slides don't autoplay | 2026-08-26 | 2026-08-26 | `closed` — **framework 0.25.0 · component 0.70.0**: `.is-full` slide rule (100%), inactive video slides pause; measured 734 = container |
| 🟢 | [SettingsPanel](done/SettingsPanel.md) | kol-r2b2's slide-over display-settings drawer (title/subtitle header · intro · sectioned rows of label+hint with word Switch / segmented Choice / count chips · reset footer). Wanted as a DS component with **drawer AND overlay** presentations from one anatomy — variant prop or a related pair — for reuse across apps. Defects at the source the user will fix in the DS, not copy: inconsistent case, some alignment, the segmented-toggle variant | 2026-08-26 | 2026-08-26 | `closed` — **component 0.69.0 · theme 0.53.0**: `SettingsPanel` organism, drawer on ShellDrawer / overlay on FullscreenOverlay (focus trap added), DS ToggleSwitch + SegmentedToggle as the controls, grid rows; rendered headless both variants |
| 🟢 | [MediaLibraryReconcile](done/MediaLibraryReconcile.md) | The DS `MediaLibrary` page variant (brand /library) and kol-r2b2's `FileList` (media. / admin.) are two renders of one bucket on the same MediaCard/MediaRow — the DS one is the weaker (Finder disclose-in-place, foot path bar, ~155px tiles, no sort direction / Flat / paging / stats). Converge the page variant on FileList's read-only render: breadcrumb + stats on top, click-to-enter folder rows, `minmax(260px,1fr)` grid, sort with direction, Flat, `pageSize`. Long-term FileList = MediaLibrary + a writes slot. Modal variant untouched | 2026-08-26 | 2026-08-26 | `closed` — **component 0.69.0 · theme 0.53.0**: page variant = FileList's read-only render (breadcrumb · stats · click-to-enter folders · bare toolbar · 260 tiles · paging), prefix-scoped provider (BREAKING for `useMediaLibrary`), measured live over the bucket |
| 🟢 | [sidenav-nested-groups](done/sidenav-nested-groups.md) | `SideNav` renders a `{ label, children }` node inside a category as a non-routing group header with its leaves indented — the tree shape is the option, no prop; a tree without nested nodes (brand app) renders unchanged. Re-scopes the 2026-08-01 "two levels only" line: it was a ruling about the brand app's anchors, inscribed in the package as a law for every consumer — kol-studio lost two groups and six routes from its rail on adoption. Reference walker (the retired fork's `GroupNode`/`ChildNode`) + screenshot attached | 2026-08-26 | 2026-08-26 | `closed` — **framework 0.24.0 · theme 0.52.1**: `{ label, children }` rows render as a group header + indented rows, recursive, lit on an active leaf; flat trees byte-identical (asserted in the showcase); header re-scoped with the user's words; dead elder atoms rule retired |
| 🟢 | [nested-theme-scope](done/nested-theme-scope.md) | declare the light surface block and the `--kol-fg-*` ramp + roles on `:root, :is([data-theme="light"], .light), :is([data-theme="dark"], .dark)` — not `:root` alone — so `data-theme` on a subtree flips the ink, not just the surface. Three selector-list edits, no value changes; kol-studio carries a verbatim override (`kol-nested-theme.css`) until it ships | 2026-08-26 | 2026-08-26 | `closed` — **theme 0.52.0**: light surface block, fg ramp + roles, oq ramp and border/focus tokens declared on the theme selectors, not `:root` alone; accent stays `:root` (brand rebind). Measured both directions headless |
| 🟢 | [NestedDsDependencies](done/NestedDsDependencies.md) | five app packages (workshop · chess · content · foundry · store) declare component/framework/theme/icons as `dependencies` with 0.x carets, so pnpm nests a stale copy under each — kol-website measured ShellHeader from framework **0.20.1** and the docs CodeBlock from component **0.39.0** after bumping to 0.23.0 / 0.68.1; the store grid runs component **0.8.0**. Ask: `peerDependencies`, the kol-dashboards way, republish the five | 2026-08-26 | 2026-08-26 | `closed` — **workshop 0.23.0 · chess 0.7.0 · content 0.9.0 · foundry 0.6.0 · store 0.2.0**: DS tier as `peerDependencies` with `>=` floors, registry-verified; kol-website deletes its `pnpm.overrides` on bump |
| 🟢 | [MobileTouchFloor](done/MobileTouchFloor.md) | the ruling request: 10px chrome and 14–26px hit areas on phones. **Ruled 2026-08-26:** no type floor; a 24px hit floor (WCAG AA) that never moves the drawn size | 2026-08-25 | 2026-08-26 | `closed` — **theme 0.51.0**: bare ToggleSwitch `::before` extent, Slider input 24px with the 2px track centred; law in `05-control-chrome.md` § Touch floor. 📌 kol-website: bump; its own 16px table buttons and 12px links get the same treatment |
| 🟢 | [SideNavMobilePosition](done/SideNavMobilePosition.md) | the aside's box moved into `.kol-sidenav`; seven utilities off the className, so the 767px drawer rule finally wins — every consumer page had opened one viewport down on phones | 2026-08-25 | 2026-08-26 | `closed` — **framework 0.23.0**; replayed on brand production at 393 / 1280. 📌 kol-website: bump, delete the stopgap rule |
| 🟢 | [TableMobileScroll](done/TableMobileScroll.md) | the premise did not hold — `.kol-table-wrapper { overflow-x: auto }` has always shipped and scrolls (measured ×4 on brand production at 393); the hidden scrollbar and headless no-drag hid it | 2026-08-25 | 2026-08-26 | `closed` — **theme 0.50.2** cited as verified-against; nothing built. Fade declined (2026-07-28 ruling); touch affordance **ruled 2026-08-26: none** — the cut columns are the affordance |
| 🟢 | [CodeBlockMobileOverflow](done/CodeBlockMobileOverflow.md) | the `<code>` computed `pre` under three declarations of `pre-wrap` (oneDark's theme spread after `wrapLongLines`); chipless blocks ran line 1 under the copy control | 2026-08-25 | 2026-08-26 | `closed` — **component 0.68.1 + theme 0.50.2**: `pre-wrap` stated where it is decided; `.kol-codeblock-line:first-child` reserves the control's lane on chipless blocks. 📌 kol-website: bump, re-check `/stack/vcap` on a phone |
| 🟢 | [WorkshopShellMobile](done/WorkshopShellMobile.md) | the strip scrolled but the active tab sat off-screen; the two-up cards are kol-website's own flex rows, not DS chrome | 2026-08-25 | 2026-08-26 | `closed` — **framework 0.23.0**: active tab scrolled into view before paint, snap on flick, no fade. 📌 kol-website: bump framework; stack its `DashboardComponents.jsx` rows below `md` |
| 🟢 | [ContentFiltersForkRetirement](done/ContentFiltersForkRetirement.md) | kol-shell 0.1.0 recreated an organism that had shipped in kol-component since 2026-08-01; the organism is canon, the fork and its `TabStrip` chip retired to `_tmp/`, `iconComponent` crossed over | 2026-08-15 | 2026-08-15 | `closed` — **component 0.44.2 → 0.45.0 · kol-shell 0.3.0** (three regressions from monitor's live QA fixed the same day, by diffing monitor's original) |
| 🟢 | [ShellHeaderFilterRefinements](done/ShellHeaderFilterRefinements.md) | monitor's adoption QA of kol-shell 0.1.0 — the h1/subtitle gap, chip size, the LIST/GRID strip's position; three publishes re-decided a filter value's look before seams ended it | 2026-08-15 | 2026-08-15 | `closed` — **kol-shell 0.1.2 → 0.1.3 → 0.2.0**; superseded by ContentFiltersForkRetirement, which removed the seams with the fork |
| 🟢 | [ShortcutsOverlaySections](done/ShortcutsOverlaySections.md) | the overlay takes a sectioned array as well as the flat one, detected on shape — one grid, headings spanning both columns | 2026-08-15 | 2026-08-15 | `closed` — **kol-shell 0.4.0**, adopted in kol-fxr the same run (99-line local overlay → ~30-line adapter) |
| 🟢 | [ListingCardSpec](done/ListingCardSpec.md) | the ArticleCardSizeSpec answer, executed — wide scope, `ListingCard` rename with `ArticleCard` alias, `readmore` removed, geometry conformed | 2026-08-15 | 2026-08-15 | `closed` — **kol-content 0.7.0**; `WorkCard` deliberately not aliased (different anatomy, folds next major) |
| 🟢 | [KolSourcesPnpmResolution](done/KolSourcesPnpmResolution.md) | every `@source` path in `kol-sources.css` resolved to nothing under pnpm — all twelve, because Tailwind realpaths the manifest into the `.pnpm` store where only kol-theme's own deps are siblings. Silent by nature: a missing utility renders as slightly-wrong layout, never an error | 2026-08-15 | 2026-08-15 | `closed` — **theme 0.42.2**, twin `@source` lines per package (the flat path + a five-up store walk to the consumer's `node_modules/@kolkrabbi/*`); one of each pair is inert per layout. Docstring's flat-tree sibling claim corrected and cited to this ticket. **Verified by running it** in kol-mirror with the consumer's local override deleted: the shortcuts overlay renders its flat two-column grid, built CSS 266.65→275.53 kB — the +8.9 being the other eleven packages' utilities reaching a pnpm consumer for the first time. Remainder there: none |

## Archived

_(none yet — ⚪ parked and ⚫ retired entries land in `archive/`)_

## Filed elsewhere

Tickets this repo filed into another lobby land here as receipts while they are
outstanding. Returned receipts with `Remainder here: none` graduate to
`.kol/llm-context/lobby-history/archive/` (12 there as of 2026-08-14).

| | Receipt | Destination | Last known | Remainder here |
|---|---|---|---|---|
| 🔵 | [TiltFamilyForks](outbox/TiltFamilyForks.md) | **kol-website** — `~/dev/projects/kol-website/lobby/INDEX.md` | 🔵 filed 2026-08-27 — the site's three local tilt forks swap onto component 0.110.0's Tilt family | **none** — the DS side shipped |
| 🟢 | [ArticleCardSizeSpec](outbox/ArticleCardSizeSpec.md) | **kol-website** — `~/dev/projects/kol-website/lobby/INDEX.md` | 🟢 answered 2026-08-15 — ListingCardSpec returned, executed as kol-content 0.7.0 | **none** |

## History

| Date | Event |
|---|---|
| 2026-08-31 | **`ContentRowRosterVariant` filed from kol-chess** — a card shape that took six user corrections to settle, every one of them walking away from `ContentRow default`. The lesson worth keeping is the last correction: *"it has to fill height of fixed card not change to height of the card"*. A row whose height follows its content moved on every copy and padding change (34 → 40 → 50 → 58), and one long meta line pushed a tile out of line with its neighbours; fixing the row and letting the contents fill it is what made the grid hold. Filed with the measured spec, the breakpoint table, and the finding that 768 clips more text than 390 does |
| 2026-08-31 | **`ShellRailNoDrawerOnMobile` filed from kol-chess** — the rail has no way to get out of the way on a phone. The package has every part of a drawer except the two that matter: a visible trigger (its only hide is a keyboard key) and a policy that does not fight it (`AppShell` re-shows the rail on every navigation, which is the opposite of what a drawer does when you tap a destination). Folded consumer-side first, with no DS source touched per the 2026-08-31 linked-dev bulletin, so the ticket arrives with a working fold and measurements rather than a request |
| 2026-08-31 | **`ChessBoardInputAndVariantSeam` filed from kol-chess — with a working prototype rather than a request.** The user stopped an earlier version of this ticket that would have been written from a guess ("maybe lets solve things locally before making requests"), so the package was symlinked into kol-chess and the fix built against the live source first. Drag (pointer events, because HTML5 drag never fires on touch), square semantics (role/tabIndex/aria-label/aria-pressed/Enter) and `touch-action` are all verified moving pieces in a real game — the diff is attached. Only the part that is genuinely a DESIGN call, a `dests` seam plus chess.js → chessops for 960 and variants, is left open, because chess.js cannot express 960 castling and no consumer-side prop can reach a rules engine that lives inside the component |
| 2026-08-30 | **`DashCardBadgePropIsDead` filed from kol-chess** — a prop that accepts and discards. Four dash cards take `badge` and none forward it; React cannot warn, because destructuring it is legal and dropping it one line later is invisible. Surfaced while building an insights page where the severity chip was passed as `badge`, rendered as nothing, and looked like a styling bug for as long as it took to read `CardHeader`. The consumer keyed severity off `icon` instead — which works and spends the icon slot on something it is not for |
| 2026-08-30 | **`OneGrabGestureBothRails` filed from kol-fxr** — the left and right rails of one screen resize with two different gestures because they were born in two packages: `NavRail`'s private gsap grab vs `useDragResize`'s plain pill. Asked for the gesture to live in the hook with `NavRail` consuming it |
| 2026-08-30 | **`RailSectionPressOpensRail` filed from kol-fxr — the closed rail's section rows are dead presses.** Icon calls `onNavigate` on a non-destination; caret clipped out at 48px. Not fixable in a consumer: `railOpen` is internal to `NavRail` with no prop or callback, and driving the width variable from outside produces a wide empty rail because the L2 rows gate on `railOpen`. Asked for the 0.17.0 `SideNav` behaviour back, in-component |
| 2026-08-30 | **`SunkenWellEatenByPageWash` filed from kol-fxr — the sunken well and `pageWash` cancel exactly in light mode.** `surface-sunken` is derived from the page TOKEN (245 under 250) while `AppShell pageWash` repaints that page to 245.4; measured delta 0,0,0 on fxr's `/settings` after the 0.99→0.106 jump, four controls pill-less. Dark unaffected. Any consumer passing `pageWash` is in the same position — kol-monitor filed the prop originally |
| 2026-08-30 | **`SettingsToggleGestureConsumerSeam` filed from kol-fxr — an adoption blocker on 0.25.0, not a complaint about it.** The gesture and the return-path-in-a-ref are right; fxr just cannot hand its copy over. Its `,` means "open whatever settings is available" — the in-chrome drawer on /editor, /labs and /randomiser without navigating, the page only on a shell route — and `settingsKey` navigates unconditionally. Second, `e.key` drops ⌥, on macOS. Asked for the toggle to be exposed so the consumer keeps only which-surface-answers |
| 2026-08-30 | **`SettingsScaffoldTabFromTrailing` filed from kol-fxr — the one seam 0.23.0 cost, after adopting all of it.** fxr is `SettingsScaffold`'s only renderer and gave it its first screen check on the two-row split: one destination lit across both strips, h1+subtitle+body following the tab, masthead 65.203125 on all three views, 0 console errors on five routes, render pixel-identical to the hand-built page. The seam: `trailingActions` can no longer move the page tab, so its OPTIONS / SHORTCUTS pair stopped returning you to Settings from About or Repo (user ruling 2026-08-28). Asked as a render-prop `(tab, setTab)` or an optional controlled pair — and explicitly not as `filtersProps`, which 0.23.0 correctly removed |
| 2026-08-30 | **`SettingsScaffoldTabRows` filed from kol-fxr — the one remainder of `SettingsScaffoldFromFxrPage`, and its answered question.** fxr adopted kol-shell 0.22.0 and is the rebuilt scaffold's FIRST render (masthead 65.203125, all three views switching title+subtitle+body, drawer opening and closing, 0 console errors on five routes). The gap the return predicted is real: `layoutOptions` is not forwarded, and fxr's selector spans both strip rows off one state, so it rides `filtersProps` and bypasses `renderContent`'s tab argument. Their answer to "is the pair one idiom": **yes** — so a `tabs` entry should carry its own row |
| 2026-08-30 | **`SettingsScaffoldFromFxrPage` filed from kol-fxr — a conversation, explicitly not a task.** The user ruled fxr's `/settings` correct and wants `SettingsScaffold` re-aimed at it: `ContentFilters` as the header row instead of the scaffold's own third header shape, `PageHeader` keeping the masthead and its `actions` cluster. He also put the settings drawer ON that page, kol-r2b2's setup, both surfaces off one section definition. His instruction at filing: do not auto-trigger it, he wants to talk about it here. The kol-shell `SettingsSection`/`LabelRow` duplicate is recorded as this repo's own call, not an fxr ask — the rename itself closed on 0.112.0 |
| 2026-08-30 | **`SliderTrackVariableNotForwarded` filed from kol-mirror**, while adopting `SliderDualThumbAndPlayhead` — which otherwise landed clean, 18 call sites moved with no edit. The track-colour seam `Slider`'s docstring promises is not wired: no `style` prop, no rest spread, and `.slider-black` declares the variable on itself so no ancestor can reach it either. The same release's dual rail uses the fallback form and works, which is the whole fix |
| 2026-08-30 | **`SliderDualThumbAndPlayhead` filed from kol-mirror** — the same shape as the dropdown swap: a consumer maintaining a private copy of DS CSS to get one feature the DS does not have. Mirror's slider skin is verbatim this repo's `.control-slider` + `.slider-black` with a single intentional difference (track ink, which `--kol-slider-track` already exposes), plus a bordered pill this repo retired 2026-07-08. What it actually needs: dual thumbs + a playhead, a non-editable readout, and `defaultValue`/alt-click reset across both value controls. Closes 10 call sites and ~145 lines of consumer CSS |
| 2026-08-29 | **`QuadrantSyncShipAndChangelogRestore` (dotfiles) closed — component 0.130.0**; changesets cascades to 1.0.0 even with the peer flag set, so releases stay hand-bumped |
| 2026-08-29 | **`QuadrantSyncShipAndChangelogRestore` filed from a dotfiles session (iMac)** — the component shipped into the tree in the same pass that damaged the repo. A `changeset version` run cascaded nine packages to 1.0.0, the publish was aborted before the registry, and the cleanup that reverted it took 46 blocks of real changelog history with it. Filed rather than fixed because both remaining steps need the user: git is gated, and the cascade is a release-machinery config call |
| 2026-08-28 | **`PageHeaderActionsGrowsBlock` (kol-fxr) closed — shell 0.19.1**; a defect in `PageHeaderTrailingSlot`, shipped the same day |
| 2026-08-28 | **`DropdownHeightAndHover` (kol-fxr) closed — theme 0.90.0**: the icon ladder for the trigger, and a stale pin-back that had become a hover |
| 2026-08-28 | **`AppShellNavKeysHomeFirst` (kol-monitor) closed — shell 0.19.0**: a miss against `AppShellNavKeys`, shipped the same day |
| 2026-08-28 | **`PageShellScrollbarGutter` (kol-monitor) closed — shell 0.18.0**; unmeasured at both ends, monitor confirms on screen |
| 2026-08-28 | **`MediaClientBucketTable` (kol-fxr, kol-mirror co-signed) closed — media-client 0.3.0**; 🔴 held: fxr proposes exact 0.x pins as an estate convention |
| 2026-08-28 | **`MediaClientBucketTable` filed from kol-fxr, co-signed by kol-mirror** — fxr adopted `kol-media-client` 0.2.0 today (it had been hand-rolling the package's whole surface, R2-only, ~430 of 7,971 files reachable) and landed on the same duplicated BUCKETS table mirror already carries. The duplicate encodes which bucket needs the CORS proxy, it drifted the day it was written (R2's policy shipped 2026-08-27), and the wrong-way drift throws rather than degrades |
| 2026-08-28 | **`RailSettingsDisclosure` flagged 🔴 by kol-fxr** — shipped (shell 0.14.0) and withdrawn (0.16.0's reversal) without ever being adopted; retire-or-port held for the user |
| 2026-08-28 | **`RailTwoLevelSections` (kol-fxr) closed — shell 0.17.0**; shell 0.16.0 deprecated on npm (it calls `GRAB.marks`, removed in component 0.126.0, and its peer range permitted the pair) |
| 2026-08-28 | **`RailTwoLevelSections` filed from kol-fxr — built and proven there first.** fxr adopted `RailFlatGrabOpen` this morning and the swap, though correct, cost labs its four method headers: the flat rail has one level, its `sub` row is a bare label with no icon slot, and there is no section-anchor row — so ~44 labs rows flatten onto a rail built for five. User: *"you can see the main components … 5 or something total — and then you expand you see that they have main 56 items each … which should not happen automatically."* L1 20/32 (NavRail's row unchanged), L2 12/20 indented right, nothing auto-expands. Working fork at `kol-fxr/src/shell/AppRail.jsx` (+ `AppShellLocal` — `AppShell` has no rail seam). Also reports a live defect in the SHIPPED rail: `GRAB.marks` does not exist in kol-component 0.126.0, so `NavRail` throws on every pointermove near its edge in every 0.16.1 consumer |
| 2026-08-28 | **`RailFlatGrabOpen` (kol-mirror) closed — shell 0.16.0 + theme 0.88.0**: the flat rail returns and reverses the morning's SideNav rail; monitor and fxr take the same component |
| 2026-08-28 | **`DropdownOptionHoverPreview` (kol-mirror) closed — component 0.125.0**, the feature the DS swap had cost them |
| 2026-08-28 | **`DropdownOptionHoverPreview` filed from kol-mirror** — invited by 6f while closing `DropdownGhostWidthAndListHeight`. `onOptionHover(value | null)`: a dropdown that picks a VISUAL setting needs to preview on hover (mirror's blend-mode picker scrubbed 16 modes against the live composite; selecting each is 16 commits instead of one glance). `null` on leave AND on close is the load-bearing half |
| 2026-08-28 | **`DropdownGhostWidthAndListHeight` (kol-mirror) closed — component 0.124.0 + theme 0.87.0**; the hover half had shipped as 0.123.0 |
| 2026-08-28 | **`DropdownGhostWidthAndListHeight` filed from kol-mirror** — mirror retired its 324-line hand-built dropdown for the DS one at 29 call sites, and the studio's LOAD → Source picker (30 long labels, 24px row, ~300px shelf) broke on all four counts: ghost-stack trigger width, no list-height ceiling, no row pitch, panel-row hover. User: "this one is broken · 1 it's way too tall, 2 it's not fitting". Mirror carries `.kol-dd-panel button { height: 1.5rem }` + the hover pin meanwhile; the width and height halves cannot be fixed from a consumer stylesheet at all |
| 2026-08-28 | **`RailFlatGrabOpen` filed from kol-mirror** — user ruling reversing `RailSideNavPixelParity`: the app rail is the FLAT 48px column (one fixed div, direct children only — "1 div parent and everything is just in that container"), not a collapsed SideNav. The r2b2 grab pill (proximity + hysteresis, sticky 20/40/60/80 marks, 2.8s gsap trail) drags it 48→264 on `--kol-shell-rail-width`, which the content margin reads, so the page is pushed; icons never move, labels clip in uppercase, chevron only with `sub`, app name beside the mark; no theme toggle (Settings → Display), the Settings rung toggles its route. Mirror carries `src/components/NavRail.jsx` + `.rail-grab` meanwhile; monitor and fxr wait on the bump |
| 2026-08-28 | **`PageHeaderTrailingSlot` + `ViewToggleWellGap` (kol-website) closed — shell 0.15.0 · component 0.121.1**; four website tickets today, four shipped |
| 2026-08-28 | **`CatalogCardFrameAndZoom` (kol-website) closed — component 0.121.0 · theme 0.83.0**; kol-website deletes its four rules on the bump |
| 2026-08-28 | **`ControlToneSunken` (kol-website) closed — component 0.120.0 · theme 0.82.0 · framework 0.35.0**: `tone="sunken"` across seven controls, chip fg-08; kol-website deletes `.icons-ctl` on the bump |
| 2026-08-28 | **`RailSettingsDisclosure` closed — framework 0.34.0 · shell 0.14.0**: the panel leaf, fxr's RailSettings.jsx promoted; the yellow active icon held for the user |
| 2026-08-27 | **`RailSettingsDisclosure` filed from kol-fxr** — with the working implementation: Settings as a disclosure at the bottom, theme toggle inside, standing theme slot off. `NavRail` needs the seam so the shell rail matches. Yellow active icon flagged as an open question |
| 2026-08-28 | **`ColumnBrowserSeams` closed — component 0.119.0 + theme 0.81.0**; kol-r2b2 deletes its override block on the bump |
| 2026-08-27 | **`ColumnBrowserSeams` filed from kol-r2b2** — the last three stopgaps standing between that consumer and an override-free stylesheet |
| 2026-08-28 | **shell 0.13.2 — upgrade note** (kol-mirror's bump): the shell rail needs `kol-framework.css` imported, not just the theme; without it the rail is a horizontal bar with a green build. Install doc + shell doc + AppShell JSDoc carry it |
| 2026-08-28 | **`RailLogomarkAtTop` closed — framework 0.33.0 · shell 0.13.1**: the header slot; the raven at the top again, the tree-head rule counts the header in |
| 2026-08-27 | **`RailLogomarkAtTop` filed from kol-fxr** — the parity return's footer logomark was a design change the ruling did not make; user has never seen the raven at the bottom. `SideNav header` slot, `NavRail` logomark there |
| 2026-08-28 | **`SideNavWidthLadder` closed — framework 0.32.0**, `--kol-sidenav-w` 264 / 320 from 1536; the toc rail's equality with it is held for the user |
| 2026-08-27 | **`SideNavWidthLadder` filed from kol-website** — the rail's single 16rem default truncates brand's footer wordmark on desktop; ruled as a ladder (320 desktop · 264 laptop), carried locally until the framework declares it |
| 2026-08-28 | **framework 0.31.1** — fxr's re-measure of RailSideNavPixelParity (four glyphs + footer pixel-identical across /library ⇄ /labs) found the theme slot and pinned rows scrolling with a long tree; moved outside the scroll region |
| 2026-08-28 | **`RailSideNavPixelParity` (kol-fxr) closed — framework 0.31.0 · theme 0.80.0 · shell 0.13.0**: the shell rail is a collapsed `SideNav`; NavRail retired into it as the adapter; the logomark moved to the footer; the rail width token is live |
| 2026-08-27 | **`RailSideNavPixelParity` filed from kol-fxr** — the shell rail (`NavRail`) and the collapsed `SideNav` measured against each other for the first time: 48/20/40 vs 56/16/38. Ruling: one component, both states; `NavRail` retires into `SideNav`; `bottomItems`; one width token |
| 2026-08-28 | **CardCornerLeak (kol-monitor, by message) closed — theme 0.79.1**, `.kol-card` `background-clip: padding-box` |
| 2026-08-28 | **`ColumnBrowserChromeCorrections` closed — component 0.118.3 + theme 0.79.0** — the four r2b2 chrome rulings carried as measured; kol-r2b2 deletes its `src/index.css` block on the bump |
| 2026-08-27 | **`ColumnBrowserChromeCorrections` filed from kol-r2b2** — the four chrome rulings that were expected to ride along with `MediaLibraryPages` and did not; measured values, live on admin. all day |
| 2026-08-28 | **Two kol-monitor asks by message, shipped — component 0.118.2 (the ContentFilters fill chain) · shell 0.12.0 (`AppShell navKeys`, Opt+1…9)** — filed and closed in one pass; monitor told, mirror and fxr adopt the one prop |
| 2026-08-27 | **`WorkspaceSidebarGeometry` closed — framework 0.30.0** — `SideNav` takes action leaves and `currentPath`, a top-level node without an icon is a label hidden on collapse; the inbox file had vanished mid-build, re-filed in `done/` from the read; receipt written to kol-fxr's outbox |
| 2026-08-27 | **`WorkspaceSidebarGeometry` filed from kol-fxr** — three hand-rolled workspace rails (~1,370 lines) of one idea; `SideNav` cannot take a dispatching rail; the collapse rule to write down |
| 2026-08-27 | **`MediaKindsExportPath` closed — component 0.118.1** — the `.js` utilities get explicit exports entries; receipt returned to kol-r2b2 |
| 2026-08-27 | **`MediaKindsExportPath` filed from kol-r2b2** — the two utilities promoted in 0.118.0 cannot be imported: `./utilities/*` maps to `.jsx` and both are `.js` |
| 2026-08-27 | **`MediaLibraryPages` closed — component 0.118.0 · media-client 0.2.0** — MediaLibrary's `browse` + `library` pages (r2b2's FileList + header cut in two), `page` a deprecated alias, one client with buckets; receipt returned to kol-r2b2, brand's swap named for kol-website |
| 2026-08-27 | **`MediaLibraryPages` filed from kol-website** — brand's `/library` still renders the 08-26 `MediaLibrary`, r2b2 has moved to `ColumnBrowser` + a forked file wall; the user wants the newer surface in brand as two pages (filters wall · folder/files) plus a local gallery. The wall promotes from r2b2's `FileList`; `kol-media-client` grows a bucket option |
| 2026-08-27 | **`IconSetCut` closed — icons 0.24.0** — per-glyph `cut` on the barrel (`KOL_ICON_SET_V1_META`), derived at build, gated; receipt returned to kol-website |
| 2026-08-27 | **`IconSetCut` filed from kol-website** — brand's icon gallery gained a TYPE filter (stroke \| solid) and had to glob the package's SVGs to learn it; the set should carry its own cut |
| 2026-08-27 | **`ControlToneInverse` closed — component 0.117.0 · theme 0.78.0** — `tone="inverse"` on ViewToggle · Dropdown · Input · SearchInput, forwarded by ContentFilters; receipt written to kol-website's outbox (the filer had left no stub) |
| 2026-08-27 | **`ControlToneInverse` filed from kol-website** — brand `/icons` on the app-tier catalog pattern over a `fg-04` page wash; the icon ViewToggle's grey well reads as a second plate there. Asks `tone="inverse"` across ViewToggle (icon) · Dropdown · Input, with ContentFilters forwarding it to its search |
| 2026-08-27 | **`FullConsumptionContract` closed — theme 0.77.0 + the checklist** — `/core` shipped; "fully consuming" is six greps on one doc page, empty = on KOL; receipt returned to kol-monitor |
| 2026-08-27 | **`FullConsumptionContract` Part 1 shipped — kol-theme 0.77.0** — `@kolkrabbi/kol-theme/core`, the app tier without the domain packs, umbrella unchanged; Part 2 (define + certify full consumption) 🔴 waits on the user |
| 2026-08-27 | **`EditorOverlaysOnFullscreenOverlay` closed — ruled; theme 0.76.0 · component 0.116.0** — one overlay tier, two fixed scrims, the z ladder as the contract (`.kol-popover` / `.kol-tooltip` off 1000, search onto modal); `01-tokens.md § Stacking` + `§ Overlays`; receipt returned to kol-fxr |
| 2026-08-27 | **`EditorOverlaysOnFullscreenOverlay` filed from kol-fxr** — the editor's six overlays against `FullscreenOverlay`: five scrims, a duplicated `SCRIM` const, z-1000/1100 over the DS's z-100, no focus trap anywhere. Asks for the tier ruling, a scrim token and the z-contract |
| 2026-08-27 | **`ColumnBrowserWidthsPersist` closed — component 0.115.0** — `columnWidths`, the controlled counterpart to `onColumnResize`; receipt returned to kol-r2b2 |
| 2026-08-27 | **`ColumnBrowserWidthsPersist` filed from kol-r2b2** — the remainder `ColumnBrowserResize` left: widths drag and report, but `columnWidth` is one number in, so they cannot be restored |
| 2026-08-27 | **`DocPageAndKindShowcase` closed — component 0.114.0 · theme 0.75.0 · framework 0.29.0** — every document on `DocPage`, frontmatter rendered, the framework's overlay clash retired, the 14-kinds showcase page; receipt returned to kol-r2b2 |
| 2026-08-27 | **`PlaybackBarAndAudioSheet` closed — component 0.114.0 · theme 0.75.0 · icons 0.23.0** — the QuickTime bar as a molecule with `speaker` / `chevrons-right` / `music-note`, `AudioSheet` in two variants, `readCover` reachable; receipt returned to kol-r2b2 |
| 2026-08-27 | **`ShelfCardTiltWrapsCard` closed — content 0.13.0 · component 0.113.0** — the grounded tilt lifted into `useTilt`, the shelf card leans as one with its border, the zoom is back; receipt returned to kol-website |
| 2026-08-27 | **`TiltBentoVoiceAndDefaults` closed — component 0.113.0** — the subtitle's dead class replaced with `kol-mono-14`; defaults confirmed in the docstring; receipt returned to kol-website |
| 2026-08-27 | **`ColumnBrowserResize` closed — component 0.113.0 · theme 0.74.0** — height + per-column width drags on Finder edge handles; receipt returned to kol-r2b2 |
| 2026-08-27 | **`LabeledControlSection` closed — component 0.112.0** — `SettingsSection` renamed `LabeledControlSection`, no alias (BREAKING); receipt returned to kol-r2b2; kol-fxr's `AutoControls.jsx` owes the same swap and has no receipt — needs telling |
| 2026-08-27 | **`ContentMediaFocusBinding` closed — component 0.111.0 · theme 0.73.0** — `--kol-media-focus` is the one anchor the fit and the zoom read, unset = today; receipt returned to kol-monitor (remainder: bump, bind `top left` on `:root`) |
| 2026-08-27 | **`ContentFiltersFirstGroupFixedWidth` closed — component 0.111.0 · theme 0.73.0** — the first filter group is one catalog column (`(100cqw − 120px) / 6` on the header row as a container), flowing groups start over the second card; receipt returned to kol-monitor (remainder: bump, drop the `w-40` interim) |
| 2026-08-27 | **`ShellPageWash` closed — kol-shell 0.11.0** — `AppShell` paints the primary back + `pageWash` → `--kol-shell-page-wash`; `PageShell` paints the wash-or-primary; receipt returned to kol-monitor (remainder: bump + one prop, RailFrame back to `contents`, the override rule deleted) |
| 2026-08-27 | **`LabeledControlSection` filed from kol-r2b2** — user ruling: `SettingsSection` is the wrong name. Rename to `LabeledControlSection`, no alias, render untouched. kol-fxr's adoption of it for a parameter rail (not a settings drawer) is the evidence the name misleads |
| 2026-08-27 | **`DocPageAndKindShowcase` filed from kol-r2b2** — documents on one plate (overlay page / column zoom, code transparent + full width), the frontmatter block, arrows fixed at the viewport edges with gutters, the framework overlay clash, and a showcase page with every one of the 14 kinds + the media sheets |
| 2026-08-27 | **`PlaybackBarAndAudioSheet` filed from kol-r2b2** — the QuickTime bar ruled against the reference (radius 12, white glyphs, mm:ss elapsed / total, pill-knob scrubber, speed `>>`), `VideoSheet` on it, `AudioSheet` in two variants; `readCover` unreachable through the exports map; three icons missing |
| 2026-08-27 | **`ColumnBrowserResize` filed from kol-r2b2** — height drag + per-column width drag on `ColumnBrowser`, Finder-style edge handles; the user ruled it a DS set, not a consumer hack |
| 2026-08-27 | **`TiltFamilyForks` filed to kol-website** — home Foundry + Highlights and WorkDetail tilt on local forks; the DS shipped the Tilt family (component 0.110.0, `BentoCard` → `TiltBento`) and the site swaps onto it |
| 2026-08-27 | **`ShelfCardMotion` filed from kol-website** — the shelf card enters (perspective settle, house ease) and tilts (`TiltCard grounded`) by default; the site stops rendering its own card |
| 2026-08-27 | **`SectionCtaConnectVariant` filed from kol-website** — the contact CTA as a `SectionCta` variant with copy defaults; the site wrapper retires |
| 2026-08-27 | **`SettingsPanelApproved` filed from kol-r2b2** — the locked drawer as the reference; four defects alongside |
| 2026-08-27 | **`SectionBackgroundProp` filed from kol-website** — every section takes `background` as a prop; today it's a hardcode or a `className` gamble |
| 2026-08-27 | **`WorkCardAndShelf` filed from kol-website** — the work card's ruled values, `ParallaxShelf` on the same DS card (WorkCard retires), eyebrow captions |
| 2026-08-27 | **`TagTertiary` filed from kol-website** — borderless mono-voice Tag variant (fg-80, 400 / 0.04em / 100%) ruled on the `/work` rows |
| 2026-08-27 | **`CollectionItemMinWidth` filed from kol-website** — a collection item sizes to a nowrap row line's min-content and the grid overflows the page; `min-width: 0` on the items, `minmax(0, 1fr)` on the list track |
| 2026-08-27 | **`SettingsPanelEyebrowAndDropdowns` filed from kol-r2b2** — eyebrow labels, dropdown rows, the all-chip |
| 2026-08-27 | **`KindPreviewMarkdown` filed from kol-r2b2** — markdown in the column preview is a placeholder; it should be prose |
| 2026-08-27 | **`SettingsPanelCompliance` filed from kol-r2b2** — the chip row went to `Tag` pills in 0.98.0; must be the `.kol-control` chip |
| 2026-08-27 | **`WorkListingRowsAndFilters` filed from kol-website** — the work row's ruled values (168 / 16 / fill thumb, no hairline, hover fg-08, typeface-row voices) and five family defects the `/work` swap surfaced, the big one: the row's `@container` md step has no container anywhere in the DS, so every row on every page renders base values |
| 2026-08-27 | **`SettingsPanelChromeAndColumnPreview` filed from kol-r2b2** — drawer chrome/type + a preview seam for the column browser |
| 2026-08-27 | **`ColumnBrowserCursorSeed` filed from kol-r2b2** — cursor start position + clamped re-landing; consumer seeds via a programmatic click meanwhile |
| 2026-08-27 | **`ColumnBrowserOnPick` filed from kol-r2b2** — one seam for the picked file; consumer wiring already in place |
| 2026-08-27 | **`ColumnBrowserCursorStart` filed from kol-r2b2** — the cursor default draws a second highlight; seen once the root column became the three buckets |
| 2026-08-27 | **`FoundrySpecimenSections` filed from kol-website** — the last three specimen sections + their two cards onto the family, and five slug-page defects in the package (double dropdown, full-ink cell outline, missing glyphs undimmed, variable-font frame, filters title gap) |
| 2026-08-27 | **`FontPreviewClampAndBearings` filed from kol-website** — corrects `FontPreviewNoClamp`: the line clamp was right and returns; "cutting" meant the italic glyphs' side bearings clipped at the box edges. Screenshots in `_assets/2026-08-27-font-preview-bearings/` |
| 2026-08-27 | **`ColumnBrowser` filed from kol-r2b2.** The Finder-column folder view built and ruled locally after the collection shipped, plus four follow-up rulings on shipped pieces (row thumb ratio, card border, overlay scrim, select-mode stability). Consumer source verbatim; screenshots in `_assets/2026-08-27-column-browser/` |
| 2026-08-27 | **`FontPreviewNoClamp` filed from kol-website** — Font Preview's per-rung line clamp cuts the Icelandic sample with an ellipsis on every typeface page; the passage renders whole |
| 2026-08-27 | **`TypefaceCardRevealText` filed from kol-website** — the typeface card's hover reveal shows the foundry's Icelandic sample (first sentence of "Rennimjúkt eðal flauel…"), not the English pangram; the passage exports once from `glyphData.js` |
| 2026-08-27 | **`FoundryComponentsReconcile` filed from kol-website** — the site's foundry components fork kol-foundry and lead it by a month of edits; the package syncs to the local copies so the fork can go. Local sources + unified diffs staged in `_assets/2026-08-27-foundry-reconcile/` |
| 2026-08-27 | **`TypefaceCardAndRow` filed from kol-website** — the typeface row's ink ladder (name + classification full, year 64), the card's title on the row's voice, and a `reveal` hover slot on the card; all values tuned and approved locally first |
| 2026-08-27 | **`ContentRowsAndPrintCard` filed from kol-website** — `/prints` is on the set locally; this closes what the rows and the print card still lack, and the kicker/label/eyebrow naming |
| 2026-08-27 | **`ContentFiltersTitleGap` filed from kol-website** — tuned locally first (title `pr-4`); the narrow-group arrangement next to it turned out to be the existing `stack: true`, nothing to port |
| 2026-08-27 | **`ContentFiltersCollection` filed from kol-r2b2.** The admin replaced its toolbar, `MediaCard`/`MediaRow`, `SortControls` and `SelectIndicator` with the collection, iterating on a pnpm link to `packages/component`; six DS slot/prop additions sit unpublished in that tree, three pieces are missing (`SortHeader`/`SortControls`, `SizeOrDownload`, on-media `ToggleCheckbox`). Consumer JSX carried verbatim; screenshots in `_assets/` |
| 2026-08-27 | **`SectionHeroFootGap` filed from kol-website** — the clearance ask from earlier today was over-specified (ramp + overlap); the tuned value is overlap + ~50 |
| 2026-08-27 | **`StackCardHover` filed from kol-website** — first ticket under the new order (local first, file the final values): hero zoom rung + article title hover dim |
| 2026-08-27 | **`SectionTextBodyMono` filed from kol-website.** Removing the overrides exposed the molecule's sans body; the user ruled mono as the default voice for the whole set |
| 2026-08-27 | **`SectionNewsletterForm` filed from kol-website** — first render of the new newsletter card: short field, and a `bodyClass` override on the molecule |
| 2026-08-27 | **`SectionNewsletter` filed from kol-website** — the follow-up the SectionSet return named; the newsletter joins the family |
| 2026-08-27 | **`ListingCardHoverZoom` filed from kol-website.** The 08-12 zoom ruling never reached kol-content's ListingCard |
| 2026-08-27 | **`ContentCollectionColsResponsive` filed from kol-website** — `cols` as a breakpoint map |
| 2026-08-27 | **`ListingCardThumbBorder` filed from kol-website.** The user's standing rule is no hairlines on thumbs; `ListingCard` has no seam and the article card frames by default |
| 2026-08-27 | **`ContentCollectionCols` filed from kol-website.** Moving `/stack` onto the collection meant back-solving a track minimum to get three across; the rule was always a column count |
| 2026-08-27 | **`SectionHeroFootClearance` filed from kol-website.** Dev vs live on Stack: the DS hero's foot overlap doesn't push the end-justified text up; the consumer had halved the overlap to hide it |
| 2026-08-27 | **`DisplayTightRamp` filed from kol-website.** The Stack hero exposed `kol-display-lg` (Tight 500) beside the numbered Narrow ramp — two display voices, no reason. User ruled the Tight face wins: 01/02 go Tight, `display-lg` retires, the size-named system leaves the theme |
| 2026-08-27 | **`SectionSplitMediaBounded` filed from kol-website.** The height ladder floors but never caps, so a split with an image is sized by the image. The media half must take its size from the section |
| 2026-08-27 | **`SectionHeightForty` filed from kol-website** — one more rung below 60 on the shared ladder; default untouched |
| 2026-08-27 | **`SectionHeightLadder` filed from kol-website** — the three sections the split-height return left out. One family, one ladder |
| 2026-08-27 | **`SectionSplitHeight` filed from kol-website.** The split is the one section with no size; it takes the family's ladder as min-height, default 60 |
| 2026-08-27 | **`ThemeToggleLabelTarget` filed from kol-website.** The toggle tells the eye the current mode and the screen reader the target mode; user ruled the target. One line in kol-framework |
| 2026-08-27 | **`SectionThemeInverse` filed from kol-website.** A section set to the paired theme by prop — the theme is already a scope, so `inverse` is a relative remap of the whole token set, not a per-utility swap. Three organisms |
| 2026-08-26 | **`SectionHeroSplitHeight` filed from kol-website.** First split hero in the wild collapsed to its text column — the split's height classes are produced by a runtime string replace and exist in no source, so the scanner never emits them. A class Tailwind cannot read is not a class |
| 2026-08-26 | **`SectionHeroNoOverrides` filed from kol-website.** After the label voice, the rest of the hero's per-slot overrides surfaced side by side with the split on Studio. User: overrides are the fork the set exists to end. Ask: bare `SectionText` in the hero |
| 2026-08-26 | **`SectionRevealSeams` filed from kol-website.** The user counted the cost of the adoption waves: six surfaces lost their entrance reveal, one degraded, each logged as "eyeball owed" and never acted on. The reveal system stays the app's; the set needs per-element class + style seams so it can land |
| 2026-08-26 | **kol-website is off `CardFeatureItem`** — on 0.77.0, its one direct call site (`HomeWorkshop`) imports `SectionCardItem`; no consumer there reads the deprecated alias any more. Same for `FullBleedHero` · `FeatureSplit` · `FeaturesCardSection` · `CtaGlobal` · `FeaturedCarousel` · `FoundryCTA`: zero call sites in kol-website (web + brand) as of this note (`TypefacePage` · `InDevelopmentSection` · `Home` moved onto `SectionHero` / `SectionCards` in the same pass) — the aliases can go at the next major without waiting on this consumer |
| 2026-08-26 | **`SectionCtaEditorial` filed from kol-website.** The editorial CTA runs edge to edge and its row labels sit on a voice the set no longer uses. User ruled: cap 1600, eyebrow voice on the labels |
| 2026-08-26 | **`SectionSplitMediaClip` filed from kol-website.** Restoring the Home foundry tilt inside `SectionSplit` showed the frame clipping the rotated card. Ask: a `mediaClip` seam so an interactive node owns its own clipping |
| 2026-08-26 | **`SectionTextLabelVoice` filed from kol-website.** Two eyebrow voices in one set — `SectionText`'s ruled label (heavy accent caps) and `SectionHero`'s private override (quiet helper). User ruled the hero's is the one. `SectionSplit` has no label seam, so the fix is the rule |
| 2026-08-26 | **`SectionHeroCarouselSeams` filed from kol-website.** The round-2 carousel branch is missing the three seams the foundry index needs (`renderTitle` · `ctaLabel` · `onNavigate`); every other hero on the site moved to `SectionHero` in the same pass, `/foundry` waits on this |
| 2026-08-26 | **`SectionHeroRound2` filed from kol-website.** Adopting `SectionSet` across the site found the remaining hand-built heroes (Stack, licensing) and `FeaturedCarousel` are the same anatomy `SectionHero` can't yet express: vh presets (`full` under the navbar · `80` · `60` — user's ruling), bottom-pinned content, gradient veil, a fold-overlap slot, array media, text-only, and `FoundryCTA` folded in. One hero component is the bar |
| 2026-08-26 | **`FeaturedCarouselFullWidth` filed from kol-website.** Studio hero double-video root-caused to the `.is-slides` slide clamp in kol-framework.css — `fullWidth` never widens the slide, so neighbours peek in on a full-bleed stage. Ask: full-width slides under `fullWidth`, no autoplay off-stage. No consumer stopgap; Studio moves to `SectionHero` on the set's return |
| 2026-08-26 | **`FrameworkComponentPeer` filed from kol-website.** The `MediaLibraryReconcile` bump (component 0.69.1) surfaced the leftover from `NestedDsDependencies`: kol-framework 0.24.0 still nests kol-component via a `dependencies` 0.x caret, so Navbar / SideNav / ShellHeader render 0.68.1 molecules beside the app's 0.69.1. Ask: peers in kol-framework, republish. Stopgap at the consumer is a workspace override with a delete-on-return note |
| 2026-08-26 | **`SectionSet` filed from kol-website (a SET).** The section organisms ship with zero shared text primitive and zero shared media slot — the exact gap the card family closed on 08-15 with `ContentText` + `ContentMedia`. Ask: the section-tier twin (`SectionText`, `ContentMedia` reused), five organisms rebuilt on it under the `Section` prefix with aliases, `align` on the split instead of a centred sibling, a `SectionFaq`; one wave. Studio is the reference and first adopter |
| 2026-08-26 | **`ContentSetRetirement` filed — the wave gets a tracking ticket.** Step 1 shipped the same day: `MediaCard` · `MediaRow` · kol-shell `GridCard` · `PrintGridCard` · `ListingCard`/`ArticleCard` · `WorkCard` · `WorkListItem` · `TypefaceLibraryItem` carry `@deprecated` → component 0.69.1 · shell 0.6.2 · store 0.2.1 · content 0.9.1 · foundry 0.6.2. Step 2 is per consumer; step 3 waits on all of them. Filed because the arc had sat silent since 08-15 with no ticket to surface it. |
| 2026-08-26 | **`MediaLibraryReconcile` filed from kol-website.** brand.kolkrabbi.io/library (DS `MediaLibrary`, page variant) and media.kolkrabbi.io (kol-r2b2 `FileList`) show the same bucket two ways; kol-r2b2 had already retired the DS `MediaBrowser` tab on 08-26 as the weaker of three renders. Ask: the page variant takes FileList's read-only render — breadcrumb + stats on top, click-to-enter folders, 260px tiles, sort direction, Flat, paging — so one organism serves both and FileList can re-adopt with a writes slot |
| 2026-08-26 | **`SettingsPanel` filed from kol-r2b2.** The admin's per-bucket display-settings drawer, staged as a spec for a DS organism with drawer + overlay presentations. Source file is `src/SettingsPanel.jsx` (220 lines, DS `Button`/`Divider`/`Icon` only). Case, alignment and the segmented toggle are the user's to fix in the DS |
| 2026-08-26 | **`NestedDsDependencies` filed from kol-website.** The 0.23.0 / 0.68.1 bump reached nothing inside kol-workshop: `ShellLayout` resolved kol-framework 0.20.1 and `DocumentationReader` kol-component 0.39.0 — each app package pins the tier below as `dependencies` with a 0.x caret, and pnpm nests its own copy (chess 0.38.0 · content 0.63.0 · foundry 0.52.0 · store 0.8.0; only kol-dashboards declares peers). Two of the four mobile returns were invisible on the consumer until a root `pnpm.overrides` forced one copy. Ask: peers in the five, republish |
| 2026-08-25 | **Five mobile briefs filed from kol-website's mobile audit** (`plans/2026-08-25-mobile-audit-web-brand.md`, Playwright at 393/360 over 100+ routes). `SideNavMobilePosition` is the one that hurts: every package-SideNav consumer opens one viewport down on phones because a className utility beats the media rule. `TableMobileScroll`, `CodeBlockMobileOverflow`, `WorkshopShellMobile` are missing-overflow defects in existing chrome; `MobileTouchFloor` is a 🔴 ruling request on 10px type and sub-36px hit areas |
| 2026-08-15 | **`ShellHeaderFilterRefinements` reopened and re-shipped** → **kol-shell 0.1.2** (registry-verified). 0.1.1 read the ticket's "at the divider level" as the header row *above* the divider; the ruled position is the row *below* it. The ambiguity was the filer's, the unchecked read was ours — a reference build existed and was not compared against. Now: LIST/GRID **below the divider, right-aligned, always visible**, filter groups sharing that row on the left only while the toggle is open, so an expanded group cannot move the strip. Filter values wear the strip idiom instead of `Tag` chips (**superseding 0.1.1's `size="sm"` fix** — no chips remain), which is what `TabStrip`'s new `Set` form for multi-select bought: one ink recipe, not a second copy. **Stated deviation:** no `text-transform` added — `LIST`/`GRID` are uppercase because they are *authored* so, and the no-auto-casing law stands. 20 gates clean. 📌 Remainder is kol-monitor's: bump ≥0.1.2 |
| 2026-08-15 | **`ShellHeaderFilterRefinements` filed from kol-monitor — kol-shell 0.1.0's first adoption QA round.** PageHeader h1/subtitle gap dropped on recreation, tag chips md→sm, LIST/GRID toggle to the divider row with filter groups pinned below-left. All three are shell files; no consumer seam exists |
| 2026-08-15 | **`ListGridCards` filed from kol-monitor — an open collection, not a task.** List + grid card pairs from active repos, screenshots in `_assets/2026-08-15-list-grid-cards/`, accruing toward an eventual card-family consolidation. First pair: kol-monitor's preset cards (kol-shell `GridCard` grid + list row, dark theme) |
| 2026-08-15 | **`KolSourcesPnpmResolution` filed from kol-mirror — the manifest fix shipped and still does not work.** The kol-shell line was reported in chat, added in kol-theme **0.42.1**, and the consumer removed its local workaround on that basis; the overlay broke again on the next reload. The line is correct — the *addressing scheme* is not: package-relative `@source` paths assume a flat `node_modules`, and pnpm gives kol-theme its own store directory where none of the sourced packages are siblings. So this was never a missing-line bug, and adding lines will not fix the next one either. Two process notes worth keeping: the defect was carried verbally for a whole session instead of filed, which is why the fix went out against an incomplete report; and it is **all twelve paths**, so pnpm consumers have been missing DS utilities for as long as they have been on pnpm, undetected because a missing utility renders as a slightly wrong layout rather than an error |
| 2026-07-31 | restructured to the estate-wide standard: live entries moved to `inbox/`, this INDEX became the ledger, emoji states adopted |
| 2026-08-01 | the queue reached **zero** — the last entries closed with resolutions and receipts returned to the repos that filed them. Detail: `.kol/llm-context/session-log/2026-08-01-MILESTONE-lobby-return-half-and-adoption.md` |
| 2026-08-01 | **`lobby-spec-two-gaps` came back 🟢.** ⚫ `retired` became the shared ladder's sixth state (humpty had run it absorbed onto ⚪, so one glyph meant both *revisitable* and *terminal*), and `staged:` was settled as the entry field with `date:` a read-only alias. Both fixes live in `~/.dotfiles/docs/operations/systems/lobby/` |
| 2026-08-01 | **the agent invented a remainder, and it was called.** Closing that ticket, it promoted two idle observations into a 📌 `Remainder here: two` — a `status:` naming mismatch and a question about whether parked entries were really retired. Neither was work this repo owed. It put tasks into a queue the user had emptied, which is the opposite of what the ledger is for. Field corrected to `none` before the record graduated |
| 2026-08-01 | **`llm-rules-bulletin-in-scaffold` filed to dotfiles.** theme **0.24.0** deleted `.text-body` / `--kol-fg-body` with no fallback — a consumer loses its text colour silently — and that is what `LLM_RULES.md` § BULLETIN exists to announce. This repo's copy has one and has used it before (`kol-theme@0.12.0` colorless links), but it is a **local file**: kol-website is the only repo on the shared symlink, and that template has no BULLETIN section at all. Second ask in the same ticket: put this repo on the symlink too — 3 of 4 hold a regular file, and the per-repo content needs a home first |
| 2026-08-01 | **LOBBY EMPTIED.** 119 records graduated to `.kol/llm-context/lobby-history/`; `inbox/` · `done/` · `archive/` · `outbox/` all hold nothing but `.gitkeep`. The ledger keeps the states, the bar and this history — the records themselves are agent history and now live where history lives |
| 2026-08-06 | **Two briefs filed from kol-website.** `RecordManager` — the Framer-CMS-shaped record table + FieldRow detail panel (three reference screenshots in `_assets/`, light/dark parity part of the ask); `SideNavGrabResize` — grab-edge resize/snap-collapse for SideNav, prior art cited from kol-mirror, two `--kol-sidenav-*` tokens to mint |
| 2026-08-09 | **The table shadow, killed at the source.** The loose clip at the lobby root (now `_assets/2026-08-06-table-shadow-brand-graphics.png`) showed a shadow band inside the brand app's Graphics table. Root cause was ours: `.kol-table-wrapper` still carried two `--kol-surface-primary` "cover" gradients orphaned by the 2026-07-28 fade ruling — invisible on primary surfaces, a shadow band on every other. Deleted; shipped as **theme 0.30.2** |
| 2026-08-09 | **Queue emptied — both 2026-08-06 briefs closed with receipts returned.** `RecordManager` → **component 0.25.0** (organism + `FieldRow` molecule + `StatusChip`; `Table` gained `rowClassName`; no kol-theme half needed — all chrome reused). `SideNavGrabResize` → **framework 0.14.0** (grab edge + snap-collapse; the `data-sidenav="collapsed"` renderer rebuilt deliberately after its 2026-07-29 deletion; three `--kol-sidenav-*` tokens minted, in kol-framework.css where that family actually lives — the brief said kol-theme). kol-website's outbox stubs synced 🟢; its adoption remainders (slide-deck manager + Library onto RecordManager, retire the brand SideNav toggle) stay with kol-website |
| 2026-08-09 | **TransportIcons filed from kol-ds-fxr.** kol-icons 0.10.0 ships `play`/`pause`/`skip-*` but not `stop`/`rewind`; the editor's TransportBar requests both by name and logs `Icon "…" not found in icon set` on every mount (editor + labs chrome). Ask: add the two glyphs — or alias existing skip drawings — so the names resolve |
| 2026-08-09 | **DropdownViewportClamp filed from kol-ds-fxr.** A ~30-item Dropdown (labs "Add FX…") renders past the viewport — `flip: false` by design, and the Popover `size` middleware sets width only, never `maxHeight`/overflow. Ask: clamp the panel to `availableHeight` with an internally scrolling `.kol-dd-list`; flip-above stays the fused-edge law's call. Consumers cannot shim `.kol-dd-*` (the no-shims ruling), so there is no downstream workaround |
| 2026-08-09 | **Queue emptied — both kol-ds-fxr tickets closed with receipts returned** (the editor's `lobby/outbox/` created for them — its first). `TransportIcons` → **icons 0.13.0**: `stop` + `rewind` real drawings in `playback/`, promoted from the legacy stroke shelf on the user's call ("we have them already"), keyline-conformed; NOT aliases — the ticket's described rewind glyph was literally `skip-start`, so the name got the legacy double-triangle instead. Inventory regenerated **171 · 26** (header had sat stale at 165 since 0.8.10). `DropdownViewportClamp` → **component 0.32.3 + theme 0.32.4**: Popover's `size` `apply` adds `maxHeight` from `availableHeight` beside the exact fused width, `.kol-dd-panel` flex column, `.kol-dd-list` scrolls, opens scrolled to the checked row; no height prop (automatic), flip-above declined (fused-edge law). All 19 gates clean; both versions registry-verified. 📌 Both remainders are the editor's: adopt the bumps |
| 2026-08-09 | **LabsNavIcons filed — 15 SVGs collected, not described.** The labs sidebar's real glyphs (`ptrn-dot` `grid-horizontal` `monitor` `target-lock` `ptrn-checker` `cycle` `sum` `a-framed` `dith-flow` `paint-drop` `ball` `phone` `font-01` `camera` `aa`) are all absent from kol-icons 0.13.0 — the editor's labs nav has been substituting guesses. Sources copied verbatim from kol-labs-single's loader shelves into `_assets/2026-08-09-labs-nav-icons/`; `cycle` doubles as labs' Randomize button glyph |
| 2026-08-09 | **`deletion-is-never-authorised` closed from this side — ⚫ `retired`, user's call: "outdated".** Not a returned receipt: humpty never reported back, and this repo is no longer waiting on it. It cost nothing to close — `Remainder here` was already `none` (files restored to `_tmp/2026-08-01-jetbrains-mono-statics/`, both `CLAUDE.md` law edits shipped to dotfiles), so the row was tracking someone else's open work. Graduated to `.kol/llm-context/lobby-history/archive/`. The `rm`-gate wiring stays humpty's, tracked in humpty's ledger |
| 2026-08-09 | **Queue emptied — LabsNavIcons closed with receipt returned** → **icons 0.14.0**. 11 of the 15 names minted (plus `globe` from the legacy shelf on user ask, and `dith-drift` — the labs dash drawing kept beside the rack wave that took the `dith-flow` name on the user's pointer); the new `pattern` group born (5); typography +3. Four names deliberately NOT minted — dupes of shipped drawings, labs maps instead: `target-lock`→`target` · `monitor`→`desktop` · `phone`→`mobile` · `cycle`→`refresh`. Reviewed frame by frame on the staged `_tmp/` proposal page (AA re-centered, ball's star filled + bordered, wave dith-flow); user sign-off "ship it". Inventory **184 · 27**; 19 gates clean; registry-verified. 📌 Remainder is the editor's: bump + swap the four mapped names in labs `GROUP_ICONS` |
| 2026-08-12 | **TextareaResizeClamp filed from kol-fxr.** The Textarea resize grip's corner drag writes an unclamped inline width on the shell (`Textarea.jsx`, floor 120, no ceiling) — any fixed-width container overflows on an X-drag; seen as a horizontal scrollbar in the design editor's right rail. Ask: cap the drag at the container's width; optional `axis` prop for rail consumers. kol-fxr stopgapped with `className="max-w-full"` (public API) pending the atom's own clamp |
| 2026-08-12 | **MenuItemDescenderClip filed from kol-fxr.** Menu labels with descenders clip ("Show grid") — `MenuDropdownItem`'s `truncate` span carries `kol-helper-12`'s `line-height: 1`, so the overflow clip cuts below the baseline. Ask: taller line box on the truncating span (zero layout shift inside the centered h-8 row). kol-fxr fixed its three look-alike toolbar dropdowns locally with `leading-normal` — the DS molecule is the one consumers can't touch |
| 2026-08-12 | **ModalConfirmLabels filed from kol-fxr.** Three findings in one: `confirm()`/`prompt()` accept no button labels (Cancel/OK hard-coded — a restore dialog can't say "New file"); the dialog title wears `kol-helper-12` on wrapping paragraph text (protocol violation the estate keeps re-hitting); and `useModal()`'s silent native-`window.confirm` fallback hid a missing `ModalProvider` in kol-fxr for months. Ask: label options + `kol-mono-*` title; the fallback warn is the DS's call |
| 2026-08-12 | **All three tickets addressed in one run** (same session, evening). `TextareaResizeClamp` — drag width clamped to the parent's content width, `axis` prop added. `MenuItemDescenderClip` — `leading-normal` on every `truncate`+`kol-helper-*` span in the packages (6 spans, 5 files: MenuItem ×2, ColorInputRow ×2, FieldRow, RecordManager — the ticket named one path; the sweep killed the defect class). `ModalConfirmLabels` — `{ okLabel, cancelLabel }` on confirm/prompt, title `kol-helper-12`→`kol-mono-12`, once-per-session warn in the native fallback. 19 gates clean. **Unpublished local delta — 🟢 waits on the user's publish** |
| 2026-08-12 | **The second wave — three more addressed, the icon batch staged** (same session, later). `KolComponentDeepExports` — five-tier subpath exports + README. `NavLinkUnderline` — `.kol-link-underline` minted in kol-utilities.css (+focus-visible, reduced-motion, `--kol-link-underline-size`). `ColorSwatchFieldSizing` — radius default `sm`, `control-sm` named size, Input `slotLeft`. `EditorInspectorIconBatch` — ten glyphs drawn, proposal page staged at `_tmp/2026-08-12-inspector-icon-proposals/`, review pending. Also this wave (not lobby): sans-heading-04 line-height 100%→120% on the user's call (CSS + vault doc). Gates: 16/19 clean — the 3 violations are all `docs/visual-reference/INDEX.md`, a parallel session's in-flight file, untouched here |
| 2026-08-12 | **Two briefs filed from kol-fxr's Figma-inspector pass.** `EditorInspectorIconBatch` — ten glyphs (resizing ×3, constrain, corner-radius, opacity, line-height, letter-spacing, angle, mask), each with its live interim stand-in named so the swap is mechanical. `ColorSwatchFieldSizing` — swatch flush with input chrome (26px, 4px radius default + corner prop; stretch: an Input slot so [swatch][hex] is one container) |
| 2026-08-12 | **Two more from the same pass — the hack-enders.** `PropertyField` — the Figma property input (affordance + value + ADJACENT unit) as a real Input variant; the consumer's ch-width/negative-margin stopgaps are named in the entry and die on adoption. `SegmentedFilledVariant` — the user's segmented state law (filled tiles always; outline ring = selected only; stateless action strips); kol-fxr's `SegBar.jsx` is a declared stopgap twin that dies on ship |
| 2026-08-12 | **PUBLISHED theme 0.35.0 · component 0.35.0** (registry-verified, exports map confirmed live) — and the six addressed tickets went 🟢 with the versions cited, entries graduated to `done/` with resolutions: `TextareaResizeClamp` `MenuItemDescenderClip` `ModalConfirmLabels` `KolComponentDeepExports` `NavLinkUnderline` `ColorSwatchFieldSizing`. Also in the wave: sans-heading-04 100%→120%, the spec-table rebuild, subpath exports. 📌 Adoption is the consumers': kol-fxr (4), kol-monitor (deep imports), kol-website (underline swap). Receipts synced to each filer's outbox |
| 2026-08-12 | **PUBLISHED theme 0.36.0 · component 0.36.0** (registry-verified) — the two newest fxr tickets went 🟢 same-day: `PropertyField` → `variant="property"` on Input (affordance · mono-ch hugging value · adjacent `unit`; the existing PropertyInput molecule is a different anatomy, untouched) and `SegmentedFilledVariant` → `.kol-seg--filled` tiles + selected-only inset ring + stateless mode (`value={null}` = action strip). Showcase filled the same run: useModal demo born (custom-label confirm), Input demo gained the slotLeft paint bar + property fields, SegmentedToggle demo shows filled + stateless, Foundations gained a Utilities section with `.kol-link-underline` live. 📌 Adoption is kol-fxr's: delete SegBar.jsx + the AxisField/MetricInput hacks, bump ≥0.36.0 |
| 2026-08-12 | **Queue emptied — EditorInspectorIconBatch closed with receipt returned** → **icons 0.15.0** (registry-verified). All 10 glyphs approved frame-by-frame on the visual-reference proposal page and minted into existing groups: layout +5, tools +3, typography +2. Inventory regenerated **194 · 27**. 📌 Remainder is the editor's: bump ≥0.15.0, swap the interim stand-ins for the real names |
| 2026-08-12 | **Queue emptied again — both late tickets closed same-day** → **theme 0.38.0 · framework 0.19.0** (registry-verified). `SegmentedFilledStateFix` — the corrected state law: selected = dark tile + bright glyph, unselected quiet, ring deleted; cell rest ink `--kol-fg-meta`→`--kol-oq-48` (opaque-icons law). `ComponentTailwindSourceTrap` — ThemeToggle's roll minted as DS chrome (`.kol-roll*` in kol-theme), `@source` contract documented in the kol-theme README; the kol-animations.css slice deliberately deferred. 📌 Remainders: kol-fxr drops its `text-oq-48` force-wraps; kol-monitor bumps and gets its roll back |
| 2026-08-12 | **Queue emptied — ShellNavItemInk closed** → **theme 0.39.0** (registry-verified). Rest ink fg-64→fg-80; `.is-active` += weight 300; the two-rails "inconsistency" root-caused by live probe: the `--own` tags block's weight 500 was the medium side — harmonized to 300, ONE emphasis step across the rail. 📌 Remainder is kol-website's: bump theme ≥0.39.0 |
| 2026-08-12 | **Queue emptied — both website-session tickets closed** → **theme 0.40.0 · component 0.38.0** (registry-verified). `HeadingTwoNarrow` — the narrow/compact boundary moved down one rung (01+02 narrow, 03–05 compact); vault table + showcase prose synced. `CardFeatureHoverZoom` — visual zooms 1.03 on card hover, chrome in kol-theme, reduced-motion safe. 📌 Remainders are kol-website's: bump, adopt the DS card pair on Home, retire the local fork |
| 2026-08-14 | **Two filed from kol-website — both re-files of a dead batch ledger, not new findings.** kol-website is retiring `docs/DS-CHANGES-2.0.md`, a 2026-07-15 hand-the-table-over ledger that predates the lobby and was never handed over. Seven of its ten items had already been resolved and struck; the two live ones came here through the current channel. `DashboardIconCoverage` (was item 2.9) — 7 dashboard icon names still unresolved after icons 0.6.1→0.15.0, live-confirmed firing during a headless walk 2026-08-12. `PublishWaveChangelog` (was item 2.10) — the changelog ask, still unhonoured, and now the reason kol-website's agent context carries "do not report 'checked the changelog' as diligence". Item 2.8 (type-token names vs registered faces) verified CLOSED before retirement: kol-theme now ships its own `@font-face` block with spaced names matching its token defaults exactly, and the consumer shim is gone |
| 2026-08-14 | **FontFolderNaming filed from kol-website.** The consumer converged `public/fonts/` onto lowercase-kebab (jetbrains-mono · tg-foundry · tg-typefaces) and hit the one folder it cannot rename: `Right-Grotesk/`, whose 98 URLs kol-theme owns. Ask: rename in a coordinated wave + drop the never-rendered `Right Grotesk Text` @font-face pair (verified: no token, no rule, no code sets the family — the browser never downloads it; the consumer already retired its two files) |
| 2026-08-15 | **`llm-rules-bulletin-in-scaffold` came back 🟢 — the outbox is empty.** `## 📢 BULLETIN` now exists in the dotfiles scaffold template, under the startup protocol, **empty**: the section IS the deliverable, since the file is symlinked into every repo root and one entry there reaches every agent at init. No announcement backfilled — entries get written when there is something to announce. The ticket's second ask (move this repo onto the symlink) did not ship and is not tracked as a lobby item: this repo still holds a regular `LLM_RULES.md` with its own BULLETIN, which is a setup choice, not outstanding work |
| 2026-08-14 | **Queue emptied — all four same-day filings closed in one wave** ("do all of them" + "publish everything") → **theme 0.41.0 · icons 0.16.0 · framework 0.20.0 · foundry 0.5.5 · kol-shell 0.1.0**, all registry-verified, every publish with a changelog entry per the new discipline. `FontFolderNaming` → 98 srcs lowercased + Text pair dropped, **BREAKING**-flagged; this repo's own `public/fonts/` renamed in the same pass (`Right-Grotesk-Text/` statics → `_tmp/`; `Right-Grotesk-Mono/` left as found — zero refs, not the ticket's call). `DashboardIconCoverage` → ruling "v1 grows": 4 retired drawings promoted under plain names (`crown` `trophy` `stopwatch` `users`, approved on the visual-reference proposal page), 3 names mapped not minted (`dashboard-bookmark`→`bookmark` · `dashboard-roadmap`→`roadmap` · `trending`→`trending-up`); inventory **198 · 27**. `PublishWaveChangelog` → the rule written into the release playbook §0 (entry per package per wave, BREAKING flags), all 13 stale/empty changelogs resumed with gap notes — resumed, not truncated: the old entries are a true record. `AppShellSet` → **`@kolkrabbi/kol-shell` born** (eleventh UI package, sibling of kol-framework): all 9 pieces recreated from the monitor/mirror twins with the shipped drift fixed (dead import, light-theme borderTop, casing transforms, z-70 stacking), the 2026-08-12 active-wash ruling carried natively scoped to `.kol-shell-rail`, chrome in `kol-components-shell.css`, framework grew `./src/*` subpath exports so both consumers' pnpm patch dies. 19 gates clean. 📌 All remainders are the consumers': kol-website (fonts folder rename · icon name swaps · bump), kol-monitor + kol-mirror (adopt the set, retire local copies + patch) |
| 2026-08-15 | **WorkshopExhibitSystem filed from kol-website.** The workshop shell is shared (both repos on `@kolkrabbi/kol-workshop` since the 08-08 reconnection) but exhibit sections are still hand-built app-side — the dashboard section carries its own landing/showcase/prose scaffold + two chrome pieces (DesCard, WorkshopSidebarContent) the shell doesn't ship. Ask: the section system moves into kol-workshop so the next section is content-only |
| 2026-08-15 | **`media-library-non-av-blindness` filed from kol-r2b2.** The organism this repo built to consolidate four forks discards every object that isn't an image or a video — measured against the live 3443-object bucket. Filed with the kind/variant/segment/system rules kol-r2b2 wrote and tested against the same data, so the fix arrives with its evidence |
| 2026-08-15 | **The anatomy pair filed from kol-website — the consolidation pass the audits never raised.** Every audit compared files to themselves; none compared pages to each other, so six heroes and five split-sections shipped the same anatomy hand-built. `SectionSplit` (the section anatomy, media+text+flip) and `ArticleCard` (one listing card, three contexts) are the round-1 briefs; the website's components count targets 79 → ~55 on adoption. A media-tile card (thumb + meta + action row) is queued as round 2 |
| 2026-08-15 | **Two closed in one publish — `ComponentSideEffectsField` + `media-library-non-av-blindness`** → **component 0.39.0** (registry-verified). `sideEffects: false` declared only after verifying it true (zero CSS imports in `src/`, theme + framework already carry it) — kol-monitor's pnpm patch retires. MediaLibrary's `accept` now **widens instead of gating**: `'all'` means everything, one kind filters, and `['image','video']` is the new allow-list — flagged **BREAKING**, it is a default flip. The four measured rules landed with it (extension-first kind detection · resolution sets collapsed, thumb on the smallest and download on the largest · HLS segments folded per folder · system files hidden **with the count** at the path bar), plus real poster pairing so `preload="none"` paints a frame. Three unnamed siblings of the same defect fixed in the pass: kind glyphs for non-paintable files, a **derived** Kind filter group, and a lightbox indexed against image/video so a narrowed grid stops opening the wrong file. 19 gates clean. **The audio component was NOT built** — audio now arrives and is pickable, but minting a component is a taxonomy ruling, not this ticket's to authorise. 📌 Remainder is kol-monitor's: bump ≥0.39.0, drop the patch. kol-r2b2 has no outbox, so nothing was filed back there |
| 2026-08-15 | **`WorkshopExhibitSystem` closed** → **kol-workshop 0.22.0** (registry-verified). The exhibit-section scaffold moved into the package: `ExhibitOverview` · `ExhibitPage` · `ExhibitSidebar` + `useExhibitToc` · `ExhibitCard` · `ExhibitLinkCard`. The brief's three page archetypes shipped as TWO components — the showcase page and the prose companion were never structurally different (both a list of PageSections; only the body varies), so a section carrying `specimens` renders the demo grid and one carrying `children` renders anything, `prose: true` for the measure. Drift fixed on recreation rather than reproduced: the rail block's hand-rolled section (two box classes stacked, inline y-spacing, a chevron at L1) now comes from `RailSection`, so it cannot drift from either rail; the landing card lost its `uppercase`; `useExhibitToc` keys on prop CONTENT, closing the shell-state loop the copied block left open. 19 gates clean, all six registered in the roster. ⚠️ **Ships unexercised** — no showcase surface, no adopting app, the kol-shell 0.1.0 caveat again. 📌 Remainder is kol-website's: re-declare the dashboard section as content and retire DesCard + WorkshopSidebarContent + OverviewCard |
| 2026-08-15 | **The two reconciliation briefs filed from kol-website** — the diverged twins its adopt-diff pass refused to blind-swap: `ArticleHeaderReconcile` (167v87L) and `FeaturedCarouselReconcile` (458-line diff). Same session consumed **kol-workshop 0.22.0** end-to-end: dashboard section re-declared on ExhibitOverview/ExhibitPage, all four DesCard/OverviewCard/WorkshopSidebarContent consumers swapped to the package exports, three chrome files retired. Also verified consumer-side: MediaCard + MediaRow already cover the queued media-tile round — no brief needed, adoption census instead |
| 2026-08-15 | **`ArticleHeaderReconcile` closed** → **kol-content 0.5.0** + **kol-component 0.40.0** (registry-verified). Verdict: **this package was already canon** — the 220-line diff was app coupling kol-content shed on purpose (four Sanity URL builders, the `reveal` utility + inline delays, `kol-helper-14 uppercase` against the no-text-transform rule, `kol-display-lg` over the prose role class), not a feature gap; "ship the superset" would have re-imported exactly what the extraction removed. Three real gaps crossed: **`authorImage`** — root-caused to the atom, since the twin hand-rolled an `<img>` only because **`Avatar` did initials only**, so Avatar gained `src`/`alt` (initial as fallback on a broken URL) and `AuthorLine` gained `image`, giving every byline and card photos; **`heroImageSrcSet`/`heroImageSizes`** passed through to DS Image; **`tagSize`**, replacing the twin's trick of rendering the whole tag row TWICE behind `lg:hidden`/`hidden lg:flex`. The audit's index-as-key was **ours too** and is fixed here. 19 gates clean. 📌 Remainder is kol-website's: bump both, adopt, retire the local copy |
| 2026-08-15 | **`FeaturedCarouselReconcile` closed** → **component 0.41.0** + **framework 0.20.1** (registry-verified). **Never a fork — two engines.** kol-website ran framer-motion `AnimatePresence` over an index (so: **no drag at all**), the package runs embla; canon went to the engine here, and with it the `{ media }` descriptor, OverlayGlassPanel and the progress ring. Five capabilities crossed back: `children` (static overlay that does not travel with the slides) · `fullWidth` · `rounded` · `showTitle/showDescription/showCta` · `subtitle`, plus `navPosition='header'` answering the brief's nav question. **CarouselNavigation superseded, not folded** — reading it explained the fork (it wanted real chevron icons; the package's buttons were the literal text glyphs `‹` `›`, in a DS that ships a chevron set), and that markup was hand-typed at THREE sites, so it became **`EmblaNav`**, one exported component now used by both carousels; MediaViewer keeps its own chips (inverse-tier, absolutely positioned — a different control). Not carried: the foundry title coupling (size ramp keyed on the literal strings 'Málrómur'/'Tröllatunga'), the deleted `kol-label-mono-xs`, and the hidden eager-preload-every-image block. 19 gates clean. 📌 Remainder is kol-website's: bump, adopt, retire FeaturedCarousel.jsx + CarouselNavigation.jsx |
| 2026-08-15 | **`SectionSplit` closed — and NO SectionSplit was built.** → **component 0.42.0** + **theme 0.42.0** (registry-verified). The anatomy the brief describes already ships as **`FeatureSplit`**; a twin beside it would have made the eleven hand-built sections into twelve implementations, which is what the brief exists to end. Three of its seven asks were already met (polymorphic media slot · the kicker/heading/body/actions contract · the text-only /CONNECT case, since `media` is optional) and three were added: **`flip`** — via `order`, NOT `flex-row-reverse`, because the grid is one column below 901px where DOM order decides the stack; **`titleSize`**; **`mediaAspect` + `mediaHover`**. The brief's named core design question (how per-site type classes thread the text contract) is ruled: **they don't** — a consumer picks a ROLE and the component emits ONE type class, since stacking `kol-sans-heading-01` on `.kol-feature-split-pull` is two equal-specificity rules decided by sheet order (ARCHITECTURE §5 · the 2026-07-30 law). HomeFoundry's silently-broken hover closed at its cause: `mediaHover` is DS CSS on CardFeatureItem's exact 1.03/300ms/reduced-motion numbers. 19 gates clean. 📌 Remainder is kol-website's: bump, re-declare the sections on FeatureSplit, retire FoundryFeatureSection |
| 2026-08-15 | **`ArticleCard` closed — most of it was already built.** → **kol-content 0.6.0** (registry-verified). `ArticleCard` already shipped `default`/`hero`/`mini`, having collapsed three duplicate components at extraction — the same consolidation this brief asks for; the consumer's `variant="grid"` maps to `size="hero"` + `showHeader={false}`, and the brief's `category` is this component's `kicker`. Two real gaps closed: **`size="readmore"`** — flagged, because it had **no reference implementation anywhere in the estate**, so it is the family's own idiom (mini's row · `label` lead-in · a trailing arrow on hero's 300ms hover clock) and its first real use should be reviewed against a design; and **`titleClassName`/`excerptClassName`/`kickerClassName`**, which **REPLACE** the type class rather than stack — WorkListItem's established seam and the 2026-07-30 law. The index-key defect the ArticleHeader audit raised was true here too, fixed. 19 gates clean. 📌 Remainder is kol-website's: bump, re-declare StackLatest + Stack on the variants, retire ArticleCardHero + ArticleCardMini |
| 2026-08-15 | **`AudioPlayer` closed — the queue is at ZERO.** → **component 0.43.0** (registry-verified). The taxonomy call the MediaLibrary ticket declined to make, made: audio was the one media kind the DS had nothing for, and it lands as an **atom beside HlsVideo**. Built to the entry's restraint — one native `<audio>` + optional label, no waveform, no scrubber, no playlist, and **no `compact` variant** (the entry said not to build it speculatively); `controls` is not a prop, since a player without controls is a different component. The three call-site values (`p-8` · `w-[420px]` · centring) dropped as instructed. **HlsVideo's hardening deliberately did NOT cross** — same tier and shape, inverted intent — and that contrast is now written into both headers so the next editor meets it first. No CSS half: the native strip is UA-painted, and a branded transport would be a separate `AudioTransport` molecule on this atom. 19 gates clean. 📌 Remainder is kol-r2b2's: bump ≥0.43.0 and swap `KindPreview.jsx`'s raw element |
| 2026-08-15 | **`ArticleCardSizeSpec` filed to kol-website** — the user's ruling after catching `readmore`'s invented geometry (0.6.0, corrected 0.6.1): there SHOULD be a standard shortlist of card sizes, and the consumer that renders the cards in every context defines it — thumbnails, type, clamps, heights, breakpoint behaviour per preset. The DS conforms `ArticleCard` on the spec's return; ruling geometry it has no context for is the failure the ticket ends |
| 2026-08-15 | **`ListingCardSpec` filed from kol-website — the ArticleCardSizeSpec return.** The consumer answered everything its ticket asked: geometry table (hero/default/mini × thumbnail/type/clamps), 120×120 confirmed as a ruling, clamps 2/3/2 confirmed, breakpoint swaps ruled consumer-side, plus the two appended rulings — wide scope (THE listing card) and the `ListingCard` rename with aliases. `readmore` is dropped as a preset: never rendered, invented twice, and a read-more band is a context (mini + `label`), not a size. The outbox receipt synced 🟢 |
| 2026-08-15 | **The kol-shell rail batch** → **icons 0.17.0** + **theme 0.42.1** (both registry-verified). Theme: `@source "../kol-shell/src"` added to `kol-sources.css` — kol-shell 0.1.0 had shipped without its manifest line, the exact silent footgun the manifest exists to prevent; adopters saw unstyled shell chrome. Icons: `rack` minted into `device/` from kol-monitor's local shelf under its plain name (inventory **199 · 27**); `nav-library`→`library` and `nav-settings`→`settings-01` map to shipped drawings, not minted. 📌 Remainders are the adopters': bump theme ≥0.42.1 (delete the local `@source` workaround) + icons ≥0.17.0 (swap the three nav names, delete the local Icon shelf) |
| 2026-08-15 | **`KolSourcesPnpmResolution` closed** → **theme 0.42.2** (registry-verified). The filing's finding held: under pnpm EVERY `@source` line resolved to nothing — Tailwind realpaths the manifest into the `.pnpm` store, where only kol-theme's own deps are siblings, so the docstring's flat-tree sibling claim was the unexamined assumption. Fix: **twin lines per package** — the flat path (npm/yarn) plus a five-up store walk to the consumer's `node_modules/@kolkrabbi/*`, **measured against kol-mirror's live store**, not derived; a missing path matches nothing, so one of each pair is inert on any layout. Dependency-edge and per-package-CSS directions rejected (cycles; ×12 import contract). Docstring corrected. 📌 Remainder is the pnpm consumers': bump ≥0.42.2, kol-mirror deletes its local `@source` workaround — and gets the other eleven packages' utilities back |
| 2026-08-15 | **`ListingCardSpec` closed — the ArticleCardSizeSpec answer, executed** → **kol-content 0.7.0** (registry-verified). The consumer's three rulings landed: scope **WIDE** + **renamed `ListingCard`** (`ArticleCard` a barrel alias until next major, nothing breaks); **`readmore` removed** one day after it shipped ("read more" is a context, not a size — an end-of-article band renders `mini` with a `label` lead-in); geometry conformed to the table — two real deltas (hero excerpt clamp 3→2 · default excerpt gained clamp 3), the rest confirmed correct. **One stated deviation: `WorkCard` is NOT aliased** — its prop contract is a different anatomy and aliasing it breaks every `/work` consumer; the name convergence is recorded for the next major. 19 gates clean. 📌 Remainder is kol-website's: bump ≥0.7.0, adopt `ListingCard`, retire its two local cards |
| 2026-08-15 | **`ShellHeaderFilterRefinements` closed** → **kol-shell@0.1.3**. Second QA round, three rulings. A filter group is a COLUMN — label on top, values beneath — not label-left/values-right on one line. The group label wears the full strip idiom (`kol-helper-12`, 1px tracking). And the filter line carries exactly TWO ink states, the strip's own: active `text-fg-96`, rest `text-fg-32` (hover `text-fg-48`) — the label joins the rest ink instead of its old `text-fg-48`, and `Clear all` drops to the same recipe, so nothing on that line holds a third opacity. The block is `items-start`, pinning the always-visible LIST/GRID strip to the label row with values hanging beneath. 20 gates clean. |
| 2026-08-15 | **`ShellHeaderFilterRefinements` closed** → **kol-shell@0.2.0**. The ping-pong ended with seams, not a fourth opinion. Three publishes went into re-deciding how a filter value and a group label render — chips, bare strip items, outlined pills — none of which is a design-system question. `ContentFilters` gained `renderFilterValue(value, isActive, toggle)` and `labelClassName`; the label seam REPLACES rather than stacks, per the 2026-07-30 equal-specificity law and `ListingCard`'s `titleClassName` precedent. Defaults reproduce 0.1.3 exactly so kol-mirror does not move. Minor bump — new public API. |
| 2026-08-15 | **`ContentFiltersForkRetirement` closed** → **kol-component@0.44.2 + kol-shell@0.3.0**. The fork is retired and the organism is canon. kol-shell 0.1.0 recreated a component that had shipped in kol-component since 2026-08-01; four publishes on 2026-08-15 tuned the duplicate. Fixed at the real organism: the filter value is a `Tag` again with its own `active` prop and `onClick` (the invalid `variant="default"` silently rendered the FILLED chip; `secondary` is the outlined one), the layout strip moved into one row below the divider — right-aligned, always visible, `items-start` pinning it to the label row — and `iconComponent` crossed over as the fork's one genuine addition. Ink read off kol-monitor's CALL SITES, not the fork's defaults, which is what I got wrong twice: category label `text-fg-96`, value rest `text-fg-48`, value selected `text-fg-96`. kol-shell's ContentFilters + the `renderFilterValue`/`labelClassName` seams are removed (BREAKING); `TabStrip` stays for SettingsScaffold's real tabs, single-select only. Quarantined in `_tmp/2026-08-15-kol-shell-contentfilters-fork/`, not deleted. 20 gates clean. |
| 2026-08-15 | **`ContentFiltersForkRetirement` closed** → **kol-component@0.45.0**. Three regressions from monitor's live QA, fixed by diffing monitor's ORIGINAL (_tmp/2026-08-15-shell-adoption/) rather than reasoning from a description. Group label now uppercases IN the component (consumers pass natural case; the label is chrome with no authoring site, same justification .kol-tag already carries for values). RECENT/SAVED is the same strip as LIST/GRID — inline spans, kol-helper-14, uppercase + 1px tracking, fg-96 active / fg-32 rest — NOT ViewToggle; the original imports ViewToggle and never renders it for this, so the filled-chip reading of the 2026-07-28 ruling is replaced on this surface. Filter/search icons back to size 16. 20 gates clean. |
| 2026-08-15 | **`InspectorSectionRhythm` closed** → **kol-component@0.46.0 + kol-theme@0.43.0**. `Section` gained a `divided` prop; the hairline is `.kol-section--divided + .kol-section--divided` (border-top --kol-fg-08, padding-top 20px). The rule sits on the ADJACENT PAIR, so the first section in a stack never carries a stray top border and no consumer needs :not(:first-child) — that pair selector is exactly what cannot be expressed as a utility class, which is why every consumer had to invent a hook class. The ticket's second ask, `density`, was deliberately NOT built: kol-fxr's local override is `gap: 0.5rem`, which is exactly Section's shipped `gap-2` — a no-op, so there is no second density to name until a real one appears. |
| 2026-08-15 | **`ThreeColumnEditorShell` closed** → **kol-framework@0.21.0 + kol-theme@0.43.0**. All three gaps closed. (1) THE GESTURE: `useDragResize(ref, { token, side })` — every name it touches (custom properties, data-attributes, both localStorage keys) now derives from one token via buildNames(), and side:'right' inverts the pointer sign AND the arrow keys so a right-hand handle drags the way it faces. Defaults reproduce the 0.17.0 names byte-for-byte, so SideNav's one-argument call is untouched; scripts/check-dragresize-names.mjs asserts that against a hand-transcribed copy of the old contract, because drift there is SILENT — the rail would read variables nobody writes and persist under keys nobody reads, with no error. (2) THE LAYOUT: --kol-rail-{w,w-collapsed,snap,step,snap-default} + .kol-brand-layout[data-rail="true"], each rail's track behind a private var so the two collapse independently off one rule each rather than a 2x2 of combined states. Deliberately NOT behind the TOC's min-width:1280px gate — an editor's inspector is the surface, not an enhancement that may drop away. (3) THE FOCUS LEAF: .kol-sidenav-link had no treatment at all, only .is-active and its dot, so it fell through to the browser default — the 'weird highlight bug'. Now 1px INSET matching its sibling .shell-nav-item, not .kol-btn's offset ring which blooms outside a dense nav row. |
| 2026-08-15 | **`GatedEmptyState` closed** → **kol-component@0.46.0 + kol-theme@0.43.0**. `usePlaceholders()` + `<EmptyState gated>`. One concept, one gate, default OFF — the ticket's own diagnosis was that inventing 'helper text' vs 'empty state' vs 'hint' as three ideas is what let the prose creep back, since each had a home and none had an off switch. The suppression is CSS (.kol-placeholder in kol-utilities.css), NOT a render branch, so it covers a consumer's OWN prose the moment the class goes on it — not just EmptyState. Written as :root:not([data-kol-placeholders]) so the element keeps whatever display it had rather than being forced back to block on reveal. The hook persists to localStorage and syncs through a module-level subscriber set, because a settings checkbox and a keybind are usually two components and per-component state would let them disagree about a preference there is only one of. `gated` is OPT-IN, not the default: flipping it would silently blank every surface already shipping an EmptyState. THE DS OWNS THE PREFERENCE, THE CONSUMER OWNS THE KEYBIND — a design system that grabs a global key collides with every app that already used it, and your own H is already toggle-visibility in the editor keymap. |
| 2026-08-15 | **`FocusRingsInConsumers` closed** → **kol-theme@0.43.0**. `--kol-focus-ring-quiet` minted; the pair is now a real off switch. TWO PREMISES IN THE TICKET WERE WRONG, and naming them matters more than the fix: (1) the showcase does NOT suppress focus rings — showcase/src/index.css is its only stylesheet and contains zero outline and zero :focus-visible rules, so there was never anything to port; (2) there is no 'outline: none !important' block in kol-editor.css — the real rule is narrow and unflagged, plain outline:none on input/textarea :focus-visible inside .kol-editor-shell. THE ACTUAL DEFECT: --kol-focus-ring existed but only half the rules read it. .kol-btn / .toggle-switch / .focus-visible:ring-focus read the token; the nav rails and .kol-seg-cell hardcoded their colours. So setting it transparent killed button rings and kept rail rings — a half-working opt-out, which is exactly why a consumer ended up writing local CSS. Now --kol-focus-ring is the loud 2px ring and --kol-focus-ring-quiet the 1px inset one; EVERY focus rule in the theme reads one of the two, no hardcoded focus colours remain anywhere. Defaults reproduce today's values, so nothing moved visually. NB both rings are white — --kol-accent-primary resolves to --kol-surface-on-primary (#fafafa dark / #121215 light) — the two differ in weight, not hue. .kol-seg-cell was rewired on the user's explicit call and its ring dims full-white to 32%; that one is a visual change, deliberate, his. |
| 2026-08-15 | **`ShortcutsOverlaySections` filed from kol-fxr.** kol-shell's `ShortcutsOverlay` takes one flat `[{label, keys}]` array; the consumer's keymap is sectioned and `shortcutsBySection()` already emits `[{section, items}]`, so adoption would drop the grouping and a 99-line local overlay stays alive instead. **Filed because the agent had first *decided* not to adopt** — a consumer/DS gap is not an agent's call to close locally; it goes to the lobby and comes back as a variant, where every other consumer gets it too. Flagged alongside, not asked for: `keymap.js` is not view-aware (`LabsParams.jsx:26` binds R/Shift+R privately, so labs and the editor disagree about R with nothing declaring it) — kol-fxr's to fix |
| 2026-08-15 | **`ShortcutsOverlaySections` closed** → **kol-shell@0.4.0**. `ShortcutsOverlay` takes a SECTIONED array as well as the flat one, detected on shape: `[{section, items:[{label,keys}]}]` groups with headings, `[{label, keys}]` renders EXACTLY as before so no existing caller moves. ONE GRID, not nested ones — the headings span both columns (`gridColumn: 1 / -1`) so every keys cell stays on one axis; a grid per section would let each group compute its own column width and the keys would stagger down the panel. `keys` remains a display string and is still never bound: the component shows a keymap, it does not own one, so formatting a combo stays next to the binding. Adopted in kol-fxr the same run: the 99-line local overlay is now a ~30-line ADAPTER that owns only the open state and the `kol:show-shortcuts` window-event channel, mapping `shortcutsBySection()` into the DS shape; its Esc listener was dropped because the DS panel owns Esc and a second one would call onClose twice. Verified in a browser, not just compiled: S opens it, all six sections render (Edit · Selection · Layer · Tools · View · Color), the wrapper computes to `display: contents`, the heading resolves to `1 / -1` and its box matches the grid's full content width. 20 gates clean, registry-verified. NOT folded in: `LabsShortcuts.jsx` is the expression quick-doc, not a duplicate overlay. Also fixed by the swap: the local file's section titles used `uppercase tracking-widest`, against the no-text-transform law. |
| 2026-08-26 | **`SideNavMobilePosition` closed** → **kol-framework@0.23.0**. The aside box moved into `.kol-sidenav` (kol-framework.css) — `position: sticky; top: 0; align-self: start; height: 100dvh; display: flex; flex-direction: column; z-index: var(--kol-z-sticky)` — and the seven utilities left the className; only the colour utilities ride the element now. Same law as 0.15.2 hop fix, one element up: a utility outranks every rule in this layered sheet, so the 767px drawer block `position: fixed` could never win while `sticky` sat on the element. Declared in the rule, the drawer block beats it by source order. Verified by replaying the exact change on brand production DOM (stopgap rule deleted, utilities stripped, the rule + drawer block injected in file order): 393 → aside `fixed` at x=-280, content top 0; 1280 → `sticky`, 256 wide, holds at top through a 600px page scroll. 20 gates clean. |
| 2026-08-26 | **`TableMobileScroll` closed** → **kol-theme@0.50.2**. Already built — the ticket premise did not hold. `Table` renders `.kol-table-wrapper { overflow-x: auto }` around every `.kol-table` and has since the component first shipped. Measured on brand production `/assets` at 393: four wrappers, each 351 wide with 668–753 of content, `overflow-x: auto`, `scrollLeft` moves on all four, no ancestor wider than the viewport. The audit could not see it because the wrapper hides its scrollbar (`scrollbar-width: none`, the reference table chrome) and headless Chromium cannot drag — the same limit the audit downgraded its own #2 (embla dragFree) for. Nothing to build. Not done, deliberately: the optional right-edge fade — the 2026-07-28 ruling bans scroll-edge paint, and the wrapper cover gradients were deleted for it on 2026-08-09. Whether a hidden-scrollbar scroll container should carry any affordance on touch is a design call, put to the user beside the MobileTouchFloor ruling — not a lobby item. |
| 2026-08-26 | **`CodeBlockMobileOverflow` closed** → **kol-component@0.68.1**. Two defects, one measured root cause each, shipped as kol-component 0.68.1 + kol-theme 0.50.2. (1) The block did not wrap: oneDark `code[class*="language-"]` carries `whiteSpace: pre` and react-syntax-highlighter spreads the theme code style AFTER its own `wrapLongLines` value — so `.kol-codeblock` pre-wrap, `customStyle` pre-wrap and `wrapLongLines` all lost, the `<code>` computed `pre`, and every long line scrolled inside the frame (production `/stack/vcap` at 393: block 359 wide, content 465–759). The override now states `pre-wrap` where it is decided; the code surface law (06-code-surface.md: Block wraps) is what the code does. Desktop changes with it — a line longer than the block wraps instead of scrolling, the ruled behaviour. (2) The copy control overlapped line 1 only on CHIPLESS blocks (measured: with a chip the control sits in the chip row, y 2148–2180 against line 1 at 2183; without one, 3729–3761 against 3735–3756). Each line is stamped `.kol-codeblock-line`, and `.kol-codeblock:not(:has(.kol-codeblock-filename)) .kol-codeblock-line:first-child` reserves `calc(2rem + var(--kol-spacing-3))` — the control box plus its inset. Verified in the showcase at 393: `<code>` computes pre-wrap, scrollWidth = clientWidth on every block, a chip removed live gives line 1 44px of end padding and its text ends 12px short of the control. Not done: `overflow-x: auto` on the pre — the block already has it, and the law says wrap. 20 gates clean. |
| 2026-08-26 | **`WorkshopShellMobile` closed** → **kol-framework@0.23.0**. Half DS, half consumer. The tab strip already scrolls — `.kol-shell-header-tabs { overflow-x: auto }`, measured on production at 393: 345 wide, 613 of content, `scrollLeft` moves — but its scrollbar is hidden and the ACTIVE tab sat off-screen (`Dashboard` at x=347–445 in a strip ending at 369): the page you were on was the one tab you could not see. ShellHeader now scrolls the active tab to the strip start edge, before paint, only when it is out of view; the strip snaps to tab starts on a flick (`scroll-snap-type: x proximity` — not mandatory, which would refuse the last tabs). Verified in the showcase at a width where two tabs overflow: fresh load on the second tab → scrollLeft 44, tab fully in the strip; fresh load on the first → 0, untouched; route change → 44. No edge fade: the 2026-07-28 ruling against scroll-edge paint stands. The two-up cards are NOT DS chrome: `div.flex-1.space-y-4` and `div.flex-1.dash-card` are kol-website own `flex flex-row gap-6 items-start` rows in `apps/web/src/routes/workshop/DashboardComponents.jsx` (lines 182, 202, 220, 555, 579); `.dash-grid` in kol-dashboards already stacks by container query and is not used there. That half returns as the consumer remainder; kol-dashboards and kol-workshop have nothing to bump. 20 gates clean. |
| 2026-08-26 | **`MobileTouchFloor` closed** → **kol-theme@0.51.0**. Ruled, and the ruling built. NO type floor: 10px chrome (`kol-helper-10` / `kol-mono-10`, the 10.4px table header) is the chrome voice at every width — a mobile step would reflow every rail and table for a class of text that is labels, not copy. HIT FLOOR 24px (WCAG 2.5.8 AA), reached without moving the drawn size: the size scale (26/32/40) and `.kol-seg-cell` (24) already clear it; the two controls under it are lifted in kol-theme 0.51.0 — the bare `ToggleSwitch` (14px: a 12px track in a 1px border) takes a `::before` extent 24px tall centred on the button, and the `Slider` range input (2px — the track was the whole target) is 24px tall with the 2px track centred inside it by the UA, thumb unchanged, row unchanged. Verified in the showcase at 393: the extent computes 24px and hit-tests 2px beyond a labelled 20px box on both sides (`elementFromPoint` lands on the button) and 5px beyond the 14px label-less box; the slider input measures 24 in a 24 row and a 3x render shows track and thumb centred. Law written into `03-components/05-control-chrome.md` § Touch floor. Not the DS: a consumer own 16px table buttons and 12px links — same rule there, extend the hit box, never the glyph. |
| 2026-08-26 | **`nested-theme-scope` filed from kol-studio.** kol-studio moved off its June inlined DS fork onto `@kolkrabbi/{kol-theme 0.51.0, kol-component 0.68.1, kol-framework 0.23.0, kol-icons 0.18.0}` and the one DS-worthy divergence in the fork came back as a ticket: light surface tokens and the derived `--kol-fg-*` ramp + roles are `:root`-only, so `data-theme="light"` on a subtree (kit preview, A4 sheets) flips the surface but not the ink. Three selector-list edits in kol-base-tokens.css + kol-opacity.css; the consumer carries a verbatim override until the fix ships. Evidence: the 2026-06-23 fork write-up in `_assets/2026-08-26-nested-theme-scope/`. |
| 2026-08-26 | **`NestedDsDependencies` closed** → **kol-workshop@0.23.0 · kol-chess@0.7.0 · kol-content@0.9.0 · kol-foundry@0.6.0 · kol-store@0.2.0**. The DS tier is a peer in all five. kol-component · kol-theme · kol-icons (+ kol-framework in workshop) moved from `dependencies` — a 0.x caret, so pnpm nested a private stale copy under each package (component 0.39.0 under workshop, 0.8.0 under store) — to `peerDependencies` with `>=` floors: component >=0.68.1 · framework >=0.23.0 · theme >=0.51.0 · icons >=0.18.0, the versions the ticket's 54-name import walk was made against; `workspace:^` in devDependencies for the showcase — the kol-dashboards / kol-shell shape. Registry-verified on all five: `peerDependencies` carries the tier, `dependencies` carries none of it (workshop keeps kol-brand ^0.1.2, not a DS-tier package). The floors are honest, not lenient — a consumer below them gets a peer warning until it bumps. Not touched, same class, outside the five: kol-framework → component/icons, kol-component → icons and kol-styleguide → component/foundry/icons/theme still declare `dependencies`; framework moves with its next publish if ruled. Topology doc §Dependencies now states the rule; every changelog flags it BREAKING. |
| 2026-08-26 | **`sidenav-nested-groups` filed from kol-studio.** On adopting kol-framework 0.23.0 the Résumé category's two sub-groups (PDF, Layouts) and the six routes under them vanished from the rail: `SideNav` reads `cat.children?.filter((c) => c.to)`, so a `{ label, children }` node is dropped silently. The cause is the 2026-08-01 "TWO LEVELS ONLY" ruling in the header — made about the brand app's scroll-spied anchors, inscribed as a package-wide law. User, verbatim: *"sometimes Im talking about a specific thing and it gets applied as a literal fit-all rule, which isnt the case. its very hard to work in such uncertainty."* Ask: render nested route groups as non-routing headers with indented leaves; tree shape is the opt-in, no prop; anchors stay dead. Reference walker + screenshot in `_assets/2026-08-26-sidenav-nested-groups/`. kol-studio ships flattened `PDF · …` leaves as the stopgap. |
| 2026-08-26 | **`nested-theme-scope` closed** → **kol-theme@0.52.0**. Every token derived from a themed surface token is now declared on the theme selectors — `:root, :is([data-theme="light"], .light), :is([data-theme="dark"], .dark)` — not `:root` alone. Four files, selector lists only, no value changes: kol-base-tokens.css (the light surface block on `:root, :is([data-theme="light"], .light)`), kol-opacity.css (the fg ramp, all three tiers, and the eight roles), kol-opaque.css (the oq ramp — same freeze, not in the ticket, same fix), kol-color.css (`--kol-border-default` · `--kol-border-focus` · `--kol-focus-ring-quiet` moved out of the `:root` block into a themed one — the ticket's follow-up). The accent family stays `:root`-only on purpose: kol-brand-color.css rebinds it at `:root`, and a themed re-declaration would hand a branded app's nested pane the neutral ink accent instead of the brand. Measured headless against the raw theme before and after: before, a light pane in a dark root flipped NOTHING — surface included; the ticket's "surface flips" was the consumer override at work. After: dark root → light pane renders `.text-emphasis` #121215 on #fafafa, `.text-body` rgba(18,18,21,.64), `oq-64` and `--kol-border-default` re-resolved; light root → dark pane the reverse; `.light` as a class flips the same way. Root-level theming and the prefers-color-scheme mirror untouched. Known and unchanged: `.bg-surface-inverse` re-declares fg-01…96 but not fg-72 or the roles, so `.text-body` inside an inverse panel is still root ink. Law written into 01-tokens.md. |
| 2026-08-26 | **`sidenav-nested-groups` closed** → **kol-framework@0.24.0 · kol-theme@0.52.1**. `SideNav` renders a `{ label, children }` node inside a category as a non-routing group header (`.kol-sidenav-group kol-helper-10 text-subtle`, `text-emphasis` when a leaf beneath it is the current route) with its rows indented one `--kol-spacing-3` step, recursively; the active dot keeps its 0.875rem lead at every depth. The tree shape is the opt-in — no prop. `#anchor` leaves stay dropped. Asserted in the showcase, not by reading: the demo's flat tree renders byte-for-byte as 0.23.0 (aside outerHTML identical before and after the change); the demo now carries a `Chrome` group naming its own page — rendered lit, leaf active, group at 56px, leaves at 68px. Header re-scoped with the user's 2026-08-26 sentence quoted verbatim. Found on the way: the theme carried a dead elder `.kol-sidenav-group { padding: 4px 0 }` that outranked the framework's rule in the showcase — the showcase's `layer(components)` import lands framework chrome in a NESTED `components.components` layer, below every theme rule (pre-existing, not touched, flagged) — retired in theme 0.52.1 so one rule owns the box. The fork's `uppercase` on the group label does not return (type law). |
| 2026-08-26 | **`SettingsPanel` closed** → **kol-component@0.69.0 · kol-theme@0.53.0**. `SettingsPanel` is a kol-component organism — one anatomy (title · subtitle · intro · `Section divided` stack · footer), two presentations: `variant="drawer"` rides the DS `ShellDrawer` (right-anchored, `width` 380 default; scrim, Escape, focus trap, scroll lock, focus return, the × control) and `variant="overlay"` rides `FullscreenOverlay` (centred; it now traps focus too — Tab cycles in the sheet, focus returns to the opener). Parts: `SettingsRow` (a grid, so a long hint wraps in its own column and the control centres on the row — the alignment defect), `SettingsSwitch` = `ToggleSwitch` sm (disabled state added to the theme, it had none), `SettingsChoice` = `SegmentedToggle` sm (the source's word-button segments were the thing to fix, not copy), `SettingsChipRow` (toggle chips with counts, the `.kol-control` pattern), `SettingsFooter` (state word + ghost reset). No `text-transform`; the hand-typed `rgba(0,0,0,0.6)` scrim, the hard 380 and the bucket/profile wiring are gone. Rendered headless in the showcase: drawer flush right at 380, overlay centred at 380, focus inside on open, Tab stays inside, Escape closes both. |
| 2026-08-26 | **`MediaLibraryReconcile` closed** → **kol-component@0.69.0 · kol-theme@0.53.0**. The page variant is FileList's read-only render: breadcrumb `root / seg / seg` on top, the stats line (`N folders · N files · size · bucket: N files · size · N system files hidden`, whole-bucket figures at root or in flat), folder rows that ENTER a prefix (`folder` 18 · name · trailing `chevron-right`, hairline, `fg-04` hover; struck in flat), a bare toolbar (filter + search on DS nav Buttons | `Flat` chip · `ViewToggle` icon · vertical Divider · sort labels with `arrow-down` asc / `arrow-up` desc, click active flips), a `.kol-media-grid` at 260px tiles / gap 12, `Show N more · N remaining` paging, Copy URL + ghost `iconOnly="download"` beside the card chip. Props: `pageSize` 60 · `defaultSort` `{ by: 'date', dir: 'desc' }` · `flat`. The provider is PREFIX-SCOPED now (`prefix` / `setPrefix` / `crumbs` / `folders` / `flat` / `kinds` / `search` / `sort` / `sorted` / `shown` / `showMore` / `stats`); paging is keyed to what is listed so entering a folder resets it. Finder's disclose-in-place tree is dropped, not a variant. The picker keeps its ContentFilters chrome and foot path bar and navigates the same way. Measured live over the website bucket in the showcase: root `3 folders · 7 files · 2.6 MB · bucket: 433 files · 1.29 GB`, enter → crumb `labs-render-examples`, Name sort → glyph, Flat → 433 files / 60 tiles / `Show 60 more · 373 remaining` → 120, kind chips with counts, picker folders + path bar, no console errors. BREAKING for `useMediaLibrary()` consumers (`rows` / `expanded` / `toggleFolder` gone). |
| 2026-08-26 | **`FeaturedCarouselFullWidth` closed** → **kol-framework@0.25.0 · kol-component@0.70.0**. `fullWidth` now fills the container: `.kol-featured-carousel.is-full .kol-embla-slide { width: 100% }` in kol-framework.css (the peek `clamp(320px, 72vw, 900px)` stays the default — nothing existing moves), and `FeaturedCarousel` stamps `is-full` on the section when `fullWidth` is set. Inactive video slides no longer play: the plain `<video>` and `HlsVideo` autoplay only while their slide is the selected one and pause when it leaves the stage (`HlsVideo` gains `active`, default true). Measured headless in the showcase at 1440: peek slide 900 in a 734 container; with `is-full` the slide is 734 = the container. No console errors. |
| 2026-08-26 | **`FrameworkComponentPeer` closed** → **kol-framework@0.25.0 · kol-component@0.70.0 · kol-styleguide@0.2.0**. kol-framework declares `@kolkrabbi/kol-component` (>=0.70.0) and `kol-icons` (>=0.18.0) as `peerDependencies`; kol-component declares `kol-icons` (>=0.18.0) as a peer; kol-styleguide declares component / foundry / icons / theme as peers — all with `workspace:^` in devDependencies, the shape of the five app packages. Registry-verified: `peerDependencies` carries the tier on all three, `dependencies` carries none of it. Every DS package that consumes the tier now declares it as a peer; the class is closed. |
| 2026-08-26 | **`SectionSet` closed** → **kol-component@0.71.0 · kol-theme@0.54.0**. `SectionText` (new molecule) — label · headline · body · actions, every slot opt-in, type by role (`headlineSize`), every default class a seam — and the five organisms on it: `SectionHero` (= FullBleedHero; text props render as SectionText in its own glass panel, `panel` still renders verbatim), `SectionSplit` (= FeatureSplit + `align: 'right' | 'left' | 'center'` — media side or one centred column; `flip` → `align="left"`), `SectionCards` (= FeaturesCardSection), `SectionCta` (= CtaGlobal), `SectionFaq` (new — SectionText over Accordion `{ q, a }`, `singleOpen`). The inspector's `Section` is `InspectorSection`. Every old name is a `@deprecated` alias with its old prop names mapped — nothing breaks, removed at the next major. The organisms emit `.kol-section-text-*` / `.kol-section-split-*` (theme 0.54.0; the old `.kol-feature-split-*` selectors sit on the same rules one release). Parity measured headless in the showcase, every node's rect/font/colour before vs after: split and CTA identical; the cards band lost one wrapper div the heading never needed (same 32px box); the composed hero puts every original node on the same pixels. Not folded (follow-up, not a blocker): `NewsletterBand` / `FramedMediaBand` / `FoundryCTA`. Not built: `ContentMedia` inside sections — a section's media is the caller's node, as today. |
| 2026-08-26 | **`SectionHeroRound2` closed** → **kol-component@0.76.0 · kol-theme@0.57.0**. One hero, seven asks, all in `SectionHero` (component 0.76.0 · theme 0.57.0). (1) `height` presets in viewport units: `full` = 100dvh (dvh, not vh — a phone's URL bar would push the foot off screen; sits under a fixed navbar, drop the pt-14 wrapper) · `80` = 70svh / 80vh · `60` = 50svh / 60vh; `lg` / `md` / `screen` stay as aliases. (2) `justify="end"` — items-end + StackHero's pb-32 → xl:pb-56 ramp. (3) `veil` — `.kol-section-hero-veil`, the bottom-heavy gradient, same device as the split's caption veil. (4) `foot` + `overlap` (default 250px) — the node renders across the fold, the hero owns the negative margin. (5) `media` as an ARRAY is the carousel: FeaturedCarousel is the engine (fullWidth, no header, one glass panel per slide; `autoPlay`, `autoPlayInterval`, `navPosition`), deprecated as a consumer import. (6) No media = the text-only hero: the composed SectionText on the surface, no glass panel. (7) `FoundryCTA` = `SectionCta variant="centered"` (rule · heading · mono line · Buttons), deprecated alias, same render. The split variant takes the same presets (`full` default). Measured headless: text-only 600px at 60 with no glass and no media; end at 80 → 800px with the veil and a 120px foot overlap; carousel `.is-full`, 2 slides, 600px, no header; centred CTA with rule, centred heading, two Buttons. Reference pages updated (`/components/section-hero`, `/components/section-cta`). |
| 2026-08-26 | **`SectionHeroCarouselSeams` closed** → **kol-component@0.76.1**. `SectionHero` forwards every FeaturedCarousel seam when `media` is an array: `renderTitle`, `ctaLabel`, `onNavigate`, `showTitle` / `showDescription` / `showCta`, `titleClassName`, `descriptionClassName`, `options` — passed only when set, so the engine's defaults hold otherwise. Measured on the showcase carousel demo: `renderTitle` renders the slide title in its own class (italic, display-02), the CTA reads the `ctaLabel` copy, and the anchor keeps its href for `onNavigate` to intercept. Reference page row added. |
| 2026-08-26 | **`SectionTextLabelVoice` closed** → **kol-component@0.76.2 · kol-theme@0.57.1**. One eyebrow voice across the set: `SectionText`'s label default is the hero's `kol-helper-12 text-meta` (12px, meta ink) instead of the split's mono-18 accent kicker; `SectionHero` drops its override; the theme's `.kol-section-text-label` is retired from the kicker rule (`.kol-feature-split-kicker` keeps painting for a consumer's own CSS until the next major). Measured on split, hero and FAQ: 12px / 500 / meta on all three. The ticket's "no case transform" line cites the retired law — the eyebrow is uppercase by ROLE (user ruling 2026-08-26, `.kol-section-text-eyebrow`, typography doc § Casing) and stays so; author the strings as you like, they render caps. |
| 2026-08-26 | **`SectionSplitMediaClip` closed** → **kol-component@0.76.3**. `SectionSplit mediaClip` (default `true`): `false` drops the frame's `overflow-hidden` — the frame keeps its ratio and radius, the node owns its own clipping, so a 3D-tilting node keeps its lifted corners. `mediaHover` and a self-animating node are noted as mutually exclusive in the JSDoc and the reference page. |
| 2026-08-26 | **`SectionCtaEditorial` closed** → **kol-component@0.76.4**. `SectionCta` editorial: the inner grid is `max-w-[1600px] mx-auto` — the surface stays full width, the wordmark and the rows stop hugging the viewport edges (measured at 2000: section 2000 wide, inner 1600, centred). `promptLabel` / `contactLabel` drop their `text-auto kol-helper-16` for the SectionText default — helper-12 · meta · uppercase by role — so the band wears the one eyebrow the set has. |
| 2026-08-26 | **`SectionRevealSeams` closed** → **kol-component@0.79.0 · kol-content@0.9.3**. Seams, not motion — nothing animates in the DS. `SectionText` takes `slotClass` / `slotStyle` maps keyed `label · headline · body · actions`, forwarded by `SectionHero` (all three routes), `SectionSplit`, `SectionFaq`, `SectionCta` (every row / the centred block) and the `SectionCards` header; `SectionCards` takes `itemClassName` + `itemStyle` (an object or `(index) => style`) onto each card and forwards them; `SectionCardItem` accepts `className` + `style` on its root (all three root forms). kol-content `ArticleHeader` takes `partClassName` / `partStyle` keyed `tags · title · meta · excerpt · hero`. A consumer with no reveal system passes nothing and sees today's render — the Section Set measured unchanged. Reference pages carry the rows. |
| 2026-08-26 | **`ChessBoardFluidCoordinates` filed from kol-chess.** Its 2026-08-26 mobile scan (390/768/1024): a fluid board's coordinate labels are sized by the `size` prop, so `fluid` always gets the desktop `p-2`/`kol-helper-12` pair and overlaps the pieces on a phone. Ask is a container-query sizing for the fluid case only |
| 2026-08-26 | **`ChessBoardFluidCoordinates` closed** → **kol-chess@0.7.1 · kol-theme@0.58.1**. `ChessBoard size="fluid"` sizes its coordinates from the square: `.chess-board--fluid` is a container (`container-type: inline-size`) and the coordinate wrappers wear `.chess-coord--fluid` — the helper voice at `clamp(7px, 2.2cqw, 12px)` with `max(2px, 0.8cqw)` inset — instead of the desktop `p-2` + `kol-helper-12` the prop fell through to. Measured on the showcase board: 390 → 35px squares, 7px labels, 2.2px inset; 1200 → 56px squares, 9.9px, 3.6px. Fixed sizes untouched. |
| 2026-08-26 | **`SectionHeroNoOverrides` closed** → **kol-component@0.80.0**. `SectionHero` renders `SectionText` bare — label · headline · headlineSize · body · actions · align · slotClass · slotStyle, plus `gap` only when the consumer passes it. The forced `gap-6`, `kol-sans-body-02` body, caps headline class and actions row are gone; the `display-04 → heading-02` remap is gone. What survives: `headlineSize` defaults per variant (media `display-04`, split `heading-02`) and the split hero's caps headline as `SectionText headlineCase="upper"` — a role on the molecule the consumer can set too. Measured side by side: hero body and split body both `kol-section-text-body` 16px, both gap 16px; split hero headline uppercase through the role. Reference pages updated. |
| 2026-08-26 | **`sidenav-rail-hairline` filed from kol-studio.** On kol-framework 0.24.0 the rail ships a right-edge hairline: `SideNav.jsx` L155 paints `bg-surface-primary border-r border-fg-08` whenever `background` (default true) is set, and `AppShell` forwards no SideNav props, so no consumer on the shell can switch it off without a CSS override of package chrome. User: the footer `border-t` is fine, the `border-r` is wrong — option as a prop, default without. Ask: a separate `hairline` prop default false; AppShell forwards SideNav props. Visual default changes, deliberately. |
| 2026-08-26 | **`appshell-content-surface` filed from kol-studio.** On kol-framework 0.24.0 rail and page are both `surface-primary`; brand.kolkrabbi.io — the reference, and the one app NOT on AppShell — paints its layout `bg-surface-tertiary` (BrandLayout.jsx L72, "the MAIN surface") under a primary rail. The package fork of that component has no surface class, so the page falls through to `kol-framework.css` `html, body { primary }`. Ask: the BrandLayout line on `.kol-brand-layout`, verbatim; no prop until a consumer wants the flat look. User: "sidenav primary, site tertiary, like brand." |
| 2026-08-26 | **`sidenav-rail-hairline` closed** → **kol-framework@0.26.0**. `SideNav`: `background` paints the surface only; new `hairline` (default `false`) paints the right-edge `border-r border-fg-08`; the footer's `border-t` is untouched. `AppShell` takes a `sideNav` object spread onto the rail — `sideNav={{ hairline: true }}`, `background`, `isActive`, `onNavigate` all reach it with no AppShell release. Measured on the showcase rail: default → border-right 0, surface painted, footer border-top 1px; `hairline` → border-right 1px. Default flip flagged BREAKING in the changelog. |
| 2026-08-26 | **`appshell-content-surface` closed** → **kol-framework@0.27.0**. `AppShell` renders `.kol-brand-layout` with `bg-surface-tertiary min-h-dvh` — BrandLayout's own line, verbatim — and the rail keeps its `bg-surface-primary`. Measured on the workbench AppShell story, both themes: light → layout `#ffffff` (= `--kol-surface-tertiary`), rail `#fafafa` (= `--kol-surface-primary`); dark → layout `#0e0e11`, rail `#121215`. Default flip flagged BREAKING; no prop for the flat look — nobody asked. |
| 2026-08-26 | **`SectionHeroSplitHeight` closed** → **kol-component@0.80.1**. `SPLIT_HEIGHTS` is a literal map beside `HEIGHTS` — `full` / `screen` / `lg` = `min-h-dvh`, `80` = `min-h-[70svh] md:min-h-[80vh]`, `60` / `md` = `min-h-[50svh] md:min-h-[60vh]` — no runtime rewrite, so Tailwind emits every class; the media half is `h-full min-h-0` so an absolute media node fills it. Measured on the Section Set at 1000 tall: full → 1000 with the media half at 1000; the `80` classes → 800; `60` → 600. |
| 2026-08-27 | **`SectionThemeInverse` closed** → **kol-component@0.81.0**. `theme` on `SectionHero` · `SectionSplit` · `SectionCards`: `inverse` = the paired theme of the nearest live one, following the toggle; `light` / `dark` pinned; omit to inherit. One attribute — `data-theme` stamped on the section root (plus the surface painted) — and every token inside resolves to the other theme's through kol-theme's subtree scopes (0.52.0), so Buttons, Pills, the glass panel flip for free; no inverse classes. Resolved in JS (`useSectionTheme`: an observer on `<html>` + the scheme query + the nearest stamped ancestor), so it follows the toggle and an inverse inside an inverse flips back. Measured on the showcase split hero: page light → section stamped dark, `#121215` ground, `#fafafa` ink; page toggled dark → section stamped light, `#fafafa` / `#121215`; toggled back → flips back. |
| 2026-08-27 | **`ThemeToggleLabelTarget` closed** → **kol-framework@0.27.1**. `ThemeToggle` renders `MODE_LABEL[next]` in all three label forms — "Dark mode" while light, "Light mode" while dark — what its `aria-label` / `title` ("Switch to … mode") already said. Icon and the system-follow hint untouched. Measured on the showcase toggle: unstamped/light → "Dark mode" · aria "Switch to dark mode"; page dark → "Light mode" · "Switch to light mode". |
| 2026-08-27 | **`SectionSplitHeight` closed** → **kol-component@0.82.0**. `SectionSplit height` — min-height on the family's ladder, the same three names as `SectionHero`: `full` = 100dvh, `80` = 70svh / 80vh, `60` = 50svh / 60vh, the default. Literal class strings, no runtime rewrite; the section is a flex column so the two columns stay vertically centred inside the min-height and the padding sits inside it. Measured on the Section Set at 1000 tall: computed min-height 600 / 800 / 1000 for 60 / 80 / full; the inner grid centred. BREAKING-flagged: every split that passes nothing now stands at least 60vh. |
| 2026-08-27 | **`SectionHeightLadder` closed** → **kol-component@0.83.0**. `height` on `SectionCards` · `SectionFaq` · `SectionCta` — min-height on the family's ladder, identical to split and hero: `full` = 100dvh, `80` = 70svh / 80vh, `60` = 50svh / 60vh, the default; one shared `sectionHeights` map, literal strings; each section is a flex column so its content stays vertically centred and the padding sits inside. Measured on the Section Set at 1000 tall, nothing passed: split · cards · CTA · FAQ all compute min-height 600 and the three content-light ones stand at exactly 600, centred. BREAKING-flagged: a section that passes nothing now stands at least 60vh. |
| 2026-08-27 | **`SectionHeightForty` closed** → **kol-component@0.84.0**. `40` = 35svh / 40vh on the shared `sectionHeights` map and on `SectionHero`'s own two maps, so every section with `height` — hero (media + split), split, cards, FAQ, CTA — takes it. Default stays `60` everywhere; nothing else moves. Measured on the Section Set at 1000 tall: the 40 classes compute min-height 400 on the CTA and on the split hero. |
| 2026-08-27 | **`SectionSplitMediaBounded` closed** → **kol-component@0.85.0**. The media frame takes its size from the section: every rung publishes `--kol-section-h`, the split publishes its vertical padding as `--kol-section-py`, and the frame's height is `calc(rung − 2 × padding)` with its width following `ratio` (`w-auto`, capped at the column, centred). The section's min-height stays for text-heavy columns. Measured on the Section Set at 1000 tall (4/5 media, lg padding 128): `full` → section 1000, frame 595×744 · `80` → 800, 435×544 · `60` → 600, 275×344 · `40` → frame 115×144 (the section stood at 483 because the text column is taller than 400). Four rungs, four card heights. Note the lg padding is 128 each side, so at `40` the frame is 144 tall — the rung minus the padding is what you asked for; if a low rung wants less air, that is the padding's ticket. The split hero already bounds its half by the row; nothing changed there. |
| 2026-08-27 | **`NotationPanelMoveDecorations` filed from kol-chess.** Its DS-compliance audit moved every local build onto its DS twin (AppShell, SectionText, SectionCardItem, ContentText, CodeBlock, Table, Tag, .kol-page); the one leftover is the review pane's own move list, which exists only because NotationPanel can't decorate a move. Ask is a trailing per-move render seam |
| 2026-08-27 | **`NotationPanelMoveDecorations` closed** → **kol-chess 0.8.0**. NotationPanel takes decorate(entry) => node, rendered trailing inside each move cell after the SAN (Badge, eval delta, or nothing); selection, disabled state and onSelectPly stay the panel's, sidelines untouched. Measured on the showcase demo: 12 move cells, 2 decorated, badge 41px after the cell's left edge behind the SAN, click on a decorated move moves selection. |
| 2026-08-27 | **`DisplayTightRamp` closed** → **kol-theme 0.59.0**. The display ramp is Right Grotesk Tight 500, one face, four rungs, no tracking: .kol-sans-display-01/02/03 move off Narrow, .kol-sans-display-04 (32/36/40) ships — SectionHero's media headline defaulted to that role since component 0.80.0 with no rule behind it. The elder voice (kol-display-lg / -section / -section-sm / -subsection) ships as aliases onto 01 / 02 / 03 / 03 keeping their uppercase, first CSS rows in 04-retirements.md; the gate now detects CSS aliases and lists importers (kol-website x8, kol-mirror x4, kol-chess x2). Measured all eight classes at 1280/768/390. |
| 2026-08-27 | **`SectionHeroFootClearance` closed** → **kol-component 0.86.0**. SectionHero justify=end + foot: the content's bottom inset is the pb ramp PLUS overlap — the ramp rides --kol-section-pb (8/10/12/14rem), overlap is published on the section as --kol-section-foot-overlap (0 without a foot), and the content padding is no longer the p-* shorthand (md:p-10 was winning the bottom back to 40px at 768-1023, before this change too). Measured on the showcase end demo at default overlap 250: padding-bottom 474 / 410 / 378 at 1280 / 900 / 390, the text clears the foot by exactly the ramp (224 / 160 / 128), foot rises 250 at every width. |
| 2026-08-27 | **`ContentCollectionCols` closed** → **kol-component 0.87.0**. ContentCollection cols={N}: one column below md, N from md (2-6, literal classes; the inline auto-fill template steps aside so the classes carry the tracks). min stays the default fluid wall, cols wins when both are passed, the list form ignores it. Gap is unchanged — the wall token is 24 flat, there is no 32-at-xl step in the DS; pass gap={32} if /stack wants it. Measured on the demo: 3 tracks at 1280 and 900, 1 at 390; fluid and list forms byte-identical. |
| 2026-08-27 | **`ListingCardThumbBorder` closed** → **kol-content 0.10.0 · kol-component 0.88.0**. ListingCard takes frame (default false) on the hero and default sizes — the hardcoded border border-fg-08 is gone, the bg-fg-04 tint stays, hero keeps its fg-16 hover step when frame is on; same contract as ContentCard's frame. And the ruling: ContentCard variant=article no longer frames its media by default — frame opts the hairline in. Measured on the comparison page, article variant: ListingCard thumb border 0 with the tint kept, ContentCard media border 0. |
| 2026-08-27 | **`ContentCollectionColsResponsive` closed** → **kol-component 0.89.0**. cols takes a breakpoint map: cols={{ md: 3, xl: 4 }} → one column below md, three from md, four from xl (sm · md · lg · xl · 2xl, counts 1-6, literal classes per rung). Number form unchanged. Measured on the demo: 4 tracks at 1400, 3 at 1279 and 900, 1 at 390. |
| 2026-08-27 | **`ListingCardHoverZoom` closed** → **kol-content 0.10.1**. ListingCard hero + default thumbs wear .kol-media-zoom — the content-card family's rule (1.06 / 600ms house ease, hover:hover only, reduced-motion opt-out) — the hero's own group-hover:scale-105 / 300ms is gone. Note the family's number is 1.06 / 600ms (ContentMedia), not the 1.03 / 300ms of CardFeatureItem the ticket cited; article cards were already on 1.06. Measured on the comparison page: ListingCard and ContentCard article both hover to matrix(1.06) over 0.6s. |
| 2026-08-27 | **`SectionNewsletter` closed** → **kol-component 0.90.0**. SectionNewsletter: SectionText (label · headline · body, centred) with the email form (Input + Button + aria-live status line) in its children slot, on the family's height ladder (height, default 60, content vertically centred), the shared --kol-container-max cap, theme, slotClass/slotStyle. NewsletterBand is a deprecated alias (title → headline, description → body, everything else passes through), on the retirement ledger; the showcase set, demo, mdx and inventory carry it. Measured on the section set: min-height 600 at 1400×1000 / 500 at 390, content centred at 0px offset, cap 1600, eyebrow uppercase, headline display-01; demo submit shows the success line. |
| 2026-08-27 | **`SectionNewsletterForm` closed** → **kol-component 0.90.1**. The form is w-full: SectionText centres its children as flex items, so the form shrink-wrapped and the Input's w-full had nothing to fill (~190px intrinsic). The field's shell now reads 520 at 1400 and 900, 390 (full width) at 390. bodyClass override dropped — the lede renders bare kol-section-text-body; the 64rem measure sits on a wrapper inside the family cap. |
| 2026-08-27 | **`SectionTextBodyMono` closed** → **kol-theme 0.60.0**. .kol-section-text-body is the mono-16 rung (JetBrains Mono 16 / 22, 72% ink unchanged) — the DS picked 16, the section scale where the Stack hero's lede already sat; sans is opt-in through bodyClass / slotClass. Every section rendering SectionText bare follows (hero, split, faq, cta, newsletter). Measured on the section set: split and newsletter bodies at JetBrains Mono 16px / 22px. |
| 2026-08-27 | **`StackCardHover` closed** → **kol-theme 0.61.0 · kol-component 0.90.2 · kol-content 0.10.2**. Hero rung on the media zoom: .kol-media-zoom.is-hero scales 1.02 — ListingCard size=hero wears it, ContentMedia takes zoom="hero" (ContentCard passes it through); the default size stays 1.06. Title dim: ContentText stamps kol-content-title-dim on the article title in both forms; the theme dims it to 70% / 200ms on .kol-card / .kol-row hover (keyed off the card root, not any .group ancestor), reduced-motion opt-out. Measured on the comparison page: ContentCard article title 1 → 0.7 over 0.2s, ContentRow 0.7, the is-hero rule at matrix(1.02). Note: ContentCard's root IS a Tailwind group (kol-card group) — the rule is on the card root anyway. |
| 2026-08-27 | **`SectionHeroFootGap` closed** → **kol-component 0.92.0**. justify=end with a foot: the content's bottom inset is overlap + 32px (48 from md), not overlap + the padding ramp; without a foot the ramp applies as before. Measured on the demo at default overlap 250: text bottom 48px above the foot at 1280 and 900, 32 at 390; foot rises 250 at every width. |
| 2026-08-27 | **`ContentFiltersCollection` closed** → **kol-theme 0.62.0 · kol-component 0.93.0**. The linked working-tree edits were already in the tree and have shipped in every kol-component since 0.86.0 (ContentFilters layout/onLayoutChange, strip items with onClick/active/title, trailingActions + Divider, leadingActions, belowActions; ContentCard controlStart) — controlStart now rides the theme's existing .kol-frame-control--top-left (same rule you asked for as --start), no inline style. New: SortHeader atom + SortControls molecule (click inactive → asc, click active → flip, one onSort per click; measured: one arrow on the active field, asc ≠ desc, rest desc == flipped desc), SizeOrDownload atom promoted verbatim with a real href (measured: size 1/label 0 at rest → 0/1 on hover, glyph 20px, 2s confirm kept), ToggleCheckbox variant=media (unchecked box on the media control's oq-12 plate, border transparent; checked unchanged — measured). |
| 2026-08-27 | **`ContentFiltersTitleGap` closed** → **kol-component 0.93.1**. The header title carries pr-4 by rule — 16px of right air on the title side, titleClassName untouched. Measured on the content-filters set: title text → divider 40px (24 gap + 16), divider → icon glyph 32, divider → frame 24 — the /stack tune, for every consumer. |
| 2026-08-27 | **`ContentRowsAndPrintCard` closed** → **kol-theme 0.63.0 · kol-component 0.94.0**. (1) Every ContentRow thumb is a fixed square box by rule — .kol-row > .kol-row-thumb is --kol-row-thumb wide, aspect 1/1, align-self flex-start, its child fills it; measured article 120×120 at the top of a 120 row, work 64×64. (2) Rows never zoom — thumbZoom gone; measured transform none on hover. (3) ContentCard print carries PrintGridCard: selected turns the card (0.4s ease-out, preserve-3d — measured matrix3d rotateY 180), the media fades in with loading=lazy (ContentMedia fade), onClick's event.currentTarget is the rect, role=button + tabindex 0 + Enter/Space on an onClick card. (4) The eyebrow has one name: kol-eyebrow is the theme role (measured mono 12 uppercase 0.06em fg-64), kol-card-kicker its alias on the ledger; eyebrow/eyebrowClass on ContentText (kicker/kickerClass alias) and SectionText + every section (label/labelClass/slot key label alias) — both aliases measured rendering. Prop aliases are documented on the components; the gate cannot see props, so only the class rides the ledger. |
| 2026-08-27 | **`TypefaceCardAndRow` closed** → **kol-theme 0.64.0 · kol-component 0.95.0**. (1) Row ink ladder as ruled: title kol-mono-14 uppercase text-emphasis, detail kol-mono-14 text-emphasis, date kol-mono-12 text-fg-64, body unchanged — measured on the comparison row: emphasis / 48 / emphasis / 64. (2) Card title = the row's string, kol-mono-14 uppercase text-emphasis — measured. (3) reveal on ContentCard (typeface / canvas): .kol-card.has-reveal — plate + media fade to 0, the reveal node to 1, 300ms house curve, hover:hover only, reduced-motion opt-out; measured plate 1→0, media 1→0, reveal 0→1. Without reveal nothing changes. |
| 2026-08-27 | **`FoundryComponentsReconcile` closed** → **kol-foundry 0.7.0**. The nine files are the site's copies, synced whole (diff vs _assets/local: 0 lines on six of them; the rest differ only by the drops — package-relative imports in FoundryCharacterSets/GlyphMetricsGrid, the SectionTitle opener rendered in-package on SpecimenSectionHeader (IconFrame + kol-btn-md text shell, lg → helper-20 else 16), and the grid's useNavigate → onNavigate(href, event) with linkComponent retired). TypefaceAlphabet ships and is exported. Peers: kol-component ≥0.95.0, kol-theme ≥0.64.0. TypefaceLibraryItem stays exported on the ledger. Rendered in the showcase: the grid's rows with the alphabet band and the typeface cards with reveal, the header opener, the alphabet clipping to its container. Pre-sync package files quarantined in _tmp/2026-08-27-foundry-pre-reconcile/. |
| 2026-08-27 | **`TypefaceCardRevealText` closed** → **kol-foundry 0.7.1**. FOUNDRY_SAMPLE_TEXT exported from glyphData (FontPreviewSection reads it); the grid's card reveal renders its first sentence in the face — rendered on the grid demo's GRID hover: 'Rennimjúkt eðal flauel, duft slæðist niður, silkislaufa & æðardúnn, fiður daðra dilur, friður.' in TGRoot at opacity 1. |
| 2026-08-27 | **`FontPreviewNoClamp` closed** → **kol-foundry 0.7.2**. The default SIZE_LADDER carries no lines, so no rung clamps; lineClamp stays an opt-in per rung. Rendered on the demo: the full passage at 96 / 64 / 48 / 24 — 30 / 20 / 14 / 7 lines, -webkit-line-clamp none, the text's bottom inside its box at every rung. |
| 2026-08-27 | **`ColumnBrowser` closed** → **kol-component 0.96.0 · kol-theme 0.65.0 · kol-icons 0.19.0**. ColumnBrowser shipped verbatim as a kol-component organism — the app bits are seams with defaults (urlOf, kindOf, kindLabel, formatSize, partition; displayKey falls back to the key under the level). Rendered: 528 tall, one column per segment, a file click opens the 320px preview with Kind · Type · Size · Dimensions · Date, → opens the folder's column. kol-icons 0.19.0 ships the columns glyph. ContentCard default carries no border (selected reads from the checkbox). ContentMedia's fit rules reach a consumer wrapper div. .kol-overlay is a flat surface-primary scrim. The row-thumb defect (§2) shipped in 0.94.0 / theme 0.63.0 — every row thumb is a fixed square. §5 (32px below row, reserved glyph, basename title) is consumer wiring and stays yours. |
| 2026-08-27 | **`FontPreviewClampAndBearings` closed** → **kol-foundry 0.7.3**. The ladder's clamp is back — 96/1 · 64/3 · 48/4 · 24/5 with the ellipsis (0.7.2 was my misread). Side bearings: the clamped element carries padding-inline 0.15em / margin-inline -0.15em (14.4px at 96, scaling down the rungs) and the wrapper no longer clips. Rendered: 1 / 3 / 4 / 5 lines, -webkit-line-clamp 1 / 3 / 4 / 5, wrapper overflow visible. |
| 2026-08-27 | **`FoundrySpecimenSections` closed** → **kol-foundry 0.8.0 · kol-component 0.97.0 · kol-theme 0.66.0**. (A) FoundryOpentypeFeatures · FoundryTypefaceDetails · FoundryTypefacePairing are in kol-foundry with the site's data as defaults (props override). (B) SectionCardItem renders text-only with no visual — the .feature-card frame + hover verbatim (kol-card-feature--text: fg-08 frame, 1% wash + 24% frame on hover, 300ms, hover only), no 96px icon; both sections are rows of it. PairingCard is a kol-foundry molecule on the ruled voices (tag kol-mono-14 uppercase text-emphasis, description kol-mono-12 text-meta, 96px Divider, .kol-pairing-card chrome). (C) GlyphMetricsSection: the axis rides the weight slot only — one dropdown on a roman-only face. (D) cells wear border-fg-08, outline gone. (E) a glyph the parsed font lacks (charToGlyphIndex 0) renders text-fg-24, no click, no hover. (F) VariableFontSection plate: no border, surface-secondary. (G) ContentFilters title gap 16 from md. Rendered: 4 tiles at min-h 180 / fg-08 → 24% on hover / no 96 icons; 3 pairing cards with a 96px seam; 1 dropdown; cell border 0.08; 5 glyphs dimmed on the demo font; plate border 0 on surface-secondary; h2 gap 16px. |
| 2026-08-27 | **`PageHeaderMonoTitle` filed** from kol-fxr — the masthead needs a mono title voice; monitor only has one by accident. |
| 2026-08-27 | **`PageHeaderMonoTitle` closed** → **kol-shell 0.7.0 · kol-theme 0.67.0**. PageHeader voice="mono" — the title rides three new mono heading rungs (kol-mono-heading-03 32 / 110%, kol-mono-display-03, kol-mono-display-02 on the display tokens at 100%; JetBrains Mono, weight 500) per size; voice="sans" is the default and unchanged; titleClass replaces the title role whole; the subtitle stays kol-mono-14. Measured on the demo: sm mono → JetBrains Mono 32px / 500, md → 48px / 500; sans untouched. |
| 2026-08-27 | **`PageHeaderMonoTitle` addendum** (kol-fxr, same day) → **kol-shell 0.7.1**: `SettingsScaffold header` spreads PageHeader props onto its own header, so `voice="mono"` reaches the Settings title; the addendum merged into the closed entry |
| 2026-08-27 | **`PageHeaderMonoTitle` addendum 2** (kol-fxr) → **kol-shell 0.7.2**: `SettingsSection` h2 is `text-fg-96 kol-helper-14` (the tab strip's role; was fg-80 / helper-16 with no seam) |
| 2026-08-27 | **`ColumnBrowserCursorStart` closed** → **kol-component 0.97.1**. The cursor is not drawn until the keyboard is used: arrows arm it, a click seeds it. Measured on the demo: at rest only the open folder is lit in column 0 and nothing in column 1; ArrowDown lights the cursor row (and opens it, Finder's move). |
| 2026-08-27 | **`ColumnBrowserOnPick` closed** → **kol-component 0.97.2**. onPick(file | null) fires whenever the picked file changes — a file row click or ↑/↓ landing on a file → the object, a folder pick or an outside prefix change → null. Driven on the demo: click a file → its key, ArrowUp → the previous file, click a folder → null. |
| 2026-08-27 | **`ColumnBrowserCursorSeed` closed** → **kol-component 0.97.3**. The cursor seeds on the open folder of the deepest column that has one (Finder's start), re-seeded when prefix changes from outside; internal moves land on the same spot. ↑/↓ that clamp without moving do nothing — no re-land, no onPrefix. Driven on the demo with prefix type/specimen/: at rest 3 columns and 0 onPrefix calls; ArrowUp at the seeded row → nothing (still 0 calls); ArrowRight → the deepest column's first file; ArrowDown → the next file. |
| 2026-08-27 | **`SettingsPanelChromeAndColumnPreview` closed** → **kol-component 0.98.0**. (1) SettingsPanel on the app register — title kol-helper-14 uppercase text-emphasis, subtitle / intro / hints / footer kol-mono-12 text-meta, row labels kol-mono-12 text-emphasis, no mono-10 left; SettingsChipRow renders the DS Tag (md · inverse when on · secondary when off) — the tag ticket; ShellDrawer's edge is oq-08. Measured on the drawer demo. (2) ColumnBrowser renderPreview(file) replaces the preview column's media frame (facts stay); without it images keep the organism's img (dimensions measured 1200 × 800) and everything else renders the new KindPreview molecule — your KindPreview promoted: HLS → HlsVideo, audio → AudioPlayer, text/code → CodeBlock by extension, the rest → AssetPlaceholder; markdown renders as code (ProsePreview is a specimen, not a renderer — it never took markdown). The DS media kinds (kindOf, extOf, isSystemFile, KINDS, KIND_LABEL — your lib/media kinds) ship from kol-component and are ColumnBrowser's defaults. Dimensions also come off any img a custom preview loads. Measured: notes.txt → a CodeBlock in the preview column; the KindPreview demo renders JSON as code and a TTF placeholder. |
| 2026-08-27 | **`ShellHomeSystem` filed** from kol-fxr — the shared Home · Library · Settings tier as one spec, thirteen items; folds in `PageHeaderMonoTitle`. |
| 2026-08-27 | **`ShellHomeSystem` closed** → **kol-shell 0.8.0 · kol-component 0.99.0 · kol-theme 0.68.0 · kol-icons 0.20.0 · kol-framework 0.28.0 · kol-brand 0.1.3 · kol-foundry 0.8.1**. All thirteen. (1) CatalogPage in kol-shell — PageHeader → ContentFilters (view strip in the header, LIST/GRID below) → the grid (cards repeat(6,1fr) gap 24 / rows repeat(4,1fr) gap 8, ContentCard / ContentRow catalog) → the action row; toCard(item, { view, layout }) is the contract, walkthrough the panel. (2) SettingsShortcuts — six columns × two, column-first, eyebrow over LabelRows. (3) shipped earlier today (shell 0.7.0–0.7.2). (4) .kol-eyebrow carries no ink — text-strong wins beside it (measured 0.8). (5) a catalog card with media zooms 1.06 and steps its frame fg-04 → fg-16 on hover by default (measured). (6) catalog detail truncates in the ramp. (7) nav-create/home/library/rack/settings in kol-icons — no registerIcons. (8) AppShell railToggleKey (never while typing; rail returns on route change) + touch='bare' (no shell on coarse pointer unless localStorage kol-desktop='1') / 'overlay' (TouchDeviceOverlay promoted). (9) THEME_BOOT_SCRIPT exported from kol-framework. (10) SettingsLinks + SettingsColophon. (11) .kol-app-shell ::selection neutral — AppShell wraps its tree. (12) opentype.js peer ^1.3.4 || ^2.0.0 on component + foundry. (13) kol-brand/svg/favicon-01.svg. Rendered on the showcase: catalog page with the mono masthead, strip, 6/4 columns, hover, truncation; shortcuts grid; links. |
| 2026-08-27 | **`WorkListingRowsAndFilters` closed** → **kol-component 0.100.0 · kol-theme 0.69.0**. (1) ContentRow work as ruled: title + type kol-mono-14 full ink, year kol-mono-12 fg-64, min-height 168, 16 padding at every width, thumb fills the content height (136×136 measured in a 168 row) with no hairline (thumbBorder false), frame transparent → fg-08 on hover (measured). (2) ContentCollection and ContentFilters' items panel declare container-type: inline-size (measured inline-size on the filters set); the work row's md step is gone — the rows were approved at base, so the container change moves nothing today. (3) the fluid group keeps 48px on its right (pr-12) and every group takes className / wrapClassName. (4) a group with ≤ 6 values stacks by default, stack: false opts out (measured: Kind with 5 values stacks with no prop). (5) .kol-tag--sm/md/lg carry their type; Tag sm's class list is kol-tag kol-tag--primary kol-tag--sm, 10px / 0.10em (measured). (6) thumb="fill" on any row — the square is the rung minus the vertical padding (the ruled 136), a definite number rather than a stretch (a min-height row has no definite cross size). (7) SectionText headlines wear kol-section-text-headline and balance (measured text-wrap: balance). |
| 2026-08-27 | **`SettingsPanelCompliance` closed** → **kol-component 0.100.1**. SettingsChipRow is the .kol-control chip again — kol-control kol-control-sm kol-mono-12, kol-control--filled when on, text-meta hover:text-emphasis when off, the count in text-meta — the string ViewToggle's text variant and the strips wear; 0.98.0's Tag pills were the DS's wrong call. Measured on the drawer demo: 0 .kol-tag in the drawer, on/off chips carry exactly that string, 0 uppercase. The rest of the panel was already on the register from 0.98.0 (helper-14 uppercase title, mono-12 rows and hints, oq-08 edge) and is untouched. |
| 2026-08-27 | **`KindPreviewMarkdown` closed** → **kol-component 0.101.0**. (1) KindPreview kind markdown fetches the file (200 KB cap) and renders it as HTML inside .kol-prose through the new markdownToHtml utility (exported; escape-first, no dependency — headings, paragraphs, emphasis, code, links, images, lists, blockquotes, fenced code, rules, pipe tables; script tags come out escaped). The workshop's parser emits tokens for its own viewer, which is not .kol-prose, so the DS renders HTML for the prose CSS. (2) json / yaml → CodeBlock with their language. (3) mediaKinds: md → markdown, json → json, yaml|yml → yaml; labels markdown · JSON · YAML; KINDS in your order. Rendered: the KindPreview demo's README shows h1 / strong / list / code in the prose voice; the column preview's README.md renders .kol-prose with Kind 'markdown'. |
| 2026-08-27 | **`SettingsPanelEyebrowAndDropdowns` closed** → **kol-component 0.102.0**. (1) SettingsSection — the DS SectionLabel (sm, helper-14 + the arrow) over the rows; Show kinds · Structure · Loading · Layout wear it on the demo (measured). (2) SettingsChoice renders the DS Dropdown, sm · primary, width the call site's (className w-40) — two dropdowns, zero strips in the drawer (measured). (3) SettingsChipRow allChip + onAll — an 'all' control chip first, the same .kol-control chip, filled when every option is on (measured: click → every chip filled). (4) The segmented strip is out of the drawer with (2); SegmentedToggle's default chrome is the 2026-08-15 state law (rest = raised tile, selected = bare ground) and I did not change it estate-wide off a drawer screenshot — if the default itself is wrong, that is a ruling on SegmentedToggle. |
| 2026-08-27 | **`CollectionItemMinWidth` closed** → **kol-component 0.102.1**. Every collection <li> is min-w-0 and the list track is minmax(0, 1fr); a truncated nowrap line can no longer hand its min-content width to the track. Verified statically (no server run, by your rule): the track string and the item class are in the shipped source; the grid form's cols classes were already minmax(0, 1fr) via Tailwind. Measure the /work row against the wall on your bump. |
| 2026-08-27 | **`TagTertiary` closed** → **kol-theme 0.70.0 · kol-component 0.102.2**. Tag variant="tertiary": secondary's fill (surface-primary), border transparent in every state, ink fg-80, the mono voice per size — sm 10 / md 12 / lg 14, weight 400, line-height 100%, letter-spacing 0.04em — hover 8% wash, active 16%, hash default true untouched. Verified in source only (no server run, by your rule): six .kol-tag--tertiary rules in the theme, the variant in the atom's map. Measure /work's rows on the bump. |
| 2026-08-27 | **`WorkCardAndShelf` closed** → **kol-component 0.102.3 · kol-content 0.11.0**. (1) ContentCard work: title one line (truncate), meta kol-helper-12 uppercase text-fg-inverse-80; the consumer passes the meta string. (2) ParallaxShelf renders ContentCard work by default on WorkCard's ladder (w-[280px] md:w-[400px] × 408/560 · 372/520 · 336/480 by index % 3), meta client ?? type label · year composed from the item, titleClass / metaClass / heights forwarded, renderCard still the seam; WorkCard no longer imported (stays exported, on the ledger). (3) the edge caption is kol-eyebrow text-fg-64. Verified in source only (no server run, by your rule); measure /work on the bump. |
| 2026-08-27 | **`SectionBackgroundProp` closed** → **kol-component 0.103.0**. background on all six sections through one SURFACES map (primary · secondary · tertiary · inverse · auto · none, or a raw utility / token string); defaults = what each painted before (Cta auto; hero / cards / newsletter primary under theme else none; split / faq none). Verified in source only (no server run, by your rule): every section root reads surfaceClass(background, …); all 21 gates clean. |
| 2026-08-27 | **`SettingsPanelApproved` closed** → **kol-component 0.104.0 · kol-theme 0.71.0**. The organism is the approved drawer: title only + a Divider under it; SettingsSection = kol-eyebrow text-fg-80 standing apart (gap-3) from a row stack (rowGap 1 / 2); SettingsRow = LabeledControl inline (uppercase helper-10 label at 160, hint on the control's title, switches at the far right, dropdowns fill via align="fill"); SettingsChoice a full-width Dropdown sm primary; SettingsMulti = one Dropdown of toggling entries (N of M kinds, ✓ on the on ones); SettingsFooter = Divider + refresh IconFrame; SettingsSwitch keeps title; ShellDrawer takes edge / shadow and the drawer passes neither. Defects: (1) .kol-popover-float z 210. (2) the lightbox scrim: the theme's .kol-overlay has been flat --kol-surface-primary since 0.65.0 and 0.70.0 did not touch it — there is no 88% ink rule on it in the theme; the wash you measured is not the DS's rule (the ShellDrawer backdrop .kol-overlay-scrim is #000 60%, a different element) — drop the override and re-measure on the bump; if it persists, it's a consumer stylesheet. (3) .kol-column-browser-preview .kol-prose zoom .5 and .kol-overlay .kol-prose on the content measure + 24px padding. (4) ColumnBrowser: picking a file in a column with an open folder collapses that folder and keeps the cursor on the file. (5) SettingsSwitch title. Verified in source only (no server run, by your rule); all 21 gates clean. |
| 2026-08-27 | **`ContentFiltersEqualColumns` filed** from kol-fxr — group columns equal by default; the ≤6-chip `shrink-0` default leaves the first column narrow on every surface. |
| 2026-08-27 | **`ContentFiltersEqualColumns` closed** → **kol-component 0.104.1**. Group columns are equal width by default — every group min-w-0 flex-1, chips wrapping inside; stack: true is the opt-in to hug. The 0.101 short-group default was the WorkListing page's ruling written into the organism — reverted. Verified in source only (no server run, by your rule). |
| 2026-08-27 | **`ContentFiltersEqualColumns` withdrawn** (kol-fxr, same day — the equal-columns ask was a misread) → **kol-component 0.104.2**: 0.104.1 reverted to the ≤ 6 hug default; the category label defaults to `kol-eyebrow text-fg-96` (what stood) |
| 2026-08-27 | **`ContentFiltersEqualColumns` corrected** — the equal-columns ask is withdrawn (the current default is the ruling); only the category-label → `kol-eyebrow` item stands. |
| 2026-08-27 | **`ContentFiltersFirstGroupHugs` filed** from kol-fxr — REVERT 0.104.1: `ContentFiltersEqualColumns` was a misread. The law: first group hugs, the rest flow (/prints); category labels are eyebrows. Write it into the component. |
| 2026-08-27 | **`ContentFiltersFirstGroupHugs` closed** → **kol-component 0.104.3**. The law is in: the FIRST group hugs (stack default = index === 0), every group after flows — by position, never by chip count; stack stays the explicit override. 0.104.1's equal columns and 0.101's ≤ 6 rule are both gone. The category label defaults to kol-eyebrow text-fg-96 (0.104.2). The law is written into renderFilterGroup's docstring, the ContentFilters mdx and the content-card-system doc, quoting you. Verified in source only (no server run, by your rule). |
| 2026-08-27 | **`SectionCtaConnectVariant` closed** → **kol-component 0.105.0**. SectionCta variant="connect" = the editorial layout with the contact copy as defaults (/ CONNECT · WORKING ON A PROJECT? · SEND A MESSAGE · CONTACT · hello@kolkrabbi.io), every prop still overridable; background / className as on every section. Verified in source only (no server run, by your rule). |
| 2026-08-27 | **`ShellHomeSystemMonitorGaps` filed from kol-monitor** — `SettingsShortcuts` (`combo`) vs `ShortcutsOverlay` (`keys`) disagree on the item field; `CatalogPage` needs `mutuallyExclusiveFilters` through and `expanded` / `expandedContent` in `toCard` before monitor's Library can sit on it |
| 2026-08-27 | **`ColumnBrowserMediaFacts` closed** → **kol-component 0.106.0**. ColumnBrowser facts: Kind · Type · Size · Dimensions (image + video) · Length (audio + video, m:ss) · Date — both off loadedmetadata captured on the media frame, so consumer renderPreview nodes count. AudioPreview · AudioTile · VideoTile promoted verbatim from kol-r2b2 (the current ruled versions — VideoTile with its play/pause control, not the ticket's native controls); KindPreview's video branch is VideoTile, its audio branch AudioTile; formatLength exported. Verified in source only (no server run, by your rule). |
| 2026-08-27 | **`ShellHomeSystemMonitorGaps` closed** → **kol-shell 0.9.0**. ShortcutsOverlay reads combo (keys tolerated for a release) — one array feeds both. CatalogPage: filtersProps spreads onto ContentFilters last (mutuallyExclusiveFilters and whatever comes next); toCard may return expanded / expandedContent, handed to ContentCard catalog. Verified in source only (no server run, by your rule). |
| 2026-08-27 | **`SettingsShortcutsComboWrap` filed from kol-monitor** — `⌘ K` / `+ / −` / `1–9` break across lines in `SettingsShortcuts`' 6 × 2 grid; ask: nowrap on the combo cell. Monitor carries a nowrap `comboLabel` meanwhile |
| 2026-08-27 | **`SettingsShortcutsComboWrap` closed** → **kol-shell 0.9.1**. SettingsShortcuts' combo cell is whitespace-nowrap; LabelRow's label keeps its 160 but yields (flex 0 1 160px, min-width 0) and truncates when the row is squeezed — the label is the prose, the combo the datum. 6 × 2 untouched. Verified in source only (no server run, by your rule). |
| 2026-08-27 | **`PauseIconFilled` closed** → **kol-icons 0.21.0**. playback/pause.svg replaced with the filled glyph verbatim — two bars at the play's weight and 0.75 radius, 5.25 → 18.75, fill currentColor, no stroke. The stroked one had no second home; quarantined at _tmp/2026-08-27-pause-stroked/. Verified in source only (no server run, by your rule). |
| 2026-08-27 | **`ShelfCardMotion` closed** → **kol-content 0.12.0**. ParallaxShelf's default card carries both: enter (default true) — the site's ShelfEnter verbatim, settled under reduced motion; tilt (default true) — TiltCard grounded in the media slot. Grid untouched. Verified in source only (no server run, by your rule). |
| 2026-08-27 | **`CatalogPageCardFit` filed from kol-monitor** — `CatalogPage` crops every card `cover`; the Empty 7U preset shows rails and no modules. Ask: `fit` through `toCard`. Monitor carries an `object-fit: contain` override meanwhile |
| 2026-08-27 | **`PlayDiscAndVideoBar` closed** → **kol-component 0.107.0 · kol-theme 0.72.0 · kol-icons 0.22.0**. (1) PlayDisc: AudioTile / VideoTile carry the Finder disc over full-bleed artwork — .kol-play-disc in kol-theme, declared after the IconFrame variants so nothing is restated; audio artwork = the ID3 cover (readCover promoted verbatim), video = the poster with the media element hidden. (2) VideoSheet promoted verbatim (skip-back-15 · play/pause · skip-forward-15 · elapsed · Slider scrubber with remaining · volume popover). (3) kol-icons: skip-back-15 / skip-forward-15 (the QuickTime badge), skip-back-bold / skip-forward-bold (3px chevrons), slider-01 knobs filled — the 1.5px skip chevrons stay for kol-chess. AudioSheet (cover above the player) not promoted — not in the ask. Verified in source only (no server run, by your rule). |
| 2026-08-27 | **`CatalogPageCardFit` closed** → **kol-shell 0.9.2**. toCard may return fit (cover default | natural | compact), handed to ContentCard per card — same shape as expanded / expandedContent. Verified in source only (no server run, by your rule). |
| 2026-08-27 | **`CatalogPageMonitorParity` filed from kol-monitor** — supersedes `CatalogPageCardFit`'s answer: the two GridCard fits (scale 0.5 / 0.3, top-left, clipped) are missing from `ContentMedia`, and `CatalogPage` must hide the 2×2's three neighbours itself (the consumer cannot see the filtered order). Monitor keeps its override rules and its own Library meanwhile |
| 2026-08-27 | **`CatalogPageMonitorParity` closed** → **kol-component 0.108.0 · kol-shell 0.10.0**. (1) ContentMedia fit natural / compact ARE GridCard's fits now — the image at 50 % / 30 % of its pixels, top-left, clipped (the theme rules verbatim); contain is gone, no consumer was on it. (2) CatalogPage skips the expanded card's three neighbours in its own render — your computeHiddenSet for the 6-column grid, verbatim. Verified in source only (no server run, by your rule). |
| 2026-08-27 | **`ContentMediaFocusBinding` filed from kol-monitor** — the media anchor is the fit's (`origin-top-left` in `natural` / `compact`, `center` on `.kol-media-zoom`), with no consumer seam; the user rules the focus a per-repo binding. Ask: `--kol-media-focus`, read on the fit and the zoom, unset = today. No carry in monitor — the fit's top-left is its value meanwhile |
| 2026-08-27 | **`ContentFiltersFirstGroupFixedWidth` filed from kol-monitor** — user ruling with monitor's TYPE column beside `/work`'s: the first filter group is NOT a hug, it is a set width — one catalog column (`1fr` of the 6-col grid, gap 24), so it sits over the first card; groups after it flow as before. Reverses the width half of `ContentFiltersFirstGroupHugs`; the by-position rule stands. Monitor carries `className: 'w-40'` on its first groups meanwhile. Announced in the LLM_RULES bulletin |
| 2026-08-27 | **`ShellPageWash` filed from kol-monitor** — user ruling: the back of the back is surface-primary and a transparent fg wash on top steps the lightness up (secondary merged with the module plates; an opaque swap hides the structure). Ask: `AppShell` paints the back + `pageWash` prop → `--kol-shell-page-wash`; `PageShell` reads it with a primary fallback. Monitor carries a `bg-surface-primary` RailFrame + an unlayered fg-12 rule meanwhile; fxr and mirror wait on the prop |
| 2026-08-27 | **`FullConsumptionContract` filed from kol-monitor** — an agent in a consumer repo cannot answer "am I fully consuming the DS?", so it guesses wrong (this session called `.bg-fg-*` Tailwind and passed `var(--kol-fg-12)` through JSX three times, against `kol-opacity.css`'s own stated rule). Two asks: a `/core` theme entry so a consumer skips the 81 KB of domain packs it can never render, and a written definition of full consumption plus a check that certifies a repo against it |
| 2026-08-31 | **`ButtonGroupResponsiveGap` closed** → **kol-component@0.147.0**. The gap is responsive now: gap-2 stacked, sm:gap-4 as a row. Your read was the right one — the group changes axis at sm, so one fixed 16 was doing horizontal separation between two side-by-side buttons AND vertical separation between two full-width stacked ones, and those do not want the same number. Shipped as a responsive default rather than a prop, for the reason you gave: the stacked case IS the narrow viewport, so the value is pickable once instead of per consumer. Nothing moves at sm and up. Note className still lands on the OUTER container by design — with the gap responsive there is nothing left to reach for. |
| 2026-08-31 | **`CardFeatureZoomScale` closed** → **kol-theme@0.114.0**. The card publishes --kol-card-feature-zoom from a per-feature zoom prop (feature.zoom through SectionCards, alongside backgroundColor and imageAspectRatio as you suggested) and kol-animation.css reads it with 1.03 as the fallback, so nothing moves for anyone who does not set it. Per-feature rather than per-set, because your own evidence is that one set holds both kinds: line-art on white needing more and dark UI screenshots correct at 3%. Requires kol-component >=0.147.0 for the prop. NOT SHIPPED, and deliberately: the shared in-view attention state. You are right that two components now need it and that a card cannot hold hover on touch, but making .is-viewing a DS-wide behaviour across the card set is an architecture call that binds every card kind, not a fix to this rule — it wants its own ticket and the user's ruling. Keep your local hook until then. |
| 2026-08-31 | **`SectionNewsletterFullBleed` closed** → **kol-component@0.147.0**. fullBleed shipped on SectionNewsletter. The breakout literal is SectionHero's, character for character (w-screen ml-[calc(50%-50vw)]) rather than a second mechanism — you were right to ask, and two organisms in one family inventing two ways to leave a gutter is exactly how they drift. The section's own px-5 sm:px-8 then re-insets the content, so only the fill moves, which is the behaviour your stopgap was getting by hand. Not used: .kol-full-bleed — container-relative, and you already hit it over-bleeding tonight. NOT DONE: hoisting fullBleed to the whole section family. It is the right question and it binds eight organisms, so it is a ruling rather than a fix; file it and it gets built. |
| 2026-08-31 | **`ChessBoardInputAndVariantSeam` closed** → **kol-chess@0.10.0**. All three parts are in. 1 and 2 (drag + a11y) shipped in 0.9.0 from your prototype, with one correction: the diff's click-suppression was wrong in both directions. Instrumenting the real DOM showed the click after a SELECTING press never fires at all — the press re-renders the square, React replaces the node the mousedown landed on, and the browser drops the click for want of a common ancestor. A press on your own piece is authoritative now, and the skip flag resets on every pointerdown rather than only when consumed, because a flag cleared by a click that may never come is a latch that eats the NEXT real click. 29 browser checks green including real CDP touch. 3 is now built rather than ruled: dests takes Map<from,to[]> or a plain object, supplied legality WINS OUTRIGHT (not merged with chess.js, not checked against it — a variant's rules are not a superset of standard chess, so a board consulting both would emit moves neither engine agreed to), and canPick follows it because chess.js's turn is not a safe proxy in a 960 position. The ENGINE IS NOT SWAPPED and that is the decision: chess.js stays the default so no current consumer moves, and a consumer that wants chessops supplies dests from chessops rather than this package taking the dependency for everyone. That makes the engine swap your later choice instead of a precondition. Regression-checked with no dests supplied: tap, drag, keyboard, illegal-move and enemy-piece paths all unchanged. Not screen-verified WITH dests — no showcase surface passes it, so that branch is source-verified only. |
| 2026-08-31 | **`ShellRailNoDrawerOnMobile` closed** → **kol-shell@0.31.0**. touch=drawer + drawerBelow (default 768), in the shape you suggested. Both blockers are fixed upstream rather than worked around. The inline width token: NavRail takes a drawer prop, so in drawer mode the grab strip is not rendered, useRailDrag returns before it runs, and the rail sizes from --kol-shell-drawer-width instead of the live token — nothing writes --kol-shell-rail-width on :root per pointermove, and the fold carries NO !important anywhere because there is no longer an inline style to fight. The forced show on navigate: the effect is mode-aware now, drawer closes and railToggleKey still restores, which also unblocks navHidden as a consumer seam — your read on child-before-parent effect order was right. JS owns the breakpoint and stamps data-rail-drawer on the shell root; the theme keys off that attribute rather than a media query, because the width is a prop and a query would be a second source of truth for it. The trigger ships inside AppShell (32px, hamburger/x, aria-expanded) so it lands once rather than in every consumer's page header. WARNING BEFORE YOU DELETE ANYTHING: this repo has no AppShell surface in its showcase, so the drawer has never been rendered in a browser here — 25 gates and the source are verified, the screen is not. Your consumer-side fold is the reference implementation. Confirm on device FIRST, then delete. |
| 2026-09-01 | **`ContentCollectionMinColumnWidth` closed** → **kol-component@0.148.0**. Shipped, and I picked the number — but not the one your measurements argue for, and the reason matters. cols is a CEILING now rather than a command: the breakpoint rungs publish --kol-wall-cols and one static template turns it into 'at most N, never narrower than the floor' via repeat(auto-fill, minmax(min(100%, max(floor, (100% - (N-1)*gap)/N)), 1fr)). All CSS — no measurement, no observer, and it works inside the container query the wall already establishes. THE FLOOR DEFAULTS TO 320px, not 360. Your datum argues for ~360, and for a roster row it is right — but that number is about a row two truncated lines tall, and this floor governs every kind in the collection. 320 is the width this DS has already ruled as the narrowest acceptable track and has lived with as min's default; carrying an existing ruling across to the cols path is a different thing from minting a new estate-wide law out of one page's evidence. So: minCol defaults to min, raising min raises both, and a wall whose content needs more says so. FOR YOUR PAGE, PASS minCol=360px — that is the seam you asked for and your measurements are the reason it exists. Note this DOES change existing renders, but only walls that were drawing tracks under 320px, which the DS's own default already calls too narrow. Verified in a browser: the floor holds at every width from 1440 down to 360 with no overflow, and the fluid path is unchanged (2152/6, 1392/4, 1232/3, 720/2, 342/1). |
| 2026-09-01 | **`ContentGridMinColumnWidth` closed** → **kol-component@0.149.0**. Shipped exactly the idiom you named: min and listMin now emit minmax(min(<value>, 100%), 1fr). Identical above the breakpoint, collapses to the container below it. Verified in a browser down to a 190px wall — the wall never overflows at any width, and the fluid counts are unchanged where there is room. Your diagnosis of WHY it took a container-level measurement to find is the part worth keeping: the page itself never overflowed, only the nearest overflow-x ancestor did, so from the outside it presents as a broken gutter. Same evening, kol-chess reported the same class of thing as a layout problem. That is the second time in one day a track-width defect was reported as something else. Note the sibling that shipped an hour earlier: ContentCollectionMinColumnWidth made  a CEILING with a floor (minCol, defaulting to min), so the count path is guarded too — between them a wall can neither demand more than its container nor take a column narrower than the floor. |
| 2026-09-01 | **`SectionFamilyFullBleed` closed** → **kol-component@0.149.0**. Hoisted. One literal in sectionBleed.js; SectionHero, SectionSplit and SectionNewsletter now import it instead of each carrying their own copy, and SectionCards, SectionCta and SectionFaq gained the prop. Six organisms, one breakout, one place to change it. Default false everywhere and verified: all six render at their existing width with margin-left 0 and no page overflow, so nothing moves until a consumer passes it. You were right that the SectionNewsletter copy was the argument rather than the mistake — the second organism to need a thing had to duplicate the first, and the third would have too. On your note about an organism that should never bleed: none is excluded. Every member can be a filled surface, so every member can have its fill clipped by the gutter; a member that should not bleed is a decision for the page, not a prop this family withholds. And confirming your read on .kol-full-bleed — it is container-relative and over-bleeds in a parent with no gutter, which is why this is viewport-relative and does not care what it is nested in. |
| 2026-09-01 | **`CardSetInViewAttention` closed** → **kol-component@0.149.0**. Promoted to the set. useInViewAttention is exported from kol-component: the centre-band IntersectionObserver TiltBento shipped in 0.145.0, carried unchanged at rootMargin -45% rather than re-derived, one element active, no cross-card coordination and no shared store. TiltBento now uses the hook instead of its private copy, and SectionCardItem stamps data-attention when it holds the centre on a coarse pointer. On your third point — that it should be the hook the existing hover rules already use — that is exactly how it landed: data-attention was added to the SAME theme rule as :hover rather than given rules of its own, so the two cannot drift. Reduced-motion opts out of both. coarseReveal='static' is the escape on both components, and SectionCards forwards it for a whole wall. Verified: fine pointer unchanged (nothing stamped at rest, hover still scales 1.03), and on touch at 390 each of the three cards is stamped and zoomed alone as it takes the centre. TiltBento re-checked after the refactor — still one card open at a time. DELETE useMobileActiveCard.js and the .is-viewing block; that behaviour is in the package now. |
