export const meta = {
  id: "2.36.0",
  slug: "portal-footer",
  title: "PortalFooter",
  type: "reference",
  status: "active",
  updated: "2026-07-30",
  tags: [
    "domain/components",
    "domain/design-system",
    "pattern/app-shell",
    "pattern/wayfinding",
  ],
  description: "Site footer chrome.",
  aliases: [
    "portalfooter",
  ],
  reuses: [
    "kol-helper-12 2★",
    "kol-portal-footer 1★",
    "kol-sans-heading-03 1★",
  ],
}

<Preview name="PortalFooter" />

## Installation

<Install name="PortalFooter" />

## Usage

```jsx
import { PortalFooter } from '@kolkrabbi/kol-framework'

<PortalFooter brand={<Wordmark />} columns={columns} socials={socials} note="© 2026 Kolkrabbi" />
```

## Behavior

Zero-prop by default: the Kolkrabbi favicon mark + credit line. Opt-in slots
scale it to a full site footer — `brand` (wordmark block), `columns` (link
columns), `socials` (bottom-bar strip; label renders as text when no icon and
feeds the aria-label), `note` (legal/credit line), `children` (escape hatch
between columns and the bottom bar). Links are plain `<a href>` —
router-agnostic.

## Props

<Api name="PortalFooter" rows={[
  {"prop":"brand","type":"ReactNode","def":"—","desc":"Brand block slot; zero-prop default keeps the favicon mark."},
  {"prop":"columns","type":"array","def":"—","desc":"Link columns: { title, links: [{ label, href }] }. Plain anchors, router-agnostic."},
  {"prop":"socials","type":"array","def":"—","desc":"Bottom-bar links: { label, href, icon? }; label renders when no icon."},
  {"prop":"note","type":"ReactNode","def":"—","desc":"Bottom-bar legal/credit line."},
  {"prop":"children","type":"ReactNode","def":"—","desc":"Escape hatch between columns and the bottom bar."}
]} />
