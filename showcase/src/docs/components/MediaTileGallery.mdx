export const meta = {
  id: "2.27.0",
  slug: "media-tile-gallery",
  title: "MediaTileGallery",
  type: "reference",
  status: "active",
  updated: "2026-07-30",
  tags: [
    "domain/components",
    "domain/components/organisms",
    "domain/design-system",
    "domain/layout",
    "pattern/media",
  ],
  description: "A stack or grid of framed media tiles that open THE shared fullscreen MediaViewer at the clicked tile, paged across all items (the monorepo's FullscreenGallery recreated on the shared viewer instead of its nav-less single image; the AssetFigure/AssetGrid deps are dropped — tile and grid are inlined).",
  aliases: [
    "mediatilegallery",
  ],
  reuses: [
    "MediaViewer 3★",
  ],
}

<Preview name="MediaTileGallery" />

## Installation

<Install name="MediaTileGallery" />

## Usage

```jsx
import { MediaTileGallery } from '@kolkrabbi/kol-component'

<MediaTileGallery items={items} layout="grid" cols={3} />
```

## Behavior

A stack or grid of framed media tiles that open **THE shared
[MediaViewer](/components/media-viewer)** at the clicked tile, paged across
all items. Tiles are real `<button>`s, so Space + Enter activation comes
free. Captions render verbatim — authored case, no auto-casing.

## Props

<Api name="MediaTileGallery" rows={[
  {"prop":"items","type":"array","def":"[]","desc":"Tiles, in the viewer's media shape: { url, alt?, kind?, caption? }."},
  {"prop":"layout","type":"'stack' | 'grid'","def":"'stack'","desc":"Flow column vs cols-column grid."},
  {"prop":"cols","type":"number","def":"4","desc":"Grid column count (grid layout only)."},
  {"prop":"tileClassName","type":"string","def":"''","desc":"Extra classes merged onto each tile button."}
]} />
