export const meta = {
  id: '2.22.0',
  slug: 'hls-video',
}

<Preview name="HlsVideo" />

## Installation

<Install name="HlsVideo" />

## Usage

```jsx
import { HlsVideo } from '@kolkrabbi/kol-component'

<HlsVideo src="https://cdn…/master.m3u8" className="absolute inset-0 h-full w-full object-cover" />
```

## Behavior

Background/decorative HLS streaming on a single native `<video>`: hls.js
attaches the `.m3u8` stream (Safari falls back to native HLS) and the instance
is destroyed on unmount/src change. **Deliberately inert** — pointer-events
none plus the full hardening set (no controls, PiP, context menu, download,
fullscreen, remote playback); do not strip these without explicit approval.
All layout/sizing arrives via `className` — the consumer decides
`absolute inset-0 object-cover` etc.

## Props

<Api name="HlsVideo" rows={[
  {"prop":"src","type":"string","def":"—","desc":"HLS manifest URL (.m3u8); attach effect re-runs on change."},
  {"prop":"poster","type":"string","def":"—","desc":"Poster frame before playback."},
  {"prop":"onEnded","type":"function","def":"—","desc":"End-of-playback callback; its presence disables loop."},
  {"prop":"className / …props","type":"—","def":"—","desc":"Layout is consumer-supplied; extra props spread onto <video>."}
]} />
