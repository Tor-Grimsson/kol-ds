export const meta = {
  id: "2.25.0",
  slug: "label",
  title: "Label",
  type: "reference",
  status: "active",
  updated: "2026-07-30",
  tags: [
    "domain/components",
    "domain/components/atoms",
    "domain/design-system",
    "pattern/input",
  ],
  description: "Minimal form label.",
  aliases: [
    "label",
  ],
}

<Preview name="Label" />

## Installation

<Install name="Label" />

## Usage

```jsx
import { Label } from '@kolkrabbi/kol-component'

<Label htmlFor="email">Email</Label>
```

## Behavior

Minimal by design: **inherits typography from its parent** — supply
`kol-helper-N` (or similar) via `className` when a specific voice is needed.
Defaults to `text-fg-48` for the muted-control look. For a label wired to a
control body, use [LabeledControl](/components/labeled-control).

## Props

<Api name="Label" rows={[
  {"prop":"children","type":"ReactNode","def":"—","desc":"Label text."},
  {"prop":"htmlFor","type":"string","def":"—","desc":"Associated input id."}
]} />
