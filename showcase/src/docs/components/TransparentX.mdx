export const meta = {
  id: '2.63.0',
  slug: 'transparent-x',
}

<Preview name="TransparentX" />

## Installation

<Install name="TransparentX" />

## Usage

```jsx
import { TransparentX } from '@kolkrabbi/kol-component'

<TransparentX />
```

## Behavior

Diagonal stroke indicator for transparent / disabled / unused slots — the
universal "no value" affordance. Renders absolute-positioned to fill its
parent: **the parent must be `position: relative`** (and `overflow: hidden`
for a clean corner clip). Non-scaling stroke keeps the line 1px at any
container size. `tone` maps to `var(--ui-{tone})` — warning (default) /
error / info / success.

## Props

<Api name="TransparentX" rows={[
  {"prop":"tone","type":"string","def":"warning","desc":"Colour tone of the marker."},
  {"prop":"className","type":"string","def":"—","desc":"Additional classes."}
]} />
