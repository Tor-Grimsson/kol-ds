export const meta = {
  id: "2.10.0",
  slug: "code-block",
  title: "CodeBlock",
  type: "reference",
  status: "active",
  updated: "2026-07-30",
  tags: [
    "domain/components",
    "domain/components/molecules",
    "domain/design-system",
    "domain/iconography",
    "domain/typography",
    "pattern/display",
  ],
  description: "REPLICATED from the elder reference (kol-website/packages/ui/src/molecules/CodeBlock.jsx, 2026-07-28 user mandate): react-syntax-highlighter (Prism) with the oneDark theme flattened onto KOL chrome — transparent bg, 14px/1.6 mono, no text-shadow — a single filename-or-language chip, and the CopyButton atom positioned in-frame (.kol-codeblock-copy).",
  aliases: [
    "codeblock",
  ],
  reuses: [
    "Icon 3★",
    "kol-codeblock 1★",
    "kol-codeblock-copy 1★",
    "kol-codeblock-filename 1★",
    "kol-codeblock-wrapper 1★",
    "kol-doc-figure 1★",
  ],
}

<Preview name="CodeBlock" />

## Installation

<Install name="CodeBlock" />

## Usage

```jsx
import { CodeBlock } from '@kolkrabbi/kol-component'

<CodeBlock language="jsx">{code}</CodeBlock>
```

## Behavior

**THE code surface** (one-code-idiom law, 2026-07-30): every code block on a
doc page renders through this component — react-syntax-highlighter (Prism)
with the oneDark theme flattened onto KOL chrome, a single
filename-or-language chip, and the [CopyButton](/components/copy-button)
atom positioned in-frame.
Three input shapes, first match wins: Portable Text
(`value={{ code, language, filename }}`), direct props, or children. The
`bare` prop drops the framed chrome for hosts that own the frame (e.g. a
preview figure's Code tab) while keeping the code surface itself.

## Props

<Api name="CodeBlock" rows={[
  {"prop":"children","type":"ReactNode","def":"—","desc":"Code content (stringified for copy)."},
  {"prop":"language","type":"string","def":"—","desc":"Language tag shown in the corner."}
]} />
