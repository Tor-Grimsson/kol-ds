export const meta = {
  id: "2.30.0",
  slug: "menu-dropdown-item",
  title: "MenuDropdownItem",
  type: "reference",
  status: "active",
  updated: "2026-07-30",
  tags: [
    "domain/components",
    "domain/components/molecules",
    "domain/design-system",
    "pattern/overlay",
  ],
  description: "Action row inside a MenuItem's dropdown panel.",
  aliases: [
    "menudropdownitem",
  ],
}

<Preview name="MenuDropdownItem" />

## Installation

<Install name="MenuDropdownItem" />

## Usage

```jsx
import { MenuDropdownItem } from '@kolkrabbi/kol-component'

<MenuDropdownItem onClick={fn}>Save</MenuDropdownItem>
```

## Behavior

The row atom of the [MenuItem](/components/menu-item) family — one action per
row inside the popover panel. The same rows also serve
[ShapeDropdown](/components/shape-dropdown)'s variant menu.

## Props

<Api name="MenuDropdownItem" rows={[
  {"prop":"children","type":"ReactNode","def":"—","desc":"Main label (truncates)."},
  {"prop":"onClick","type":"() => void","def":"—","desc":"Fires before the menu closes."},
  {"prop":"iconLeft","type":"ReactNode","def":"—","desc":"Leading icon in a fixed column."},
  {"prop":"shortcut","type":"ReactNode","def":"—","desc":"Trailing shortcut / marker."},
  {"prop":"disabled","type":"boolean","def":"false","desc":"Dims and blocks activation."}
]} />
