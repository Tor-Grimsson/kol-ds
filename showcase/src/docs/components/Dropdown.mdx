export const meta = {
  id: '2.16.0',
  slug: 'dropdown',
}

<Preview name="Dropdown" />

## Installation

<Install name="Dropdown" />

## Usage

```jsx
import { Dropdown } from '@kolkrabbi/kol-component'

<Dropdown options={opts} value={v} onChange={setV} />
```

## Variants

**The trigger IS button chrome** (2026-07-08 chrome law): it emits
`kol-btn kol-btn-{variant} kol-btn-{size}`, so it renders pixel-identical to a
Button of the same rung — the `.kol-dd-*` classes only add trigger layout and
the open/panel fusion. `primary` (default) is the filled trigger whose open
panel continues the same fill as one piece; `grey` is the opaque oq-12 fill;
`outline` is the bordered pair. Dropdowns have **no hover and no clicked
state** (2026-07-15 ruling) — rest and open are the only states. For menu
actions use [MenuItem](/components/menu-item); this is value-list selection.

## Props

<Api name="Dropdown" rows={[
  {"prop":"options","type":"{ value, label }[]","def":"[]","desc":"Selectable entries."},
  {"prop":"value","type":"string","def":"—","desc":"Selected value (defaults to first)."},
  {"prop":"onChange","type":"(value) => void","def":"—","desc":"Fires with the chosen value."},
  {"prop":"variant","type":"primary | outline","def":"primary","desc":"Button chrome on the trigger (chrome law). Legacy aliases: default/subtle → primary, minimal → outline."},
  {"prop":"size","type":"sm | md | lg","def":"auto","desc":"Overrides the viewport-derived size."}
]} />
